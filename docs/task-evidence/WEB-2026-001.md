---
task_id: WEB-2026-001
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 89c5f298
tests: PASS
e2e_staging: NOT_RUN
migration: N/A
migration_na_reason: "The transaction-state hardening uses the existing order, payment, and idempotency tables; no schema or persisted-data migration was added."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Staging/Docker E2E with a real customer session, a real payment-provider callback, transport timeout, replay, and duplicate-submit verification."
known_blockers: "Staging/payment-provider runtime access was not available for this local implementation turn."
locally_actionable_remaining: NONE
unblock_condition: "Provide staging/payment-provider runtime access, then run authenticated transaction recovery with a real payment callback, transport timeout, replay, and duplicate submit."
dependency_chain_blocked: true
next_eligible_task: NONE
updated_at: 2026-09-01
---

## Scope Implemented

- On-demand customer order creation now sends a stable `X-Idempotency-Key`, validates `success === true`, and requires a non-empty persisted server order id before any success path.
- A timeout or transport-level unknown outcome is surfaced as pending/retry. The key and a non-PII payload fingerprint are retained in session storage so a retry can replay the same request; an idempotency-in-progress response is also treated as retryable.
- A successful create response is stored with its order id before payment initialization. Refresh/retry can reconcile that order through the authenticated server detail endpoint instead of creating another order.
- Payment-session initialization has the same server-response validation and stable per-order idempotency key. Payment success is only emitted after the server-owned payment check reports `paid`; pending state has an explicit status-retry action.
- Aggregator helper code now reuses the shared idempotency-key generator without changing its server contract.
- Aggregator order and payment-session helpers now also require an explicit `success: true` response from the server, preventing a response with only an order/payment-shaped payload from being treated as persisted success.
- Local transaction tests cover a transport timeout followed by a replay of both order creation and payment-session initialization using the same idempotency key.

## Files Changed

- `frontend/src/app/(portal)/orders/new/ondemand/page.tsx`
- `frontend/src/components/orders/PaymentModal.tsx`
- `frontend/src/hooks/useCreateAggregatorOrder.ts`
- `frontend/src/lib/orderTransaction.ts`
- `frontend/src/tests/unit/orderTransaction.test.ts`
- `frontend/src/tests/unit/PaymentModal.test.tsx`
- `frontend/src/tests/unit/useCreateAggregatorOrder.test.ts`

## Commands / Checks Run

- `npm test -- --run src/tests/unit/orderTransaction.test.ts src/tests/unit/PaymentModal.test.tsx src/tests/unit/useCreateAggregatorOrder.test.ts` — PASS, 3 files / 9 tests.
- `npm test -- --run src/tests/unit/orderTransaction.test.ts src/tests/unit/useCreateAggregatorOrder.test.ts src/tests/unit/PaymentModal.test.tsx` — PASS, 3 files / 14 tests, including timeout/replay with key reuse and explicit aggregator server confirmation.
- `npm test -- --run` — PASS, 6 files / 15 tests.
- `npx tsc --noEmit` — PASS.
- `npm run lint` — PASS, 0 errors; repository reports 497 pre-existing warnings.
- `npm run build` — PASS; Next.js webpack build compiled and generated all 29 app routes.
- `git diff --check` — PASS.
- `graphify update .` — PASS; graph refreshed to 17,090 nodes, 31,425 edges, 1,163 communities. Existing SQL parser/edge-relation warnings remain tooling limitations.
- `python3 scripts/tasks/validate_task_evidence.py` — NOT_RUN: `python3` is unavailable in this Windows environment.
- `py scripts/tasks/validate_task_evidence.py` — PASS, task evidence scan 327 sections / 19 with checked boxes.

## Reality Gate Evaluation

- `REALITY-2026-003`: BLOCKED. The local implementation and automated checks are real, but staging/Docker payment-provider and transport-timeout evidence is not available yet; no local actionable work remains.
- `REALITY-2026-011`: PASS. No local success path is allowed without a persisted order id or server-owned `paid` status; unknown transport outcomes remain pending/retry and no provider callback was fabricated.

## Unproven / Remaining

- Run the customer flow against Docker/staging with an authenticated session and real configured payment sandbox/provider callback.
- Prove create-request timeout after server commit replays the same idempotency key and does not create a second order.
- Prove payment-session timeout/retry and `payment/check` replay with the same key across a browser refresh.
- Verify the rendered pending/retry state in the deployed web container and record the runtime request/order references without storing PII or payment secrets.

Local implementation and automated coverage is complete; no local actionable work remains.
