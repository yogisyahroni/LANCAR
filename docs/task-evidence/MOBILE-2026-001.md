---
task_id: MOBILE-2026-001
status: BLOCKED

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: 29f287eb

tests: PASS
integration: PASS
e2e: PARTIAL

migration: N/A
migration_na_reason: "No persistent schema or stored data schema changed."

observability: PARTIAL
security_privacy: PASS
rollback_recovery: N/A

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: BLOCKED
release_followups: "Run low/high device-tier matrix and resolve the measured mid-tier jank regression before promotion."

unproven_requirements: "Low/high representative device-tier measurements for all three Android apps."
known_blockers: "No usable low/high emulator or physical device is available: registered API 37 AVDs booted QEMU but never registered with adb; the only connected device is a mid-tier RMX2061."

locally_actionable_remaining: NONE

blocker_resolution_attempts: "Tried Pixel_6_kurir, Pixel_7_merchant and Pixel_5_kurir AVDs with the configured SDK root, cold boot/no-snapshot, headless and windowed modes, alternate GPU settings, read-only mode and explicit adb ports. The RMX2061 physical device was used successfully for all three apps at 20 runs each."
unblock_condition: "A low-resource and high-resource Android device/AVD becomes reachable through adb, or the local Android emulator installation is repaired so an ADB-registered low/high AVD can boot."

owner_action_required: true
owner_action_summary: "Provide authorized adb access to representative low-memory and flagship Android devices, or repair/register the local low/high AVDs. No secret is required."
verification_after_unblock: "Install the current debug artifacts, collect 20+ raw runs per app/surface/tier, merge the reports, run verify_performance_report.py, and re-evaluate the mid-tier jank failures."

dependency_chain_blocked: true
next_eligible_task: NONE

blocked_since: 2026-09-08
retry_when: "Low/high adb device or AVD becomes available."

updated_at: 2026-09-08
---

# Evidence — MOBILE-2026-001

## Acceptance Criteria Source

- `[ ]` Budgets defined per app/surface and measured on representative low/mid/high device tiers.
- `[x]` Regression threshold gates release, not dashboard-only observation.
- `[x]` Performance target uses percentile, not only average.

## Scope Implemented

- Added machine-readable p95 budgets for courier, customer and merchant launch surfaces across low, mid and high resource profiles.
- Added an adb collector with cold/warm startup, PSS memory, frame-jank, UID network-byte and artifact-size measurements, with at least 20 raw samples required.
- Added a fail-closed verifier that rejects missing tiers, duplicate cells, insufficient samples, non-p95 policy and over-budget p95 values.
- Added a merge utility for complete app/surface/tier reports.
- Added a CI policy-contract job that must pass before Android build jobs proceed.
- Measured all three apps on the connected RMX2061 Android 11 device as a real mid-tier baseline. The baseline is intentionally not represented as a release PASS: its observed jank p95 exceeds the active 8% threshold.

## Files Changed

- `scripts/mobile/performance_budgets.json` — app/surface/tier p95 policy.
- `scripts/mobile/measure_performance.py` — repeatable adb collector and warm/jank measurement semantics.
- `scripts/mobile/verify_performance_report.py` — fail-closed release decision.
- `scripts/mobile/merge_performance_reports.py` — report matrix merge.
- `scripts/mobile/test_performance_gate.py` — gate contract and regression tests.
- `docs/mobile/performance-budgets.md` — policy, commands and interpretation.
- `docs/mobile/device-support-matrix.md` — tier profiles, app surfaces and ownership.
- `.github/workflows/android-apps.yml` — hard CI dependency on the gate contract.
- `docs/mobile/performance-reports/*.json` — three real 20-run mid-tier baselines and their merged report.
- `task-food-marketplace-parity-2026.md` — checked only the two proven checklist requirements.

## Commands / Checks Run

    command: python -m py_compile scripts/mobile/measure_performance.py scripts/mobile/verify_performance_report.py scripts/mobile/test_performance_gate.py scripts/mobile/merge_performance_reports.py
    result: PASS

    command: python scripts/mobile/test_performance_gate.py
    result: PASS — p95 policy, over-budget regression and insufficient-sample failures verified.

    command: python scripts/mobile/merge_performance_reports.py --output docs/mobile/performance-reports/mid-rmx2061.json docs/mobile/performance-reports/courier-mid-rmx2061.json docs/mobile/performance-reports/customer-mid-rmx2061.json docs/mobile/performance-reports/merchant-mid-rmx2061.json
    result: PASS — 3 unique app/surface/tier measurements merged.

    command: python scripts/mobile/verify_performance_report.py --budgets scripts/mobile/performance_budgets.json --report docs/mobile/performance-reports/mid-rmx2061.json
    result: FAIL as designed for the incomplete/non-eligible baseline — low/high cells are missing and all three observed mid-tier jank p95 values exceed 8%.

    command: python -u scripts/mobile/measure_performance.py --app merchant --package com.tembus.merchant --activity com.tembus.merchant/.SplashActivity --tier mid --device-profile "RMX2061 Android 11, 7.4 GiB/8 cores/1080x2400" --serial 66fcb3 --apk android-app-merchant/app/build/intermediates/apk/debug/app-debug.apk --runs 20 --settle-seconds 0.2 --adb "C:\Users\yogis\AppData\Local\Android\Sdk\platform-tools\adb.exe" --output docs/mobile/performance-reports/merchant-mid-rmx2061.json
    result: PASS — 20/20 raw samples collected.

    command: python -u scripts/mobile/measure_performance.py --app courier --package com.tembus.courier --activity com.tembus.courier/.ui.MainActivity --tier mid --device-profile "RMX2061 Android 11, 7.4 GiB/8 cores/1080x2400" --serial 66fcb3 --apk android-app/app/build/outputs/apk/debug/app-debug.apk --runs 20 --settle-seconds 0.2 --adb "C:\Users\yogis\AppData\Local\Android\Sdk\platform-tools\adb.exe" --output docs/mobile/performance-reports/courier-mid-rmx2061.json
    result: PASS — 20/20 raw samples collected.

    command: python -u scripts/mobile/measure_performance.py --app customer --package com.tembus.customer --activity com.tembus.customer/.ui.MainActivity --tier mid --device-profile "RMX2061 Android 11, 7.4 GiB/8 cores/1080x2400" --serial 66fcb3 --apk android-app-customer/app/build/outputs/apk/debug/app-debug.apk --runs 20 --settle-seconds 0.2 --adb "C:\Users\yogis\AppData\Local\Android\Sdk\platform-tools\adb.exe" --output docs/mobile/performance-reports/customer-mid-rmx2061.json
    result: PASS — 20/20 raw samples collected.

    command: .\gradlew.bat assembleDebug (android-app, android-app-customer, android-app-merchant; JAVA_HOME=JDK 21)
    result: PASS — all three debug artifacts built; merchant artifact was present under the Gradle intermediates APK path.

## Task-Local Verification

### Tests

Status: PASS

Evidence: Python syntax compilation and the gate contract test pass. The contract test exercises p95 nearest-rank evaluation, complete matrix coverage, over-budget rejection and insufficient-sample rejection.

### Integration

Status: PASS

Evidence: The collector installed/started the three current app packages on adb serial `66fcb3` and collected 20 raw samples per app. The reports preserve serial, device profile, measured timestamp and raw arrays.

### E2E

Status: PARTIAL

Evidence: Real device launch flows were exercised for courier, customer and merchant on the RMX2061. Low/high AVD flows were attempted but did not become adb devices; therefore no low/high E2E claim is made.

### Migration

Status: N/A

Evidence: No persistent schema or stored data schema changed.

### Observability

Status: PARTIAL

Evidence: The collector records raw timing, memory, frame, network and artifact measurements. Crash/ANR, battery/location, background cost and request-count telemetry remain owned by the later mobile observability/reliability tasks identified in the support matrix.

### Security / Privacy

Status: PASS

Evidence: No credentials were added. The collector records an adb serial and device profile only; no tokens, request payloads or personal data are written to the reports. The gate workflow uses read-only repository permission.

### Rollback / Recovery

Status: N/A

Evidence: This task adds measurement policy and CI gating only; no persisted runtime state or deployment migration requires rollback.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value: `false`

Reason: The original task requires representative device-tier measurements, but does not require staging or production runtime evidence.

### External Runtime Validation

Status: NOT_RUN

Evidence: No staging deployment was required or claimed for this task.

### Release Readiness

Status: BLOCKED

Evidence: The release gate correctly rejects the incomplete matrix and the real RMX2061 mid-tier baseline currently exceeds the jank p95 budget: courier 100%, customer 55.71%, merchant 28.95%. Other mid-tier observed p95 values were courier cold 2248 ms/warm 180 ms/memory 211.4 MiB/network 0 B/artifact 223343827 B; customer cold 2847 ms/warm 280 ms/memory 214.2 MiB/network 0 B/artifact 186360575 B; merchant cold 1351 ms/warm 151 ms/memory 172.5 MiB/network 0 B/artifact 20690163 B.

### Release Follow-ups

- Collect low/high representative device-tier reports after adb access is available.
- Investigate the observed mid-tier jank regression before promoting a candidate that must satisfy the 8% p95 policy.
- Run the full merged report through the release gate.

## Locally Actionable Remaining

NONE — implementation, gate logic, CI wiring and all available-device measurements are complete; remaining proof requires unavailable low/high device resources and the measured app regression belongs to the subsequent startup/performance optimization work.

## Blocker Resolution Attempts

- Checked Android SDK/emulator/adb tooling and corrected the runtime SDK root to `C:\Users\yogis\AppData\Local\Android\Sdk`.
- Tried registered `Pixel_6_kurir`, `Pixel_7_merchant` and `Pixel_5_kurir` AVDs with resource overrides, no-snapshot cold boot, no-window and windowed modes, alternate GPU modes, explicit ports and read-only mode.
- Confirmed QEMU processes started but none appeared in `adb devices` or exposed an adb port during the boot timeout.
- Confirmed the physical `66fcb3` device is reachable and identified its model/OS/ABI/CPU/RAM/display.
- Installed and exercised the current courier/customer/merchant debug packages on the physical device.

## External Blockers

The original low/high device-tier proof cannot be completed in this environment because no low-resource or flagship adb device/working AVD is available. This is distinct from the implemented policy and gate, which are locally verified.

## Owner Action Required

### Why Owner Action Is Required

The required low/high representative measurements need either physical devices or a functioning ADB-registered AVD. The available local AVDs launch QEMU but never become visible to the authorized adb server, and the only attached physical device is the RMX2061 mid-tier baseline.

### Exact Action Required

1. Connect an authorized Android Go/low-memory device and a current flagship Android device with USB debugging enabled, or repair/register equivalent low/high AVDs in the local Android SDK.
2. Confirm both devices appear in `adb devices` as `device` using `C:\Users\yogis\AppData\Local\Android\Sdk\platform-tools\adb.exe`.
3. Provide the two adb serials or make the repaired AVD names available; do not provide credentials or private keys.

### Where To Configure It

The devices/AVDs must be available to the local Android SDK at `C:\Users\yogis\AppData\Local\Android\Sdk`, or to the authorized CI/emulator runner used for mobile verification.

### Security Handling

No secret is needed. Do not commit adb private keys, device authorization files, tokens or credentials; do not place them in task evidence, logs or screenshots.

### What The Agent Needs Afterwards

Only the adb serials (or working AVD names) and reachable device state. No sensitive material should be pasted into chat or committed.

## Unblock Condition

At least one low-resource and one high-resource device/AVD is visible as `device` through adb and can install/launch the three debug APKs.

## Verification After Unblock

Collect 20+ runs for each of the 9 app/surface/tier cells, merge them with `merge_performance_reports.py`, run `verify_performance_report.py`, investigate any p95 failure, then update this evidence and the master checklist.

## Dependency Impact

`true` — downstream mobile release-quality tasks that depend on a complete performance-tier matrix should not be advanced from this blocked prerequisite.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: PARTIAL

Evidence:

- implementation: PASS — policy, collector, verifier, merge utility and CI contract exist.
- tests: PASS — syntax and gate contract checks.
- integration: PASS — three app packages measured on the available physical device.
- E2E: PARTIAL — mid device PASS, low/high unavailable.
- migration: N/A — no schema/data change.
- observability: PARTIAL — deterministic metrics recorded; crash/ANR/battery/request-count telemetry belongs to later mobile tasks.
- security/privacy: PASS — no secret or PII added.
- rollback/recovery: N/A — no persisted/deployed state change.
- external proof if explicitly required: N/A — not required by original task.

### REALITY-2026-011 — No Fake Completeness

Status: PASS

The evidence distinguishes real mid-device measurements, unmeasured low/high tiers and a failing release baseline. No emulator, staging, production or performance PASS was fabricated.

## Unproven / Remaining

- Low/high representative device-tier measurements for courier, customer and merchant launch surfaces.

## Next Eligible Task

NONE

## Status Decision

Original task work is no longer locally actionable. One original acceptance criterion remains unproven because the required low/high device resources are unavailable after all safe local tool/emulator/device paths were attempted; status is therefore `BLOCKED`.
