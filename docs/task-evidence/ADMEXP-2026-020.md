---
task_id: ADMEXP-2026-020
status: BLOCKED

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: WORKTREE — local GUI gate and runbook changes pending scoped commit

tests: PASS
integration: PASS
e2e: PASS

migration: N/A
migration_na_reason: "ADMEXP-2026-020 changes GUI labeling, runbook documentation and verification coverage only; no persistent schema or stored data changes."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: true
external_runtime_validation: PARTIAL

release_readiness: PARTIAL
release_followups: "Record the authenticated staging drill with a non-engineer: banner change → targeted publish → app verification → GUI rollback."

unproven_requirements: "The required staging drill has not been recorded because authorized staging deployment/operator access is unavailable on this host."
known_blockers: "No authorized staging operator/session or staging deployment path is available: the staging workflow still lacks STAGING_SSH_HOST, and the final launch-gate evidence cannot be collected from the local mock environment."

locally_actionable_remaining: NONE

blocker_resolution_attempts: "Built the GUI click-path runbook; started the Admin Vite server; executed Playwright Chromium GUI smoke for all App Experience surfaces; executed Admin draft/preview/approval/publish and RBAC E2E; attempted to use the repository staging workflow and confirmed the documented STAGING_SSH_HOST deployment blocker; checked available local browser tooling and found no authorized staging operator session."
unblock_condition: "An authorized staging deployment is reachable and a non-engineer operator can execute and record the banner change, targeted publish, deployed app verification and GUI rollback with redacted evidence."

owner_action_required: true
owner_action_summary: "Provide authorized staging deployment access and nominate a non-engineer Product/Marketing/Ops operator for the recorded drill."
verification_after_unblock: "Run the exact staging drill in the runbook, capture revision/audit/runtime evidence without secrets, rerun the Admin and customer browser flows against staging, then update this evidence and the unchecked staging checklist item."

dependency_chain_blocked: true
next_eligible_task: NONE — this is the GUI-only operational release gate.

blocked_since: 2026-09-10
retry_when: "STAGING_SSH_HOST and authorized staging/operator access are available."
updated_at: 2026-09-10
---

# Evidence — ADMEXP-2026-020

## Acceptance Criteria Source

The original ADMEXP-2026-020 requirements in `task-food-marketplace-parity-2026.md` are:

- `[x]` Replace hero/header banner.
- `[x]` Change localized banner copy and CTA.
- `[x]` Reorder supported home sections.
- `[x]` Add/remove supported promo/info component from Home.
- `[x]` Launch/schedule/pause campaign intro after native splash.
- `[x]` Target campaign by market/city/locale/app version/cohort.
- `[x]` Change existing service badge/subtitle/order.
- `[x]` Marketing-hide an existing service entry.
- `[x]` Execute authorized `new_order_gate`/provider kill switch with explicit blast-radius warning.
- `[x]` Start 1%/5%/25%/100% staged rollout.
- `[x]` Configure soft-update/minimum-version policy according to permission.
- `[x]` Upload/select/version campaign assets.
- `[x]` Select/test typed deep link.
- `[x]` Preview exact market/surface/app-version candidate.
- `[x]` Submit for approval and approve/reject according to RBAC.
- `[x]` Publish/schedule production revision.
- `[x]` Inspect revision diff and audit log.
- `[x]` Roll back to last-known-good revision.
- `[x]` See release health/analytics by revision.
- `[x]` Product/Marketing/Ops runbook documents the click path for every operation above.
- `[ ]` A staging drill is recorded where a non-engineer performs banner change → targeted publish → app verification → rollback without engineering intervention.
- `[x]` No required routine Experience operation depends on manually editing server JSON/config/env.
- `[x]` Any action that still requires an app update is explicitly labeled in Admin as `Requires App Release` rather than presented as a broken remote-config option.

The staging drill is an original acceptance criterion, so its absence is a genuine task blocker. It is not being replaced with local mock evidence.

## Scope Implemented

- Added `docs/runbooks/app-experience-operations.md`, a click-path runbook covering every routine operation, safety boundary, rollback procedure, `Requires App Release` behavior and staging drill record format.
- Added the explicit `Requires App Release` badge and safety copy to the Admin mobile release policy surface.
- Added a browser GUI smoke suite that opens all 16 App Experience surfaces, verifies the main controls for editor/targeting/approval/revision/assets/deep-links/version-policy, and executes typed deep-link validation plus a `new_order_gate` mutation through GUI controls.
- Retained the ADMEXP-2026-019 Admin E2E as evidence for draft → preview → approval → publish and read-only RBAC mutation boundaries.

## Files Changed

- `docs/runbooks/app-experience-operations.md` — Product/Marketing/Ops click-path and staging drill runbook.
- `admin-dashboard/src/pages/MobileReleasePolicies.tsx` — explicit `Requires App Release` label.
- `frontend/e2e/admin-experience-operations.spec.ts` — browser GUI operations smoke and mutation coverage.
- `task-food-marketplace-parity-2026.md` — checked locally proven GUI/runbook/label requirements; retained staging drill unchecked.

## Commands / Checks Run

    command: ADMIN_E2E=true BASE_URL=http://127.0.0.1:4173 npx playwright test e2e/admin-experience-operations.spec.ts --project=chromium
    location: frontend
    result: PASS — 2 tests.

    command: ADMIN_E2E=true BASE_URL=http://127.0.0.1:4173 npx playwright test e2e/admin-experience-flow.spec.ts e2e/admin-experience-operations.spec.ts --project=chromium
    location: frontend
    result: PASS — 4 tests.

    command: npx eslint e2e/admin-experience-operations.spec.ts
    location: frontend
    result: PASS.

    command: npx eslint src/pages/MobileReleasePolicies.tsx
    location: admin-dashboard
    result: PASS.

    command: npx tsc -b --pretty false
    location: admin-dashboard
    result: PASS.

    tool: local Admin Vite server + Playwright Chromium
    result: PASS — 16 App Experience routes opened; explicit GUI controls and release-boundary label rendered.

    tool: staging deployment/operator drill
    result: NOT_RUN — blocked by unavailable authorized staging deployment/operator access; no staging claim is made.

## Task-Local Verification

### Tests

Status: `PASS`

Evidence: The GUI operations suite passed 2/2 tests. The combined Admin flow plus operations suite passed 4/4 Chromium tests. ADMEXP-2026-019 separately passed 12/12 focused contract tests and 2/2 Admin flow tests.

### Integration

Status: `PASS`

Evidence: Browser automation used the real Admin React routes and controls, including the typed deep-link tester, external URL validator, kill-switch confirmation dialog, service-control mutation, draft editor controls, approval controls, revision/rollback surfaces and mobile release policy boundary. API calls were controlled local fixtures and are not represented as staging/provider proof.

### E2E

Status: `PASS`

Evidence: Local Playwright Chromium opened all 16 App Experience surfaces and verified the central operational controls. It executed two GUI mutations and the existing Admin-to-App flow executed the complete draft/preview/approval/publish path. This is local E2E, not staging E2E.

### Migration

Status: `N/A`

Evidence: No persistent schema or stored data changed.

### Observability

Status: `PASS`

Evidence: The local GUI smoke opened Overview/Analytics with a controlled observability payload and verified the release-health surface. Existing experience observability tests passed in the ADMEXP-2026-019 focused suite.

### Security / Privacy

Status: `PASS`

Evidence: Admin flow E2E passed read-only RBAC behavior; the GUI kill-switch path requires explicit reason, preserves active-order visibility, shows blast-radius impact and opens a confirmation dialog. No secrets, credentials or customer identifiers were added to code, evidence or screenshots.

### Rollback / Recovery

Status: `PASS`

Evidence: The runbook documents GUI rollback to an immutable compatible known-good revision. Existing ADMEXP-019 rollback and active-order recovery tests passed; the GUI revisions/rollback surface was opened by the operations smoke.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value: `true`

Reason: ADMEXP-2026-020 explicitly requires a recorded staging drill performed by a non-engineer, including deployed app verification and GUI rollback.

### External Runtime Validation

Status: `PARTIAL`

Evidence: Local GUI behavior is proven. The required authenticated staging drill is `NOT_RUN` because the staging workflow cannot deploy from this host while `STAGING_SSH_HOST` is unset and no authorized non-engineer staging session is available. This is the same external boundary documented in `GLOB-2026-014`.

### Release Readiness

Status: `PARTIAL`

Evidence: The GUI-only operational release gate is not release-ready until the staging drill is recorded. Provider/payment, capacity and Android launch validation remain separately tracked by `GLOB-2026-014`.

### Release Follow-ups

1. Configure the authorized staging deployment path, including `STAGING_SSH_HOST`, in the staging secret manager/workflow without committing secrets.
2. Nominate a non-engineer Product/Marketing/Ops operator and provide the approved staging session.
3. Execute `docs/runbooks/app-experience-operations.md` staging drill: banner change → targeted publish → deployed app verification → GUI rollback.
4. Capture redacted revision IDs, audit rows, timestamps and runtime verification; never capture credentials or private tokens.

## Locally Actionable Remaining

`NONE` — all local GUI implementation, labeling, click-path documentation and local browser verification available to this agent have been completed.

## Blocker Resolution Attempts

- Inspected the full ADMEXP-2026-020 checklist and existing Admin route/component ownership.
- Started the local Admin Vite server and used Playwright Chromium against the actual dashboard.
- Added and ran GUI smoke coverage for all App Experience surfaces and representative high-impact mutations.
- Added the runbook and explicit release-boundary label.
- Confirmed the repository staging deployment limitation: `STAGING_SSH_HOST` is unset, so an authenticated staging deployment cannot be established from this host.
- Checked local browser automation; local proof is available but cannot substitute for the original staging/operator criterion.

## External Blockers

The original staging drill requires both:

1. an authorized staging deployment/session reachable by the workflow; and
2. an authorized non-engineer Product/Marketing/Ops operator to perform and record the drill.

Neither is available in this execution environment. Local mock APIs and local GUI E2E cannot prove deployed staging behavior.

## Owner Action Required

### Why Owner Action Is Required

The staging drill is an explicit original release-gate requirement and must be performed by a real non-engineer against the deployed staging environment. This agent has no authorized staging operator identity/session, and the staging workflow is missing `STAGING_SSH_HOST`.

### Exact Action Required

1. Configure the authorized staging deploy target/secret `STAGING_SSH_HOST` in the repository's staging secret manager or GitHub Actions environment; keep all SSH credentials and tokens secret.
2. Confirm the staging deployment succeeds for the pushed `staging` commit.
3. Nominate a Product/Marketing/Ops operator who is authorized to use Admin Web and provide an approved staging session/window.
4. Have that operator execute: change banner in GUI → target the approved market/city/cohort → preview/publish → verify the deployed app → open Revisions & Rollback → roll back to known-good.
5. Save only redacted evidence: revision IDs, actor role, timestamps, audit event references, and app/runtime result. Do not store passwords, SSH keys, cookies, tokens, or private customer data.

### Where To Configure It

GitHub Actions staging environment/repository secrets and the authorized staging Admin Web/app environment. Do not add credentials to source, task evidence, screenshots or logs.

### Security Handling

`STAGING_SSH_HOST` and all deployment/operator credentials are environment secrets. Never commit them, paste them into evidence, expose them in screenshots, or include them in logs. Evidence should use redacted revision/audit references only.

### What The Agent Needs Afterwards

The minimum resume signal is a successful staging deployment of the pushed `staging` commit plus the redacted drill record or an authorized staging URL/session that can be exercised by Playwright.

## Unblock Condition

`STAGING_SSH_HOST` is configured, the staging deployment is reachable, and a non-engineer operator has completed the banner change → targeted publish → deployed app verification → GUI rollback drill with redacted evidence.

## Verification After Unblock

1. Verify deployed Admin Web route and authenticated operator permissions.
2. Run the staging Playwright/Admin flow against the deployed URL.
3. Confirm app fetch/render of the targeted revision.
4. Execute and verify GUI rollback and client recovery.
5. Inspect staging audit/revision evidence and update the unchecked master checklist item.
6. Rerun `python scripts/tasks/validate_task_evidence.py` and move ADMEXP-2026-020 toward `COMPLETE` only after the staging drill is real and recorded.

## Dependency Impact

`true` — ADMEXP-2026-020 is the GUI-only operational release gate. Its staging drill and downstream final launch readiness remain blocked; no local implementation work remains.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: real Admin release-boundary label, runbook and browser GUI coverage exist;
- tests: local lint and TypeScript checks pass;
- integration: real dashboard routes and representative GUI mutations pass against controlled local contracts;
- E2E: 16 App Experience surfaces opened; 4 Admin Chromium tests passed in the combined run;
- migration: N/A because no schema/data change;
- observability: local health/analytics surface is exercised;
- security/privacy: RBAC and high-blast-radius confirmation behavior are exercised;
- rollback/recovery: existing rollback/recovery proof and GUI rollback surface are available;
- external proof: the explicit staging drill remains unproven.

### REALITY-2026-011 — No Fake Completeness

Status: `PASS`

The evidence keeps local GUI fixture proof separate from staging proof, leaves the staging checkbox unchecked, documents the exact external blocker and owner action, and does not present local mocks as deployed behavior.

## Unproven / Remaining

Only the original staging drill remains unproven:

- `[ ]` A staging drill is recorded where a non-engineer performs banner change → targeted publish → app verification → rollback without engineering intervention.

## Next Eligible Task

`NONE` — this is the final GUI-only operational release gate; resume after the unblock condition is satisfied.

## Status Decision

All locally actionable work is complete. The task cannot be COMPLETE because one original acceptance criterion explicitly requires unavailable external staging/operator proof. Status is therefore `BLOCKED`, with a documented path to resume and complete.

## Notes / N/A Justification

- `migration: N/A` — no persistent schema or stored data changed.
- Local API fixtures prove only GUI wiring and local behavior; they are not claimed as staging/provider evidence.
