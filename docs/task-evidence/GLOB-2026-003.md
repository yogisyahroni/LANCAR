---
task_id: GLOB-2026-003
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: change:GLOB-2026-003

tests: PASS
integration: PASS
e2e: N/A
e2e_na_reason: "The original task is a market/role compliance boundary and policy contract; no interactive customer, courier, or merchant journey is required to prove the six checklist items."

migration: PASS
observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: PARTIAL
release_followups: "Push the implementation to origin/staging, observe CI, deploy the migration through the staging migration runner, and run authenticated policy/consent smoke after the staging SSH rollout is configured."

unproven_requirements: NONE
known_blockers: NONE

locally_actionable_remaining: NONE

blocker_resolution_attempts: NONE
unblock_condition: NONE

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE

dependency_chain_blocked: false
next_eligible_task: GLOB-2026-004

updated_at: 2026-09-08
---

# Evidence — GLOB-2026-003

## Acceptance Criteria Source

Original checklist requirements from `task-food-marketplace-parity-2026.md`:

- `[x]` Customer/courier/merchant verification requirements are market/role based.
- `[x]` Consent captures document version, locale, timestamp, actor and purpose.
- `[x]` Data retention/deletion/export rules are market scoped.
- `[x]` Sensitive verification artifacts use least-privilege access and dedicated retention policy.
- `[x]` Launching a new market requires explicit compliance checklist instead of inheriting Indonesian rules silently.
- `[x]` Restricted/regulated service categories can be disabled per market without app rebuild.

These are the original requirements. Staging deployment and authenticated staging smoke are recorded as release follow-up because they are not part of the original GLOB-2026-003 checklist.

## Scope Implemented

- Added market-scoped, role-scoped verification requirements, data retention/export/deletion policies, and sensitive artifact access policies in the existing market configuration ownership boundary.
- Added an append-only consent event store and authenticated API. Consent input is validated against the active market/role requirement, including document version, locale, and purpose; actor and timestamp are recorded server-side.
- Extended `market_config_readiness` so every supported role and an active data policy are explicit launch prerequisites. Unknown or incomplete markets fail closed and never inherit `id-jk` policy.
- Reused the existing `market_service_availability` source of truth for per-market service restrictions and exposed enabled/disabled category state through the compliance policy resolver.
- Added public policy discovery, authenticated consent endpoints, gateway auth/proxy wiring, an Admin Security policy/readiness panel, and the compliance contract document.

## Files Changed

- `database/migrations/20260908000023_global_compliance_boundary.sql` — policy tables, constraints, baseline `id-jk` policy, append-only trigger, readiness gate, and rollback restoration.
- `backend/admin-service/src/services/complianceBoundary.ts` — policy resolver, consent validation, consent persistence, and subject-scoped reads.
- `backend/admin-service/src/controllers/compliance.controller.ts` — public/admin policy and authenticated consent controllers.
- `backend/admin-service/src/routes/public.routes.ts`, `backend/admin-service/src/routes/admin.routes.ts`, `backend/admin-service/src/controllers/index.ts` — API route registration and exports.
- `backend/api-gateway/src/index.ts`, `backend/api-gateway/src/routeAuthMatrix.ts`, `backend/api-gateway/package.json`, `backend/api-gateway/scripts/complianceBoundary.test.js` — authenticated/public route policy, proxy, and gateway contract test.
- `admin-dashboard/src/pages/settings/security.tsx` — market policy, readiness, retention, artifact, role, and service-category view.
- `docs/contracts/compliance-market-policy-2026.md` — ownership and API contract.
- `backend/admin-service/src/services/complianceBoundary.test.ts`, `backend/admin-service/src/services/complianceMigration.test.ts` — unit and migration contract verification.
- `task-food-marketplace-parity-2026.md` — checked only the six proven original requirements.

## Commands / Checks Run

    command: goose -dir database/migrations validate
    result: PASS

    command: goose -dir database/migrations postgres <local-tembus-dsn> up-by-one
    result: PASS — 20260908000023_global_compliance_boundary.sql applied; four tables and baseline policy rows created.

    command: local PostgreSQL migration round-trip (Up → Down → Up)
    result: PASS — Down removed all four compliance tables, restored baseline readiness without compliance references, and Up reapplied successfully.

    command: local PostgreSQL consent trigger transaction smoke
    result: PASS — granted, withdrawn, and re-granted events were inserted as three immutable events; an attempted update was blocked by the append-only trigger; transaction was rolled back.

    command: npm test -- --runInBand src/services/complianceBoundary.test.ts src/services/complianceMigration.test.ts (backend/admin-service)
    result: PASS — 2 suites, 8 tests.

    command: npm test -- --runInBand (backend/admin-service)
    result: PASS — 86 suites, 371 tests.

    command: npm run build (backend/admin-service)
    result: PASS — TypeScript compilation.

    command: npm run test:compliance-boundary (backend/api-gateway)
    result: PASS — gateway build and public/authenticated compliance route assertions.

    command: npm run test:auth-matrix (backend/api-gateway)
    result: PASS — existing route auth matrix regression.

    command: VITE_API_URL=https://api.bawain.my.id/api/v1 VITE_WS_URL=wss://api.bawain.my.id VITE_SOCKET_URL=https://api.bawain.my.id npm run build (admin-dashboard)
    result: PASS — production environment validation, TypeScript build, and Vite production bundle.

    command: npm run lint (admin-dashboard)
    result: PASS — 0 errors; existing repository warnings remain.

    command: git diff --check
    result: PASS.

`make test` and `make lint` were not run because `make` is unavailable on this Windows environment; the applicable service, gateway, dashboard, and database checks were run directly.

## Task-Local Verification

### Tests

Status: `PASS`

Evidence: Unit tests cover strict versioned consent input, market normalization, fail-closed unknown markets, full policy resolution, active requirement matching, actor-bound consent insertion, and stale-document rejection. The migration contract test verifies role constraints, consent fields, append-only behavior, readiness reasons, and absence of an invalid active-grant uniqueness rule. The full admin-service suite passed with 86 suites and 371 tests.

### Integration

Status: `PASS`

Evidence: The migration was applied against local PostgreSQL, queried for four compliance tables plus 9 role requirements, 4 data policies, and 3 artifact policies, then rolled back and reapplied. The gateway build and contract test verified public policy routing and authenticated consent routing. Admin-service and dashboard production builds passed.

### E2E

Status: `N/A`

Evidence: The original checklist does not require an interactive app journey. The Admin panel is compile/build verified and the API boundary is covered by service and gateway contract tests; no browser/device result is represented as E2E evidence.

### Migration

Status: `PASS`

Evidence: Goose validation passed. Up/Down/Up passed against local PostgreSQL. Down removed the new tables and restored the preceding `market_config_readiness` behavior. The append-only consent trigger and withdrawal/re-grant history were verified in a rolled-back database transaction.

### Observability

Status: `PASS`

Evidence: Unexpected controller failures use the existing structured `securityLog` boundary, gateway proxy forwarding uses the existing proxy/breaker logging path, and request correlation remains supplied by the existing gateway/request-context middleware. No live staging dashboard or alert firing is claimed.

### Security / Privacy

Status: `PASS`

Evidence: Public policy queries do not select policy metadata or credential-bearing fields. Consent writes require mobile/web authentication, active requirement matching, and idempotency middleware; consent reads are subject-scoped. Admin policy reads are restricted to `super_admin`, `ops_admin`, and `ops_security`. Artifact policies use `restricted`/`compliance_only` access classes, exports can be prohibited, and the database trigger blocks consent mutation. No secret, raw identity artifact, or provider credential was added to source/evidence.

### Rollback / Recovery

Status: `PASS`

Evidence: Migration Down/Up round-trip passed. Consent correction is represented by new immutable withdrawn/granted events, allowing re-grant without mutating history. Existing market service-category configuration remains the rollback-capable source of truth.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value:

`false`

Reason: The original GLOB-2026-003 checklist requires the compliance boundary, policy model, consent fields, market launch gate, artifact controls, and per-market service restriction. It does not require staging, production, provider, or physical-device proof.

### External Runtime Validation

Status:

`NOT_RUN`

No staging deployment or live authenticated compliance smoke is claimed here. The current staging release follow-up remains separate from task completion.

### Release Readiness

Status:

`PARTIAL`

The implementation still needs to be included in the staging deployment and followed by authenticated policy/consent smoke after the staging SSH rollout is available.

### Release Follow-ups

- Push the implementation to `origin/staging` and observe the evidence, security, CI/CD, and mobile workflows.
- Apply migration `20260908000023_global_compliance_boundary.sql` through the staging migration runner.
- Run authenticated customer/courier/merchant consent smoke and Admin policy-panel smoke in staging.

## Locally Actionable Remaining

`NONE` — all six original GLOB-2026-003 requirements are implemented and proven with local database lifecycle, unit, gateway, service, and dashboard verification.

## Blocker Resolution Attempts

`NONE` — there is no original-task blocker. External staging validation is a release follow-up under the evidence protocol.

## External Blockers

`NONE` for the original TASK-ID.

## Owner Action Required

`NONE`.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PASS`.

Evidence: Each original checklist item maps to real database schema/constraints, service/API wiring, gateway auth, Admin GUI behavior, and actual tests/builds. Release validation is explicitly separated and no live result is fabricated.

### REALITY-2026-011 — No Fake Completeness

Status: `PASS`.

Evidence: No staging/provider/device result is represented as complete; policy resolution is market-specific and fail-closed; consent events are database-enforced append-only; service restrictions reuse the authoritative market configuration; credentials and raw artifacts are not returned or recorded; the master checklist was not expanded or rewritten.

## Unproven / Remaining

`NONE` for the original GLOB-2026-003 requirements.

Release follow-up remains separately tracked under `release_followups`.

## Next Eligible Task

`GLOB-2026-004` — dependency-valid next implementation task after GLOB-2026-003.

## Notes / N/A Justification

- `e2e: N/A` is justified because the original task is a policy/schema/API boundary, not an interactive flow requirement.
- `external_runtime_validation: NOT_RUN` and `release_readiness: PARTIAL` are release fields and do not block this implementation task because external proof is not an original acceptance criterion.
- `make` was unavailable; direct applicable verification was executed and the limitation is recorded.
