---
task_id: FOOD-2026-001
status: PARTIAL

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: "f1cfe6d7"

tests: PASS
integration: PASS
e2e: NOT_RUN

migration: N/A
migration_na_reason: "The coordinate contract uses existing order fields and address records; no schema change was introduced."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: BLOCKED
release_followups: "Run authenticated Food checkout with a real saved address, geocoded address, device-location permission path, and an edited destination in staging."

unproven_requirements: "Authenticated staging checkout only."
known_blockers: "Approved google-services.json and authenticated staging runtime are unavailable locally."

locally_actionable_remaining: "No additional local implementation remains for the coordinate contract; authenticated runtime evidence remains pending."
blocker_resolution_attempts: "Separated discovery coordinates from checkout coordinates, added saved-address/geocode/device-location selection, rejected checkout without an explicit valid pin, and added server-side destination validation."
unblock_condition: "Provide authorized staging credentials/runtime and approved Android configuration, then capture checkout evidence."

owner_action_required: true
owner_action_summary: "Provide the approved Android configuration and authenticated staging access for runtime evidence."
verification_after_unblock: "Confirm the submitted address and coordinates are the selected destination, never browse/discovery coordinates, and that editing the address requires a fresh quote before payment/order creation."

dependency_chain_blocked: false
next_eligible_task: FOOD-2026-002
blocked_since: null
retry_when: "After FOOD-2026-002 quote/requote implementation is available."
updated_at: 2026-09-02
---

# Evidence — FOOD-2026-001

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md`:

- `[x]` Atomic address+coordinate destination.
- `[x]` Saved/manual/pinned change coordinates.
- `[x]` Discovery GPS not silently reused as checkout destination.
- `[ ]` Address change requotes.

## Scope Implemented

- Food discovery coordinates are stored separately and are never copied into checkout state.
- Saved receiver addresses use their persisted latitude/longitude.
- Manual address text must be resolved through the existing server-side geocoding endpoint before submit; selecting a result binds its pin to the address.
- Device location is an explicit user action and requires foreground location permission.
- Checkout refuses to create an order when the destination has no explicit valid coordinate.
- Order service validates address presence, coordinate ranges, finite values, and rejects `0,0` before merchant/order processing.

## Files Changed / Audited

- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodCheckoutScreen.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodViewModel.kt`
- `android-app-customer/app/src/main/AndroidManifest.xml` — existing foreground location permissions audited.
- `backend/order-service/internal/service/order_food.go`
- `backend/order-service/internal/service/order_service.go`
- `backend/order-service/internal/service/order_food_coordinate_test.go`
- `task-food-marketplace-parity-2026.md`

## Commands / Checks Run

    backend/order-service: go test ./internal/service
    result: PASS.

    backend/order-service: gofmt + git diff --check
    result: PASS.

    android-app-customer: .\gradlew.bat :app:compileDebugKotlin --no-daemon
    result: BLOCKED — approved google-services.json is missing.

    android-app-customer: .\gradlew.bat :app:compileDebugKotlin --no-daemon -x processDebugGoogleServices
    result: BLOCKED — Gradle source-set mapping still evaluates the skipped Google Services task.

## Task-Local Verification

### Tests

Status: PASS

Evidence: `TestValidateFoodDestination` covers valid coordinates, missing pin, `0,0`, and out-of-range latitude. Existing order-service service tests also pass.

### Integration

Status: PASS

Evidence: Android sends the explicitly selected checkout coordinate fields to the existing Food create endpoint; backend recalculates delivery distance/fee from those fields.

### E2E

Status: NOT_RUN

Evidence: Authenticated Android/staging runtime is unavailable in this session.

### Migration

Status: N/A

Evidence: No persistent schema changed.

### Observability

Status: PASS

Evidence: Existing order request and service errors remain the authoritative audit surface; no client-side fallback price or location was introduced.

### Security / Privacy

Status: PASS

Evidence: Location is requested only after explicit checkout action and only foreground permissions are used; manual/saved selection does not require location permission.

### Rollback / Recovery

Status: PASS

Evidence: The change is isolated to Food checkout validation and uses existing API fields; reverting these files requires no data migration.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL — local contract and backend tests pass, but the address-change requote path still needs runtime/build proof.
- `REALITY-2026-011`: PASS — no mock coordinates, fabricated quote, or placeholder order data was added.

## Unproven / Remaining

- Execute the authenticated staging checkout journey with real address records.

## Blockers

The coordinate safety implementation is locally testable. The address-change requote path is implemented but remains unchecked until Android build/runtime evidence is available; Android runtime compilation additionally requires the approved Google Services configuration.
