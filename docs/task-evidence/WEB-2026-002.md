---
task_id: WEB-2026-002
status: PARTIAL
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "bbee48c1"
tests: PASS
e2e_staging: NOT_RUN
migration: N/A
migration_na_reason: "The service-aware order presentation reads existing order, payment, and logistics columns; no schema or persisted-data migration was added."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Staging/Docker runtime verification with real authenticated order history, order detail, and resi data; provider/AWB values and payment-state reconciliation still need runtime evidence."
known_blockers: "Staging/Docker runtime access and a usable seeded customer session were not available for this local implementation turn."
updated_at: 2026-09-01
---

## Scope Implemented

- Customer order list, detail, and resi endpoints now return service metadata, logistics provider/service/AWB fields, and the latest payment status from existing persisted records.
- The web UI distinguishes Instan LANCAR, food delivery, aggregator, named services, and unknown service metadata without inventing a service label when the server did not provide one.
- Aggregator orders visibly separate First-mile LANCAR from the external carrier and show provider/service/AWB only when present in the API response.
- Payment status and delivery status render in separate UI rows; missing totals render as `-` instead of a fabricated zero.
- Resi filters and labels use the actual persisted order models (`p2p` and `hub_and_spoke`) rather than placeholder model values.

## Files Changed

- `backend/admin-service/src/controllers/order/customerOrder.controller.ts`
- `frontend/src/components/orders/orderPresentation.ts`
- `frontend/src/components/orders/OrderServiceBadge.tsx`
- `frontend/src/components/orders/OrderPriceBreakdown.tsx`
- `frontend/src/app/(portal)/orders/page.tsx`
- `frontend/src/app/(portal)/orders/[id]/orderDetailTypes.ts`
- `frontend/src/app/(portal)/orders/[id]/OrderDetailContent.tsx`
- `frontend/src/app/(portal)/resi/page.tsx`
- `frontend/src/app/(portal)/resi/[id]/page.tsx`
- `frontend/src/tests/unit/orderPresentation.test.ts`

## Commands / Checks Run

- `npm test -- --run` in `frontend` — PASS, 7 files / 18 tests.
- `npx tsc --noEmit` in `frontend` — PASS after the production build completed.
- `npm run lint` in `frontend` — PASS, 0 errors; repository reports 499 existing warnings.
- `npm run build` in `frontend` — PASS; Next.js webpack build compiled and generated all 29 app routes.
- `npm run build` in `backend/admin-service` — PASS.
- `npm test -- --runInBand` in `backend/admin-service` — PASS, 39 suites / 206 tests.
- `git diff --check` — PASS.
- `graphify update .` — PASS; AST refresh completed. Existing zero-node and SQL parser warnings remain tooling limitations.
- `py scripts/tasks/validate_task_evidence.py` — pending until this evidence file is committed.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL. The backend contract and UI wiring are implemented and tested locally, but the deployed/Docker flow with real customer data has not been captured.
- `REALITY-2026-011`: PASS. Service, carrier, AWB, payment, delivery, and total values are read from server fields; absent values remain explicitly unavailable and no placeholder business data was added.

## Unproven / Remaining

- Run history, detail, and resi against Docker/staging with an authenticated customer session and record the real request/response route references without storing PII.
- Verify an aggregator order with a real external provider and AWB renders First-mile LANCAR and carrier states independently.
- Verify paid, pending, failed/expired payment states alongside delivery states in the deployed runtime.
- Confirm the same presentation is used for all routes that link to order detail and resi after deployment.
