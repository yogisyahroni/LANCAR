---
task_id: TOW-2026-004
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: d51c865c
tests: PASS
e2e_staging: NOT_RUN
migration: N/A
migration_na_reason: "The existing towing_reports proof columns and service-report endpoint were reused; no schema migration was introduced."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Live staging proof uploads, destination verification with real order state, signature persistence, and customer-visible proof retrieval are not verified."
known_blockers: "Docker and staging backend/storage access are unavailable in this workspace."
locally_actionable_remaining: NONE
unblock_condition: "Make Docker/staging storage and order-state runtime available, then run authenticated lifecycle verification with real uploads and persisted reports."
dependency_chain_blocked: true
next_eligible_task: NONE
updated_at: 2026-09-01
---

# Evidence — TOW-2026-004

## Scope Implemented

- The courier towing flow now requires an inspection photo before leaving pickup, a loading photo before the transit action can advance the order, and an unloading photo before opening completion/signature.
- Loading and unloading proof URLs are retained in the service-report draft store and included in the towing report request. Successful completion clears those draft proof values.
- The order-service validates before-condition, loading, unloading, completion-photo, signature, and completion timestamp before calling the repository. Missing loading or unloading proof is rejected as `ERR_INVALID_SERVICE_REPORT` input.
- Existing `towing_reports` columns are used for loading/unloading proof persistence, keeping the data real and traceable rather than adding placeholder values.
- Completion now requires `unloading_completed_at` as a destination-verification timestamp; the courier client sends it only alongside a persisted unloading proof URL.
- Local service tests cover missing unloading timestamp rejection and proof/signature/destination timestamp retrieval through the service repository contract.

## Files Changed

- `android-app/app/src/main/java/com/tembus/courier/data/repository/ServiceReportProofDraftStore.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/order/OrderViewModel.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/service/TowingFlowScreen.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/service/TowingFlowViewModel.kt`
- `backend/order-service/internal/service/service_report_service.go`
- `backend/order-service/internal/service/service_report_service_test.go`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/order/OrderViewModel.kt`

## Commands / Checks Run

```text
command: ./gradlew.bat :app:compileDebugKotlin (android-app)
result: PASS — Kotlin compilation completed successfully.

command: go test ./... (backend/order-service)
result: PASS — all order-service packages passed, including service-report validation tests.

command: go test ./internal/service ./internal/handler (backend/order-service)
result: PASS — towing report validation and handler packages passed, including destination timestamp and retrieval coverage.

command: git diff --check
result: PASS.

command: graphify update .
result: PASS — graph refreshed; known parser warnings remain for missing tree_sitter_sql and oversized HTML visualization.

command: Docker/staging/upload-storage/signature/destination E2E
result: NOT RUN — Docker and staging runtime access are unavailable in this workspace.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

- Local Android flow gating and server-side required-proof validation are implemented and compiled/tested.
- The server contract now refuses a towing completion report without loading and unloading proof, in addition to the existing before/completion/signature requirements.
- A real staging run is still required to prove upload URLs, order-state chronology, destination verification, signature storage, and downstream customer visibility.
- Local proof retrieval/signature/destination-timestamp coverage is complete; no further local implementation remains for this task.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

- The four local implementation boxes are checked, but staging/Docker verification remains unchecked.
- No proof URL, signature, destination result, or successful live completion was fabricated in this evidence.

## Unproven / Remaining

- Run a real towing order in staging and verify inspection → loading → transit → unloading → completion in the persisted order state and `towing_reports` row.
- Verify the uploaded proof objects are immutable, bound to the same order/courier/vehicle, and retrievable by the customer/admin surfaces.
- Verify completion signature acceptance and failure/retry behavior with the real object-storage/API path.

## Blockers

Docker and staging backend/storage access are unavailable in this workspace. This is the only remaining proof gap; `locally_actionable_remaining` is `NONE`.
