---
task_id: CORE-2026-001
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "3a460cbd + a6e8c400"
tests: PASS
e2e: PARTIAL
migration: PARTIAL
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Deployment of this commit and migration/backfill proof against the service-visible staging database remain unproven; authenticated Admin login and Customer Android interactive rendering remain unproven. Customer mobile/web/courier API probes pass for known categories, while seven legacy rows intentionally remain category-unknown."
known_blockers: "The latest staging workflow published images but skipped the actual SSH rollout because STAGING_SSH_HOST is unset; no authorized service-visible staging database session is available. The local Docker CLI is unavailable, although Docker Compose and local PostgreSQL are usable. The admin login probe returned HTTP 401, so authenticated Admin runtime proof is unavailable."
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Implemented and unit-tested the canonical mapper and typed metadata, added the PostgreSQL migration and state-version trigger, wired Admin/Web/order-service create/read paths, updated customer Room schema and canonical presentation fallback, ran Admin TypeScript/build, Go, Customer Android, Courier Android, graphify, and diff checks. Rechecked Docker (CLI unavailable), verified health endpoints, authenticated read-only customer and courier staging probes, exchanged a customer web session, and inspected GitHub Actions run 34242275931 plus repository secret names. CI migration up/down, security, tests, image build/push, and workflow completion passed; Deploy to Staging logged an empty STAGING_SSH_HOST and skipped SSH deployment. Customer mobile order list returned 100 items with 93 canonical and 7 intentionally unknown legacy rows; order-service list returned 152 with 145 canonical and 7 unknown; customer web returned 152 with 145 categorized and 7 unknown; customer detail nested data contained canonical fields; courier login and order list returned 8 canonical rows. Admin login returned HTTP 401. On 2026-09-10, Docker Compose v5.1.3 listed the running local stack after an ephemeral process-only Grafana password was supplied; psql confirmed canonical migration versions 20260902000001 and 20260902000002 applied, latest local migration 20260909000017, and the canonical orders column/index/trigger present. A migrator image pull could not run because docker-credential-desktop is missing. These are local checks only and do not prove staging deployment or authenticated staging runtime. No token, password, raw payload, order ID, address, or customer PII was recorded."
owner_action_required: true
owner_action_summary: "Configure the approved GitHub staging environment with STAGING_SSH_HOST, STAGING_SSH_PRIVATE_KEY, and the required non-secret SSH/app-directory settings so the existing deploy job can roll out this commit. Then apply the canonical-order migration to the service-visible staging database, seed representative legacy/new orders for all five categories plus an unknown subtype, and provide authorized authenticated runtime access for Admin/Web/Customer Android/Courier verification. Do not paste credentials into chat, source, logs, or evidence."
verification_after_unblock: "Apply the migration, query representative rows to confirm truthful category/metadata/backfill behavior and state_version increments, then create/read each service through staging and verify every client renders canonical category or the generic degraded-safe state without inventing facts."
unblock_condition: "The staging workflow reports deployed=true for this commit, the deployed database is reachable through the authorized migration path with seeded representative fixtures, and authenticated Admin/Web/Customer Android/Courier runtime access is available for the documented contract matrix."
dependency_chain_blocked: true
next_eligible_task: PKG-2026-001
blocked_since: 2026-09-01
retry_when: "Retry after STAGING_SSH_HOST and the approved SSH deployment configuration are set, the migration is applied to the deployed staging database, and the multi-surface test identities are available."
updated_at: 2026-09-10
---

## Scope Implemented

- Added one canonical category vocabulary: `package_on_demand`, `food`,
  `tambal_ban`, `aggregator`, and `towing`.
- Added a common order contract containing identity, customer, service,
  state/money state, timestamps, actor ownership, quote ID, state version,
  correlation ID, contract version, and degraded-safe service resolution.
- Added typed parcel, food, roadside, aggregator, and towing metadata. Roadside
  metadata can carry structured vehicle/report facts instead of relying on
  `item_description`.
- Added migration/backfill/index/trigger support for canonical order fields.
  Unknown legacy rows remain category-null and degraded rather than being
  assigned a guessed category.
- Preserved legacy flat response fields while exposing `order_contract` and
  `service_metadata` from Admin customer/mobile/web order reads.
- Updated web service presentation to prioritize canonical category and keep
  unknown categories generic; customer Room stores the canonical scalar fields
  through migration 7→8.
- Completed degraded-safe presentation across the remaining surfaces: customer
  Android now has an explicit `UNKNOWN` detail section, courier Android shows
  `LAYANAN BELUM DIKENAL` instead of defaulting to package/regular, Admin uses
  a canonical category display with an explicit unknown label, and customer
  Web order/receipt screens pass the top-level category and contract into the
  shared presenter.

## Files Changed

- `backend/admin-service/src/services/orderContract.ts`
- `backend/admin-service/src/services/orderContract.test.ts`
- `backend/admin-service/src/controllers/order/_shared.ts`
- `backend/admin-service/src/controllers/order/customerOrder.controller.ts`
- `backend/admin-service/src/controllers/order/customerOrderMobile.controller.ts`
- `backend/admin-service/src/controllers/orders.controller.ts`
- `backend/admin-service/src/controllers/orders.controller.test.ts`
- `backend/order-service/internal/domain/order.go`
- `backend/order-service/internal/domain/order_contract.go`
- `backend/order-service/internal/domain/order_contract_test.go`
- `backend/order-service/internal/service/order_create.go`
- `backend/order-service/internal/service/order_food.go`
- `backend/order-service/internal/service/order_read.go`
- `backend/order-service/internal/repository/postgres_repository.go`
- `database/migrations/20260902000001_canonical_order_contract.sql`
- `frontend/src/app/(portal)/orders/[id]/orderDetailTypes.ts`
- `frontend/src/components/orders/orderPresentation.ts`
- `frontend/src/tests/unit/orderPresentation.test.ts`
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/Order.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/OrderContractModels.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/data/db/OrderDatabase.kt`
- `android-app/app/src/main/java/com/tembus/courier/data/model/Order.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/order/OrderScreen.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderDetailSections.kt`
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/detail/OrderDetailPolicyTest.kt`
- `admin-dashboard/src/components/ActiveOrdersTable.tsx`
- `frontend/src/app/(portal)/orders/page.tsx`
- `frontend/src/app/(portal)/orders/[id]/OrderDetailContent.tsx`
- `frontend/src/app/(portal)/resi/page.tsx`
- `frontend/src/app/(portal)/resi/[id]/page.tsx`
- `docs/contracts/order-state-contract-2026.md`

## Commands / Checks Run

- `npm test -- --runInBand src/services/orderContract.test.ts src/controllers/orders.controller.test.ts` from `backend/admin-service` — PASS, 2 suites / 5 tests.
- `npm run build` from `backend/admin-service` — PASS.
- `go test ./internal/domain/... ./internal/service/... ./internal/repository/... ./internal/handler/...` from `backend/order-service` — PASS, including canonical mapper/report metadata tests.
- Customer Android targeted unit tests + compile — PASS, `BUILD SUCCESSFUL`.
- Customer Android compile after canonical contract model completion — PASS, `BUILD SUCCESSFUL`.
- Courier Android unit tests + compile — PASS, `BUILD SUCCESSFUL`.
- `npm test -- --run src/tests/unit/orderPresentation.test.ts` from `frontend` — PASS, 1 file / 6 tests.
- `npx tsc --noEmit` from `frontend` — PASS.
- `npm run lint` from `admin-dashboard` — PASS, 0 errors (pre-existing warnings remain).
- `npm run build` from `admin-dashboard` with non-local production URL validation values — PASS.
- `npm run build` from `frontend` — PASS.
- `./gradlew.bat testDebugUnitTest --tests com.tembus.customer.ui.screens.detail.OrderDetailPolicyTest` from `android-app-customer` — PASS, `BUILD SUCCESSFUL`.
- `./gradlew.bat testDebugUnitTest` from `android-app` — PASS, `BUILD SUCCESSFUL`.
- `git diff --check` — PASS.
- `graphify update .` — PASS; graph rebuilt with 17,637 nodes and 32,355 edges. Existing extractor warnings for unsupported SQL parser/zero-node config files remain.
- Staging migration/runtime verification — NOT_RUN; no reachable authorized Docker/database session was available.
- `Get-Command docker` — FAIL/UNAVAILABLE; Docker is not installed in this workspace.
- `Invoke-RestMethod https://api.bawain.my.id/health` — PASS, HTTP 200; this only proves the edge is reachable.
- `gh run view 34406498671 --repo yogisyahroni/LANCAR` — PASS for the current workflow checks; frontend, service, migration, resilience, observability, security, container-audit and image-push jobs completed. `Deploy to Staging` logged `STAGING_SSH_HOST belum di-set` and `deployed=false`, while release validation remained pending.
- `gh secret list --repo yogisyahroni/LANCAR` — PASS read-only configuration check; the required staging SSH host/private-key secret names are not configured.
- Authenticated read-only staging probes against customer mobile, order-service, customer web, courier, and customer detail surfaces — PASS for transport and known canonical mapping, but PARTIAL for this task's full proof: customer mobile 100 items/93 canonical/7 unknown, order-service 152/145/7, web 152/145/7, courier 8/8 canonical; customer detail's canonical fields are nested under `data`. Unknown legacy rows are intentionally degraded and not guessed.
- Authenticated admin login probe against `/api/v1/auth/web/login` — FAIL, HTTP 401; no Admin runtime proof was claimed.

## 2026-09-08 Re-verification

The current staging release still cannot be considered deployed: the workflow
run `34406498671` built/published artifacts but skipped SSH rollout because the
staging host secret is absent. Read-only authenticated probes nevertheless
confirmed that the reachable customer mobile, order-service, customer web, and
courier APIs now expose canonical fields for known service categories. Seven
legacy rows are deliberately returned with an unknown category because their
stored model/service facts do not identify a supported category; this is the
expected degraded-safe behavior, not a backfill success claim. The customer
detail response exposes canonical fields inside its `data` envelope. Admin
login returned 401, and no Admin authenticated response or Android interactive
device result was recorded.

## 2026-09-10 Re-verification

GitHub Actions run `34406498671` validated the current `staging` branch with
frontend, service, migration UP/DOWN, resilience, observability, security,
container-audit and image-push jobs passing. The deploy guard still found no
`STAGING_SSH_HOST`, set `deployed=false`, and skipped authenticated staging
runtime validation. This confirms the implementation remains locally/CI
verified while the original staging acceptance criterion is still external.

Additional local runtime checks on 2026-09-10 found the Docker Compose-managed
service stack running with core health endpoints returning HTTP 200. The local
database reports the canonical migration and the immediately following
contract migration as applied, with the canonical orders column, index and
state-version trigger present. The Compose migrator could not be started
because its `ghcr.io/pressly/goose:latest` image pull requires the unavailable
`docker-credential-desktop` helper. Local database and health results remain
implementation evidence only; they do not replace the missing current-commit
staging deployment, backfill query and authenticated multi-surface proof.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL. Local implementation, tests, typechecks, and
  Android builds are real and passing; staging migration, backfill queries,
  and authenticated cross-surface E2E are not proven.
- `REALITY-2026-011`: PASS. Unknown categories remain null/degraded, missing
  facts are absent/null, legacy flat fields are preserved, and no fake order,
  price, provider, AWB, vehicle, or payment data is generated.

## Unproven / Remaining

- No locally actionable requirements remain for this task.
- Owner must make the deployment and service-visible migration path available,
  then run the migration and contract matrix in staging, including unknown/new
  subtype behavior and status-version increment verification. Admin credentials
  must also be valid for the configured staging tenant.
- Observability and rollback are locally reviewed but not exercised against a
  running database/runtime; no staging success is claimed.
- Do not commit database credentials, tokens, customer PII, or raw staging
  payloads in evidence.
