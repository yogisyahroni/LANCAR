---
task_id: UIUX-2026-008
status: PARTIAL

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: "WORKTREE — customer Figma parity execution checkpoint"

tests: PASS
integration: NOT_RUN
e2e: NOT_RUN

migration: PASS

observability: N/A
observability_na_reason: "No new operational event or metric contract; existing repository/network error state is rendered honestly."
security_privacy: NOT_RUN
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: PARTIAL

release_readiness: PARTIAL
release_followups: "Local Home/flow parity and authenticated emulator checkpoints are proven for the completed surfaces. Release still requires authorized staging/provider proof for customer↔merchant↔mitra mutation and payment callback/replay, plus a product/backend-owned account-deletion contract."

unproven_requirements: "Compact-width route/back-stack proof for FLOW-02 through FLOW-09; authenticated cross-app mutation and real payment callback/replay; saved-card/digital-wallet management remains an explicit backend capability gap; safe account-deletion behavior because no customer deletion contract exists in the backend."
known_blockers: NONE

locally_actionable_remaining: "Continue FLOW-02 through FLOW-09 visual/exit gates, including compact route proof and any remaining Figma screen hierarchy gaps; keep external provider proof as a release follow-up."

blocker_resolution_attempts: "Located the installed Android SDK and customer AVD, corrected the stale ANDROID_HOME/ANDROID_SDK_ROOT path, started Pixel_6_Pro_customer, repaired and verified the local Docker stack, rebuilt the stale admin-service image, built and installed the debug APK, loaded UAT credentials from the private vault only in the process, captured authenticated Home/emergency/account/notification/message/compact screenshots plus accessibility trees, delivered the supported orders/{orderId}/tracking deep link, corrected roadside client and backend status vocabulary, exercised local offline recovery and notification read-all, and checked logcat for customer crashes. Staging/provider callback behavior and the account-deletion contract remain outside the local environment."
unblock_condition: "Authenticated staging credentials and provider callback/replay environment are available through the approved secret channel; and product/backend owners provide or approve a customer account-deletion contract if that Figma action is required."

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: "Install the debug APK on the authorized emulator/device, exercise each completed flow at 390dp and a compact/common width, capture screenshot + accessibility state, verify cold-start/deep-link/process recreation/offline recovery, run authenticated customer↔merchant↔mitra scenarios, replay the real payment callback, and rerun applicable design/build/evidence gates."

dependency_chain_blocked: false
next_eligible_task: NONE

blocked_since: NONE
retry_when: NONE
updated_at: 2026-09-23
---

# Evidence — UIUX-2026-008

## Acceptance Criteria Source

`task-customer-figma-code-parity-2026.md` — FLOW-00 through FLOW-09, with the existing customer shell and cross-app service-flow boundaries described in the task file.

Proven at this checkpoint:

- `[x]` Customer screen source is reconciled with the stable component-library source without weakening the token/catalog validators.
- `[x]` Customer Home primary service hierarchy uses `Kirim Paket`, `Agregator`, `Food`, `Tambal Ban`, `Towing`, and `Lainnya`.
- `[x]` `Agregator` routes through the existing typed `aggregator` destination; `Ambil Paket` remains a secondary `pickup` intent.
- `[x]` Home wallet amount is profile-backed or explicitly unavailable; sample Figma/code amounts are not rendered as business truth.
- `[x]` Activity filter empty state follows the selected filter instead of stale tab state.
- `[x]` Missing customer `Business`, `Payment`, `ServiceTracking`, and `ServiceReport` destinations are registered in the root navigation graph.
- `[x]` Food, parcel, and roadside/towing order creation now enters server-authoritative payment confirmation before tracking/dispatch UI.
- `[x]` Customer wallet balance/top-up API models, repository, profile entry, secure route, and server-provided invoice URL handoff are wired.
- `[x]` Cross-app contract matrix records ownership and canonical lifecycle across customer, merchant, and mitra/courier apps.
- `[x]` Figma `Pesan` tab now opens an order-scoped inbox that reuses customer history and the existing chat bridge; the former business payment-link screen remains available on its own route.
- `[x]` Account `Alamat Tersimpan` now has a dedicated `address-book` destination with explicit back-stack behavior while booking continues to reuse the same address repository.
- `[x]` Towing tracking uses its own inspection/loading/transit vocabulary and bounded lifecycle mapping instead of parcel copy; roadside report errors expose a retry and honest empty-proof state.
- `[x]` Food discovery, saved-address editing, and runtime map surfaces no longer substitute Jakarta/sample coordinates: they require device/server coordinates and render an explicit location-unavailable state.
- `[x]` Debug auto-login no longer embeds a reusable credential in source; UAT credentials are read only from injected BuildConfig values.
- `[x]` Cold-start customer deep links are held while startup auth/NavGraph is unresolved and replayed only after an authenticated destination is ready.
- `[x]` Tambal Ban search rejects neutral `0,0` route coordinates and the service home renders backend errors with a retry instead of an empty-success surface.
- `[x]` Account `Privasi & Ketentuan` now consumes approved market legal documents from the existing public `/api/v1/markets/config` contract; document links are resolved only from server-provided first-party paths/URLs.
- `[x]` Parcel scheduled pickup now validates the 30-minute lead time, persists the normalized schedule, keeps paid orders in `scheduled`, and activates them through an admin worker before courier dispatch; food scheduled activation remains owned by the existing merchant/order-service worker.
- `[x]` Customer order detail now preserves server tracking identity (`order_number`, service category, scheduled pickup), offers resume-payment for `pending_payment`, and opens terminal tracking for proof/rating instead of hiding completed/cancelled detail actions.
- `[x]` Food checkout now exposes server quote expiry and fee breakdown, blocks stale quotes before submit, maps backend requote/item/minimum-order errors to recoverable copy, and keeps the food create idempotency key stable across retry.
- `[x]` Food merchant/menu availability now follows backend operating state, pause/enforcement, menu status/schedule, inventory, and daily limit; quote and create paths reject an item that became unavailable after discovery.
- `[x]` Payment recovery now re-reads server payment status after error/expiry/network recovery, preserves the server-authoritative paid boundary, and refuses to render an empty QRIS session when a non-paid response has no redirect URL.
- `[x]` Roadside tracking now reads the canonical order-detail snapshot directly, preserves the last snapshot with an explicit stale/offline message after refresh failure, and distinguishes no-technician terminal outcomes.
- `[x]` Terminal Tambal Ban/Towing tracking opens the shared Order Detail surface so cancellation, price adjustment, proof/report, support, claim, rating, and payment adjustment follow existing ownership instead of a second roadside flow.
- `[x]` Towing now mounts the existing roadside aftercare component in Order Detail; the service selector also avoids static price claims before the server quote exists.
- `[x]` Parcel review now renders the server quote components available in the contract and shows the server-provided protection premium when selected, while retaining the same quote expiry and idempotent create guard.
- `[x]` The existing `support` deep-link and notification action now navigate to a real customer help surface with WhatsApp/email fallback instead of being dropped by navigation.
- `[x]` Parcel booking draft input is serialized into `SavedStateHandle` for process recreation; server quote/price data is intentionally excluded and recalculated after restore.
- `[x]` Authenticated local Home runtime now matches the attached Figma composition: light location/search shell, compact wallet bar, active-order card, 2x3 service grid, promo banner, nearby-food strip, and Indonesian five-item bottom navigation; server-backed location, wallet, order, and merchant data remain authoritative.
- `[x]` Authenticated local Activity runtime now follows the Figma shell: shared location/search/wallet chrome, Indonesian title/subtitle, status filters, active-order map surface, server-backed order identity/status/address data, track/chat/call affordances, history section, and persistent five-item navigation. Missing tracking coordinates render an explicit compact unavailable state instead of sample map data.
- `[x]` Home visual tuning now uses the Figma off-white surface, compact wallet proportions, tighter 2x3 service tiles, and reduced active-order spacing; wallet voucher count and quick-repeat entry remain server-backed and never invent customer data.
- `[x]` Home promo parity now renders the Figma price line between the headline and supporting copy. The value is read from CMS `price`/`starting_price` when present and falls back to the design copy `Rp 8.000` only for the local empty-manifest experience; it is not used as an order quote.
- `[x]` Home voucher entry now opens a dedicated server-backed promo center through `GET /api/v1/customer/promos/eligible`; empty eligibility renders an honest empty state rather than a fabricated promo code.
- `[x]` Tambal Ban landing now follows the Figma emergency hierarchy: safety notice, device location, vehicle/issue selection, photo state, server price range, nearby technician state, consent, and guarded CTA.
- `[x]` Towing landing now follows the Figma emergency hierarchy: safety notice, route placeholders, vehicle type, condition chips, vehicle-photo state, consent, and existing server booking handoff.
- `[x]` Debug clean-install auth no longer starts with an implicit `debug_active_token`; when UAT credentials are injected process-only, the app obtains a real customer token before rendering authenticated Home data.
- `[x]` Food discovery runtime now follows the Figma composition with TEMBUS/location/search shell, category rail, filter chips, horizontal nearby merchants, server-derived availability/rating/distance facts, trust surface, and nearby merchant list; missing media remains an honest placeholder.
- `[x]` Food Merchant Detail runtime now follows the Figma hero composition with real merchant/menu media when supplied, overlay back/cart actions, merchant operating-state facts, menu categories, availability gating, and cart handoff; missing media remains an honest placeholder.
- `[x]` Food Checkout runtime now follows the Figma checkout rhythm with merchant context, compact order summary, quote/location guardrails, and a sticky `Pesan Sekarang` CTA; submit remains fail-closed until the server quote and delivery pin are valid, then hands off to the existing payment confirmation route.
- `[x]` Account runtime now follows the Figma hierarchy with compact customer header, TEMBUS-Pay green wallet, grouped account/security menus, preserved top-up/withdraw/profile actions, and persistent five-item navigation with `Akun` selected.
- `[x]` Notification runtime now keeps category filters, unread/read/archive actions, order-scoped navigation, honest empty/error/loading states, and persistent five-item navigation with `Notifikasi` selected.
- `[x]` Top Up wallet runtime now follows the Figma frame with a green balance hero, six-option nominal grid, honest provider/method disclosure, server-backed balance state, pending-session summary, and sticky payment CTA; runtime balance is `Rp 0` from the API, not a Figma sample.
- `[x]` Payment/wallet gateway forwarding now preserves the canonical `/api/v1/...` path instead of stripping the mounted Express prefix; authenticated emulator refresh changed wallet balance from the observed `404` to `200 OK` with a real API response.

Still unproven:

- `[x]` Emulator screenshot and accessibility tree at a 390dp-equivalent compact width; Home hierarchy remains readable without clipping after restoring the emulator size.
- `[x]` Unknown-order tracking deep link delivered to the customer activity and rendered the honest `Lokasi belum tersedia` state after the expected server 404; no customer `FATAL EXCEPTION` was observed.
- `[x]` Airplane-mode force-stop/start reached the branded offline splash without a customer crash; this is only a cold-start safety proof, not authenticated stale-session recovery.
- `[x]` Authenticated local offline/stale recovery and notification-badge runtime proof. Wi-Fi and mobile data were disabled through the emulator service controls; force-stop/start rendered authenticated Home with cached active-order content, `Kamu sedang offline`, retry copy, and unavailable wallet data. Network restoration returned the server-backed `Rp 0` wallet/Home state. The notification `Baca` mutation separately removed the unread count/dots. These are local emulator proofs, not provider or cross-app proofs.
- `[ ]` Compact-width route/back-stack proof for the broader FLOW-02 through FLOW-09 matrix; compact Home itself is proven.
- `[x]` Server-side top-up retry contract uses the client UUID key, deterministic provider reference, persisted provider session metadata, and a migration-backed uniqueness constraint.
- `[ ]` Real payment provider callback/replay proof; local build/test and local migration verification do not prove deployed/provider behavior.

## Scope Implemented

- Added a two-layer Figma source boundary: stable reusable component library plus `ALL TEMBUS CUSTOMER` customer-screen source.
- Added the FLOW-01 developer handoff spec with measurements, semantic tokens, component mapping, states, edge cases, responsive behavior, motion, and accessibility notes.
- Changed the Home service grid to match the Figma hierarchy and wired `Lainnya` to the existing universal-search entry.
- Aligned the bottom-navigation account label with Figma (`Akun`) while retaining the existing `profile` route.
- Wired the Home wallet to `ProfileRepository`; unavailable profile data now renders an explicit unavailable state.
- Corrected the existing activity/history filtered-empty copy/icon to use the selected filter.
- Reused the server-state-aware `OrderActionPolicy` for history status copy and expanded terminal error coloring without changing order state.
- Registered missing `Business`, `Payment`, `ServiceTracking`, and `ServiceReport` destinations.
- Routed food and roadside/towing order creation through payment confirmation before tracking/dispatch.
- Added wallet top-up UI/API contract with server-provided invoice URL and explicit pending state.
- Added server-side top-up retry/replay handling with deterministic reference IDs and migration-backed uniqueness; the customer receives the original invoice session on retry.
- Added cross-app contract matrix for customer ↔ merchant ↔ mitra/courier ownership and canonical lifecycle.
- Added customer towing report read endpoint with ownership validation and aligned customer/API-gateway report and settlement paths with order-service.
- Added a customer `Messages` route/screen for order-scoped conversations, with detail fallback and explicit empty/error/loading states.
- Added dedicated customer address-book route and routed Profile's saved-address entry to it.
- Added towing-specific status vocabulary/tests and service-report retry/empty-proof states.
- Added honest location gates for Food and runtime maps, and removed remaining customer runtime fallbacks that could present sample coordinates as live business state.
- Moved the debug UAT login contract to environment-injected BuildConfig credentials instead of a source-embedded password.
- Added startup-safe replay for validated order, tracking, chat, and booking deep links so auth resolution cannot discard or prematurely navigate them.
- Added a roadside location policy/test and explicit Tambal Ban search/home error states so a restored or direct route cannot query with neutral coordinates.
- Added the Account `privacy-terms` route, market-config Retrofit/repository contract, server-approved legal-document screen, and pure URL-resolution tests without inventing policy URLs in the client.
- Added the parcel scheduled-pickup contract across customer checkout, payment confirmation/webhook/LAPAY paths, cancellable status policy, and the admin scheduled-order worker; dispatch starts only after the scheduled activation transition.
- Added FLOW-02 order-detail read-model fields and action parity: order number/service category/scheduled pickup, pending-payment resume, and terminal proof/rating entry.
- Added the Figma Activity composition to `OrderHistoryScreen`: shared Home shell, active-order tracking snapshot/map state, service-aware Indonesian activity cards, status filters, persistent navigation, and track/chat/call routing; `OrderHistoryViewModel` now reads wallet, map configuration, and the canonical active tracking snapshot without inventing coordinates.
- Added FLOW-04 food availability/quote parity: merchant operating-state gate, menu schedule/inventory/moderation fields, authoritative minimum-order/fee/expiry display, recoverable backend error parsing, and server-side schedule availability enforcement.
- Added FLOW-05 payment recovery guard: expired/error actions resync the canonical payment session, and non-paid/no-redirect responses remain an explicit error instead of opening a blank provider surface.
- Added FLOW-08 safety-share contract: the customer can create, share, and revoke a server-issued read-only status link; the backend returns the canonical token identity and the mobile copy discloses minimum data without claiming live position.
- Added FLOW-09 security parity: customer account/transaction PIN change now calls the existing server contract with six-digit confirmation, while the existing local device PIN/biometric protection remains explicitly separate.
- Added server-backed Home promo eligibility/count wiring and a dedicated `PromoCenterScreen`; the empty UAT result remains an explicit empty state.
- Added Figma-aligned Tambal Ban and Derek Towing landing compositions while preserving existing location, service availability, quote, consent, and booking contracts.
- Added a server-backed `Pesan Lagi Cepat` Home block that opens Activity for review and never silently recreates a prior order.
- Corrected debug session bootstrap so clean installs do not treat a placeholder token as authenticated; injected UAT credentials now auto-login only when present, otherwise the app opens the real auth graph.

## Files Changed

- `design-tokens/tembus.tokens.json` — customer screen source metadata.
- `docs/design/tembus-component-catalog-2026.json` — customer screen source metadata while preserving stable-library validator contract.
- `docs/design/tembus-figma-governance-2026.md` — source boundary and authority rules.
- `docs/design/customer-home-figma-handoff-2026.md` — FLOW-01 handoff spec.
- `docs/design/customer-figma-code-flow-map-2026.md` — resolved Home mapping.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/DashboardHomeComponents.kt` — Home hierarchy and profile-backed wallet rendering.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/DashboardScreen.kt` — Home callbacks and wallet state wiring.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/DashboardOrderSections.kt` — server-backed Figma-like active-order summary and quick-repeat card.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/DashboardViewModel.kt` — profile-backed wallet state.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/promo/PromoCenterScreen.kt` — eligible-promo loading, honest empty/error states, and server-code handoff to booking.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/VoucherModels.kt` — eligible customer promo response contract.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/session/AuthSessionManager.kt` — no implicit debug session on clean install; real/session-expiry behavior remains explicit.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/history/OrderHistoryScreen.kt` — Figma Activity shell, active-order map availability, service-aware history cards, filters, persistent navigation, and canonical track/chat/call actions; existing local user changes preserved.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/history/OrderHistoryViewModel.kt` — profile-backed wallet, map configuration, and canonical active tracking snapshot for Activity.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/navigation/Screen.kt` — payment/service and dedicated `messages`/`address-book` route contracts.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/navigation/RootNavGraph.kt` — missing route registration, address-book destination, and payment handoffs.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/messages/MessagesScreen.kt` — Figma Pesan inbox backed by canonical order history/chat routes.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/WalletTopUpScreen.kt` — wallet top-up provider handoff UI.
- `backend/api-gateway/src/index.ts` — payment/wallet proxy path preservation under Express 5 and authenticated path filtering.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/WalletTopUpViewModel.kt` — top-up validation and pending state.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/api/TEMBUSApiService.kt` — wallet balance/top-up, settlement, and eligible-promo read contracts.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/CustomerModels.kt` — wallet top-up session models.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/repository/ProfileRepository.kt` — wallet balance/top-up repository wiring.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceTrackingScreen.kt` — roadside chat/call/report actions and honest error state.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceTrackingPolicy.kt` — bounded tambal-ban and towing status vocabulary.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceTrackingViewModel.kt` — canonical detail refetch, terminal/no-supply classification, and stale snapshot state.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/service/ServiceTrackingPolicyTest.kt` — terminal/no-supply state coverage.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceReportScreen.kt` — retry and honest missing-proof states.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceReportViewModel.kt` — clears stale report data before retry.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/service/ServiceTrackingPolicyTest.kt` — towing lifecycle/copy tests.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodHomeScreen.kt` — Figma food discovery shell, category/filter rail, merchant surfaces, honest location/error/empty/loading states, and coordinate-preserving retry.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceLocationPolicy.kt` — neutral-coordinate guard for roadside search.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/TambalBanSearchScreen.kt` — location-unavailable state for direct/restored routes.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/TambalBanHomeScreen.kt` — backend error/retry state.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/service/ServiceLocationPolicyTest.kt` — valid/neutral/out-of-range coordinate coverage.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/MarketConfigModels.kt` — public market/legal document response models.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/repository/MarketConfigRepository.kt` — approved legal document read contract.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/PrivacyTermsScreen.kt` — Account legal-document surface and safe browser handoff.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/PrivacyTermsViewModel.kt` — loading/error/retry/content state.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/PrivacyTermsPolicy.kt` — first-party URL resolution guard.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/ProfileScreen.kt` — Figma-aligned Account shell, compact wallet/menu groups, preserved profile actions, and persistent navigation.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/notifications/NotificationCenterScreen.kt` — persistent Figma shell navigation for notification categories and order-scoped notification actions.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/profile/PrivacyTermsPolicyTest.kt` — relative/unsupported legal URI coverage.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingComponents.kt` — pickup-time choice and server-contract-aligned schedule copy.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingScreen.kt` — minimum-lead schedule picker.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingStepContent.kt` — schedule card placement in the checkout sequence.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingViewModel.kt` — ISO schedule serialization and validation.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/policy/PackageOrderFlowPolicy.kt` — shared 30-minute schedule policy.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/policy/PackageOrderFlowPolicyTest.kt` — schedule lead-time coverage.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/FoodModels.kt` — merchant/menu availability, minimum-order, and quote-expiry fields.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodCheckoutPolicy.kt` — fail-closed quote-expiry policy.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodCheckoutScreen.kt` — authoritative food fee/minimum/expiry disclosure and stale-quote guard.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodViewModel.kt` — recoverable food API error parsing, quote invalidation, and persisted cart merchant context for checkout parity.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/MerchantDetailScreen.kt` — Figma hero overlay, merchant closed/menu-unavailable gating, and minimum-order disclosure.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/food/FoodCheckoutPolicyTest.kt` — quote expiry boundary and fail-closed coverage.
- `backend/order-service/internal/domain/order_food.go` — minimum-order field in the food quote contract.
- `backend/order-service/internal/repository/food_repository.go` — detail menu availability/schedule/inventory/moderation read model.
- `backend/order-service/internal/service/food_quote_service.go` — server minimum-order quote field.
- `backend/order-service/internal/service/order_food.go` — create-time schedule/status availability enforcement.
- `backend/order-service/internal/service/order_food_test.go` — regression proof for menu schedule invalidation.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/payment/PaymentViewModel.kt` — server-status recovery and no-empty-redirect guard.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/payment/PaymentScreen.kt` — expired/error actions resync payment state.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingStepContent.kt` — passes server protection premium into the parcel add-on surface.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingComponents.kt` — quote component breakdown and protection-premium disclosure in parcel review.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingViewModel.kt` — primitive booking-draft persistence with server quote invalidation on restore.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceTrackingScreen.kt` — stale/offline/no-supply disclosure, terminal proof/support handoff, and service-specific actions.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/tracking/TrackingComponents.kt` — canonical service-category mapping and technician/towing/food/package vocabulary selection.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/tracking/TrackingDetailSections.kt` — category-aware stage, timeline, proof, and package-section rendering.
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/tracking/TrackingScreenLogicTest.kt` — regression coverage for category-only Tambal Ban tracking payloads.
- `backend/admin-service/src/controllers/order/customerOrderTracking.controller.ts` — canonical service category/subtype fields in the customer tracking-detail read model; local admin image rebuilt from this worktree for runtime proof.
- `backend/admin-service/src/controllers/order/statusLabels.ts` — category-aware customer status vocabulary for roadside, towing, food, and parcel orders.
- `backend/admin-service/src/controllers/order/statusLabels.test.ts` — status-label regression coverage for category-only roadside payloads and cross-service vocabulary.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceCategoryScreen.kt` — server-quote pricing disclosure.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/ServiceCategoryScreen.kt` — Figma-aligned Derek Towing emergency landing and server booking handoff.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/TambalBanHomeScreen.kt` — Figma-aligned Tambal Ban emergency landing with real location/service/quote states.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/service/SubTypeSelectorScreen.kt` — removes static pre-quote price claims.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderDetailScreen.kt` — mounts roadside aftercare for towing as well as Tambal Ban.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/SupportScreen.kt` — customer help destination with official contact actions.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/navigation/RootNavGraph.kt` — permission/location handoff for Food discovery without a sample fallback.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/components/maps/RuntimeMapRenderer.kt` — explicit unavailable state when runtime coordinates are absent.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/components/maps/MapPrimitives.kt` — neutral map state instead of a sample city camera.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/MainViewModel.kt` — injected-only debug UAT credentials.
- `backend/payment-service/internal/domain/wallet.go` — provider-neutral top-up session contract.
- `backend/payment-service/internal/service/wallet_service.go` — provider invoice URL propagation.
- `backend/payment-service/internal/handler/wallet_handler.go` — top-up session response contract.
- `database/migrations/20260921000001_wallet_topup_idempotency.sql` — canonical unique top-up reference constraint for retry safety.
- `docs/design/customer-cross-app-contract-matrix-2026.md` — cross-app ownership/lifecycle matrix.
- `backend/admin-service/src/controllers/safety.controller.ts` — customer safety-share token identity and read-only response contract.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/SafetyModels.kt` — safety-share/revocation response models.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/repository/TrackingRepository.kt` — customer safety-share create/revoke calls with response validation.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/tracking/TrackingViewModel.kt` — share URL/token lifecycle and idempotency keys.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/tracking/TrackingScreen.kt` — share/revoke actions and truthful minimum-data disclosure.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/AuthModels.kt` — server PIN change request/response contract.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/repository/AuthRepository.kt` — server PIN mutation and safe error mapping.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/ProfileViewModel.kt` — customer PIN mutation state.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/profile/ProfileScreen.kt` — account PIN dialog separated from local device security.
- `task-customer-figma-code-parity-2026.md` — current implementation checkpoint.

## Commands / Checks Run

    command: node scripts/design/validate-tembus-design-tokens.mjs
    result: PASS — token contrast, generated CSS/TypeScript, and Android mappings are synchronized.

    command: node scripts/design/validate-tembus-component-adoption.mjs
    result: PASS — existing benchmark comment was removed from production source; component/mobile acceptance contract passes.

    command: node scripts/design/validate-tembus-figma-traceability.mjs
    result: PASS — 14 stable components, 3 governed library pages, Code Connect deferred.

    command: Get-Content JSON | ConvertFrom-Json; git diff --check
    result: PASS — metadata JSON parses and diff has no whitespace errors.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest; Pop-Location
    result: PASS — BUILD SUCCESSFUL; 36 actionable tasks, 12 executed, 24 up-to-date.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL; customer unit tests and debug APK assembly completed after address-book, towing/report, no-fake-location, injected-UAT-credential, and Tambal Ban location/error-state updates.

    command: Push-Location android-app-customer; .\gradlew.bat :app:compileDebugKotlin; Pop-Location
    result: PASS — BUILD SUCCESSFUL after startup-safe deep-link replay and Food retry-state cleanup; only existing deprecation/unused-parameter warnings remain.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after the market-config legal route, URL guard, and parcel pickup schedule UI/contract were added; the latest run completed customer JVM tests and debug APK assembly.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after FLOW-02 detail-action and FLOW-04 food quote/availability changes; customer JVM tests and debug APK assembly completed. Existing deprecation/unused-parameter warnings remain.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after FLOW-05 payment recovery/no-empty-redirect changes; customer JVM tests and debug APK assembly completed. Existing deprecation/unused-parameter warnings remain.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after FLOW-06/FLOW-07 canonical roadside snapshot, stale/offline, terminal handoff, towing aftercare, and pre-quote copy changes; customer JVM tests and debug APK assembly completed. Existing deprecation/unused-parameter warnings remain.

    command: Push-Location backend/order-service; go test ./internal/service ./internal/handler; Pop-Location
    result: PASS — roadside aftercare/report handlers and service contracts passed.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after parcel server-quote component/protection disclosure changes; customer JVM tests and debug APK assembly completed. Existing deprecation/unused-parameter warnings remain.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after FLOW-08 support destination/deep-link and notification wiring; customer JVM tests and debug APK assembly completed. Existing deprecation/unused-parameter warnings remain.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after parcel booking draft persistence via SavedStateHandle; customer JVM tests and debug APK assembly completed. Actual process-death/device recreation remains NOT_RUN without an authorized emulator/device.

    command: Push-Location backend/admin-service; npm run build; Pop-Location
    result: PASS — TypeScript build completed after the customer safety-share response began returning the canonical token id for revocation.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after FLOW-08 safety-share create/share/revoke wiring; customer JVM tests and debug APK assembly completed. Authenticated device flow and public-link expiry/revocation remain NOT_RUN.

    command: Push-Location backend/admin-service; npm test -- --runInBand src/controllers/customerAuth.controller.test.ts; Pop-Location
    result: PASS — customer PIN controller suite passed 3/3 tests, including malformed input rejection and authenticated PIN-change behavior.

    command: Push-Location android-app-customer; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL after FLOW-09 server PIN change wiring; customer JVM tests and debug APK assembly completed. Authenticated server response and device accessibility proof remain NOT_RUN.

    command: Push-Location backend/admin-service; npm run build; npm test -- --runInBand src/services/customerOrderSchedule.test.ts src/controllers/order/customerOrderConnectionOwnership.test.ts; Pop-Location
    result: PASS — TypeScript build completed; 5 targeted tests passed covering schedule normalization, lead time, capability rejection, payment status mapping, and controller ownership boundaries.

    command: Push-Location backend/payment-service; go test ./...; Pop-Location
    result: PASS — all payment-service packages passed after idempotent top-up contract changes.

    command: database/migrations/20260921000001_wallet_topup_idempotency.sql
    result: PASS — Goose applied the migration, rolled it back, reapplied it, and PostgreSQL confirmed the unique index plus zero pre-existing TOPUP-* rows.

    command: Push-Location backend/order-service; go test ./...; Pop-Location
    result: PASS — all order-service packages passed, including payment/status/roadside contracts.

    command: Push-Location backend/order-service; go test ./internal/service ./internal/repository; Pop-Location
    result: PASS — food quote/create service and repository packages passed, including the menu schedule invalidation regression test.

    command: Push-Location backend/merchant-service; go test ./...; Pop-Location
    result: PASS — merchant order/settlement packages passed.

    command: Push-Location backend/api-gateway; npm run build; npm run test:auth-matrix; Pop-Location
    result: PASS — TypeScript build and gateway JWT route-auth matrix passed after order-domain forwarding updates.

    command: Push-Location backend/order-service; gofmt -w internal/handler/roadside_customer_report_handler.go cmd/api/main.go; go test ./...; Pop-Location
    result: PASS — order-service report handler and route registration compiled and all packages passed.

    command: Push-Location android-app; .\gradlew.bat :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL; mitra/courier unit tests and debug APK assembly completed.

    command: git diff --check
    result: PASS — no whitespace errors in the current diff.

    command: C:\\Users\\yogis\\AppData\\Local\\Android\\Sdk\\platform-tools\\adb.exe -s emulator-5554 install --no-incremental -r -d -g android-app-customer/app/build/outputs/apk/debug/app-debug.apk
    result: PASS — customer debug APK installed on the Pixel_6_Pro_customer emulator.

    command: adb -s emulator-5554 shell monkey -p com.tembus.customer 1
    result: PASS — customer process launched; logcat showed no FATAL EXCEPTION for com.tembus.customer.

    command: adb -s emulator-5554 shell uiautomator dump /sdcard/customer-window.xml; adb -s emulator-5554 shell cat /sdcard/customer-window.xml; adb -s emulator-5554 exec-out screencap -p > runtime_customer_smoke.png
    result: PASS — login surface exposes readable TEMBUS, email, password, password-visibility, forgot-password, login, and registration semantics; screenshot recorded at runtime_customer_smoke.png.

    command: adb -s emulator-5554 shell input tap 900 2880; adb -s emulator-5554 shell uiautomator dump /sdcard/customer-register.xml; adb -s emulator-5554 shell cat /sdcard/customer-register.xml; adb -s emulator-5554 exec-out screencap -p > runtime_customer_register.png
    result: PASS — registration surface exposes full-name, phone, email, password, Google sign-up, and disabled empty-submit semantics; no customer process crash observed.

    command: adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 shell input tap 1100 2380; adb -s emulator-5554 shell uiautomator dump /sdcard/customer-forgot.xml; adb -s emulator-5554 shell cat /sdcard/customer-forgot.xml; adb -s emulator-5554 exec-out screencap -p > runtime_customer_forgot.png
    result: PASS — forgot-password surface exposes reset-policy notice, email field, disabled empty submit, safe return-to-login action, and protection copy; screenshot recorded at runtime_customer_forgot.png.

    command: adb -s emulator-5554 shell am start -a android.intent.action.VIEW -d tembus://orders/ORDER-EXPIRED/tracking
    result: PASS — supported order tracking deep link resolved to the customer activity; unauthenticated state remained on login and no order data was exposed. The unsupported tembus://tracking host was intentionally not treated as a valid route.

    command: graphify update .
    result: PASS — graph rebuilt with 32,717 nodes, 55,263 edges, and 1,921 communities after FLOW-02/FLOW-04 contract changes; existing SQL parser/zero-node/edge-extraction warnings remain and are not task evidence.

    command: graphify update .
    result: PASS — graph rebuilt with 32,721 nodes, 55,274 edges, and 1,915 communities after FLOW-06/FLOW-07 tracking/aftercare changes; existing SQL parser/zero-node/edge-extraction warnings remain and are not task evidence.

    command: graphify update .
    result: PASS — graph rebuilt with 32,724 nodes, 55,284 edges, and 1,909 communities after FLOW-08 support destination wiring; existing SQL parser/zero-node/edge-extraction warnings remain and are not task evidence.

    command: graphify update .
    result: PASS — graph rebuilt with 32,728 nodes, 55,294 edges, and 1,934 communities after parcel booking draft persistence; existing SQL parser/zero-node/edge-extraction warnings remain and are not task evidence.

    command: graphify update .
    result: PASS — graph rebuilt with 32,738 nodes, 55,309 edges, and 1,926 communities after FLOW-08 safety-share wiring; existing SQL parser/zero-node/edge-extraction warnings remain and are not task evidence.

    command: graphify update .
    result: PASS — graph rebuilt with 32,746 nodes, 55,327 edges, and 1,917 communities after FLOW-09 account PIN wiring and safe network-error mapping; existing SQL parser/zero-node/edge-extraction warnings remain and are not task evidence.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 2m 59s; 55 actionable tasks, 12 executed and 43 up-to-date after the Figma Home header, compact wallet, active-order card, service grid, promo banner, Indonesian shell labels, and tile-copy correction.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 3m 41s; 55 actionable tasks, 13 executed and 42 up-to-date after the compact emergency badge correction.

    command: adb -s emulator-5554 install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 shell uiautomator dump /sdcard/customer-figma-home-final.xml; adb -s emulator-5554 exec-out screencap -p > customer-figma-home-final-id.png
    result: PASS — authenticated local customer Home rendered on emulator-5554 with no FATAL EXCEPTION after the compact badge correction; final screenshot and accessibility tree are recorded in customer-figma-home-final-id.png and customer-figma-home-final.xml.

    command: adb -s emulator-5554 shell input swipe 720 650 720 2700 700; adb -s emulator-5554 exec-out screencap -p > customer-figma-home-final-id.png
    result: PASS — Home reset to the top scroll position for visual inspection; location/search shell, wallet, active order, 2x3 services, promo, recommendation, and Indonesian bottom navigation are visible in the final artifact. UAT credentials were read from the private vault only in the process and are not stored in evidence.

    command: graphify update .
    result: PASS — graph rebuilt with 32,749 nodes, 55,335 edges, and 1,909 communities after the authenticated Home visual parity changes; existing SQL/parser and edge-field warnings remain non-blocking.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:compileDebugKotlin; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 2m 45s after the FLOW-02 Activity shell, map-availability, wallet/profile, tracking-snapshot, and root-navigation changes; existing deprecation/unused-parameter warnings remain.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 1m 01s; debug APK packaged from the FLOW-02 Activity changes.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 1m 14s; 55 actionable tasks, 7 executed and 48 up-to-date after FLOW-02 Activity changes.

    command: adb -s emulator-5554 install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb -s emulator-5554 shell am force-stop com.tembus.customer; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 shell input tap 425 2850; adb -s emulator-5554 shell uiautomator dump /sdcard/activity-flow-02-runtime.xml; adb -s emulator-5554 exec-out screencap -p > activity-flow-02-runtime-v2.png
    result: PASS — authenticated Activity rendered on emulator-5554 with live reverse-geocoded device location, Indonesian shell labels, active order `#TMB-805738-000408`, and the explicit no-coordinate map state. Screenshot and AX tree are recorded as activity-flow-02-runtime-v2.png and activity-flow-02-runtime-v2.xml.

    command: adb -s emulator-5554 shell input swipe 720 2600 720 800 500; adb -s emulator-5554 shell uiautomator dump /sdcard/activity-flow-02-history-runtime.xml; adb -s emulator-5554 exec-out screencap -p > activity-flow-02-history-runtime.png
    result: PASS — Activity scroll reaches the Figma-aligned `RIWAYAT PESANAN` section and its honest empty state for the authenticated account, while the persistent bottom navigation remains visible.

    command: adb -s emulator-5554 shell input tap 550 1985; adb -s emulator-5554 shell dumpsys activity activities; adb -s emulator-5554 exec-out screencap -p > activity-flow-02-track-action.png
    result: PASS — the active-card `Lacak Pesanan` CTA opened the canonical tracking surface for the server order; tracking displayed the existing order lifecycle/timeline and package snapshot.

    command: graphify update .
    result: PASS — graph rebuilt with 32,764 nodes, 55,394 edges, and 1,896 communities after the FLOW-02 Activity implementation; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 3m 26s; 55 actionable tasks, 16 executed and 39 up-to-date after eligible-promo contracts, PromoCenter navigation, and server-backed quick-repeat wiring.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 2m 34s after Figma Home surface, compact wallet, service-tile, and active-order spacing adjustments; existing deprecation/unused-parameter warnings remain.

    command: adb -s emulator-5554 install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity
    result: PASS — latest debug APK installed and customer activity launched without a customer-process crash. A clean-install smoke run used the existing debug fallback token; customer data endpoints returned 401, so that run is not evidence of authenticated data parity.

    command: adb -s emulator-5554 logcat -d -v time | Select-String 'okhttp|HTTP FAILED|401 Unauthorized|FATAL EXCEPTION'
    result: PARTIAL — no customer FATAL EXCEPTION; the clean-install fallback token produced expected 401 responses for customer data endpoints. The previously captured authenticated Home/Activity artifacts remain the valid data-backed proof.

    command: graphify update .
    result: PASS — graph rebuilt with 32,795 nodes, 55,480 edges, and 1,944 communities after promo, Home, Tambal Ban, and Towing changes; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: python scripts/tasks/validate_task_evidence.py; node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; git diff --check
    result: PASS — evidence gate, token sync/contrast, component adoption, Figma traceability, and whitespace checks passed; the evidence validator retains repository-wide advisory warnings for older task documents.

    command: Push-Location backend/auth-service; go test -v -timeout 5m ./...; Pop-Location; Push-Location backend/routing-service; go test -v -timeout 5m ./...; Pop-Location; Push-Location backend/order-service; go test -v -timeout 5m ./...; Pop-Location
    result: PASS — auth-service, routing-service, and order-service test suites completed successfully.

    command: Push-Location backend/admin-service; npm test; Pop-Location
    result: PASS — 136 test suites and 631 tests passed.

    command: Push-Location backend/auth-service; go vet ./...; Pop-Location; Push-Location backend/routing-service; go vet ./...; Pop-Location; Push-Location backend/order-service; go vet ./...; Pop-Location
    result: PASS — Go vet completed for all Makefile Go lint targets.

    command: Push-Location frontend; npm run lint; Pop-Location
    result: PASS — exit code 0 with 0 errors and 512 existing warnings.

    command: Push-Location backend/admin-service; npm run lint; Pop-Location
    result: NOT_RUN — package.json has no `lint` script; no admin lint result is claimed.

    command: Push-Location android-app-customer; MOBILE_UAT_EMAIL/MOBILE_UAT_PASSWORD/MOBILE_API_BASE_URL injected process-only; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 3m 38s after removing implicit debug-session bootstrap; credentials were read from the private vault only in the process and were not written to source or evidence.

    command: adb -s emulator-5554 uninstall com.tembus.customer; adb -s emulator-5554 install app-debug.apk; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 shell uiautomator dump; adb -s emulator-5554 exec-out screencap -p
    result: PASS — clean-install debug UAT auto-login obtained a real customer session; profile, orders, delivery-services, banners, eligible promos, notifications, food merchants, device-token, and Socket.IO calls returned successful authenticated responses. Home screenshot and AX tree were captured in the temporary runtime artifacts; no customer FATAL EXCEPTION occurred.

    command: graphify update .
    result: PASS — graph rebuilt with 32,795 nodes, 55,480 edges, and 1,935 communities after the explicit debug-session bootstrap fix; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: Push-Location backend/api-gateway; npm run build; npm run test:auth-matrix; Pop-Location
    result: PASS — TypeScript build and gateway JWT route-auth matrix passed after preserving the canonical payment/wallet path through the Express 5 proxy.

    command: docker compose build payment-service api-gateway; docker compose up -d --no-deps payment-service api-gateway
    result: PASS — rebuilt and restarted the local payment-service and API gateway images from the current worktree; payment-service health endpoint returned `200 OK`.

    command: Push-Location android-app-customer; .\gradlew.bat :app:compileDebugKotlin --no-daemon; .\gradlew.bat :app:assembleDebug --no-daemon; Pop-Location
    result: PASS — Kotlin compilation and debug APK assembly completed after the Figma-aligned Top Up wallet surface changes; existing deprecation/unused-parameter warnings remain.

    command: adb -s emulator-5554 install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb -s emulator-5554 shell input tap 1300 2820; adb -s emulator-5554 shell input tap 1050 850; adb -s emulator-5554 exec-out screencap -p > wallet-topup-after-gateway-fix.png
    result: PASS — authenticated emulator rendered the Figma-aligned Top Up wallet surface with real `Rp 0` balance, nominal grid, provider disclosure, and sticky CTA; screenshot recorded in the temporary runtime artifact.

    command: adb -s emulator-5554 shell input tap 1270 760; adb -s emulator-5554 logcat -d -t 500 | Select-String 'GET http://10.0.2.2:8080/api/v1/wallet/balance|404 Not Found|200 OK http://10.0.2.2:8080/api/v1/wallet/balance|FATAL EXCEPTION'
    result: PASS — authenticated wallet refresh returned `200 OK` with a 265-byte API response after the gateway path fix; no customer `FATAL EXCEPTION` was reported in the filtered output. The provider callback/payment completion path remains NOT_RUN.

    command: graphify update .
    result: PASS — graph rebuilt with 32,813 nodes, 55,531 edges, and 1,912 communities after the Top Up wallet surface and payment-proxy path fix; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: Push-Location android-app-customer; .\gradlew.bat :app:compileDebugKotlin --no-daemon; .\gradlew.bat :app:assembleDebug --no-daemon; Pop-Location
    result: PASS — final Android compile and debug APK assembly passed after Home, Merchant Detail, and Food Checkout Figma parity tuning; existing deprecation/unused-parameter warnings remain.

    command: adb -s emulator-5554 install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb -s emulator-5554 shell am force-stop com.tembus.customer; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 exec-out screencap -p > customer-home-final-parity-ready.png
    result: PASS — authenticated UAT emulator rendered the Figma-aligned Home shell with server-backed location, wallet, active order, green tracking CTA, 2x3 services, promo, and persistent Indonesian navigation; screenshot recorded in the temporary runtime artifact.

    command: adb -s emulator-5554 shell input tap 1175 1700; adb -s emulator-5554 shell input tap 250 2050; adb -s emulator-5554 shell input tap 1170 2910; adb -s emulator-5554 exec-out screencap -p > checkout-after-build.png; adb -s emulator-5554 shell uiautomator dump /sdcard/checkout-after-build.xml; adb -s emulator-5554 logcat -d -t 800 | Select-String 'FATAL EXCEPTION|AndroidRuntime'
    result: PASS — authenticated local Food discovery → Merchant Detail → cart → Checkout navigation rendered without customer crash; Merchant Detail showed real UAT food media/menu and overlay actions, Checkout showed merchant context and sticky `Pesan Sekarang`, and the missing delivery pin was surfaced as an explicit validation error. Order creation/provider callback was not executed.

    command: graphify update .
    result: PASS — graph rebuilt with 32,813 nodes, 55,531 edges, and 1,900 communities after the Home/Merchant/Checkout parity changes; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: adb -s emulator-5554 shell wm size 1365x3120; adb -s emulator-5554 shell input swipe 700 500 700 2800 1000; adb -s emulator-5554 exec-out screencap -p > customer-home-390dp-top.png; adb -s emulator-5554 shell uiautomator dump /sdcard/customer-home-390dp-top.xml; adb -s emulator-5554 shell wm size reset
    result: PASS — compact 390dp-equivalent Home screenshot and accessibility tree were captured; header, wallet, active order, 2x3 service grid, promo, and navigation remained readable without clipping. Emulator size was restored to the physical `1440x3120` configuration.

    command: adb -s emulator-5554 shell am force-stop com.tembus.customer; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 exec-out screencap -p > customer-home-final-parity-ready.png; adb -s emulator-5554 logcat -d -t 500 | Select-String 'FATAL EXCEPTION|AndroidRuntime'
    result: PASS — cold-start/process-recreation smoke returned to authenticated Home after force-stop/start with no customer `FATAL EXCEPTION`; deep-link/offline/notification-badge scenarios remain NOT_RUN.

    command: adb -s emulator-5554 shell am start -W -a android.intent.action.VIEW -d tembus://orders/00000000-0000-0000-0000-000000000000/tracking com.tembus.customer; adb -s emulator-5554 exec-out screencap -p > customer-deeplink-unknown-tracking.png; adb -s emulator-5554 shell uiautomator dump /sdcard/customer-deeplink-unknown-tracking.xml; adb -s emulator-5554 logcat -d -t 300 | Select-String 'FATAL EXCEPTION|AndroidRuntime|com.tembus.customer'
    result: PASS — supported tracking deep link delivered to the customer activity; the dummy order returned the expected server 404 and the UI rendered `Lokasi belum tersedia` without inventing coordinates or exposing data. No customer `FATAL EXCEPTION` was observed.

    command: adb -s emulator-5554 shell settings put global airplane_mode_on 1; adb -s emulator-5554 shell am broadcast -a android.intent.action.AIRPLANE_MODE --ez state true; adb -s emulator-5554 shell am force-stop com.tembus.customer; adb -s emulator-5554 shell am start -n com.tembus.customer/.ui.MainActivity; adb -s emulator-5554 exec-out screencap -p > customer-offline-start.png; adb -s emulator-5554 shell settings put global airplane_mode_on 0; adb -s emulator-5554 shell am broadcast -a android.intent.action.AIRPLANE_MODE --ez state false; adb -s emulator-5554 shell settings get global airplane_mode_on; adb -s emulator-5554 shell logcat -d -t 500 | Select-String 'FATAL EXCEPTION|AndroidRuntime|UnknownHost|Unable to resolve host|com.tembus.customer'
    result: PARTIAL — offline cold start captured the branded splash and emitted no customer fatal/host-resolution crash. After network restoration and a subsequent force-stop/start, the app rendered the real login surface because the installed APK had no injected UAT credentials; authenticated offline session recovery is therefore unproven.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 48s after Derek Towing landing, subtype selector, nearby-petugas palette, sticky CTA, Courier Detail, and Service Booking visual parity updates; existing deprecation/unused-parameter warnings remain.

    command: node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; python scripts/tasks/validate_task_evidence.py; git diff --check
    result: PASS — design token, component adoption, Figma traceability, evidence, and whitespace gates passed; repository-wide evidence warnings remain advisory and pre-existing.

    command: graphify update .
    result: PASS — graph rebuilt with 32,813 nodes, 55,531 edges, and 1,906 communities after the latest service-flow UI parity changes; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 2m 53s after changing the Home active-order `Lacak Live` CTA to the Figma orange CTA token with dark accessible text; existing deprecation/unused-parameter warnings remain.

    command: graphify update .
    result: PASS — graph rebuilt with 32,813 nodes, 55,531 edges, and 1,908 communities after the Home CTA parity correction; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 3m 1s after aligning Tambal Ban with the emergency Figma treatment: fixed off-white surface, server-backed pricing copy, and a persistent orange technician CTA with explicit consent gating; existing deprecation/unused-parameter warnings remain.

    command: node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; python scripts/tasks/validate_task_evidence.py; git diff --check
    result: PASS — design token, component adoption, Figma traceability, evidence, and whitespace gates passed; repository-wide evidence warnings remain advisory and pre-existing.

    command: graphify update .
    result: PASS — graph rebuilt with 32,814 nodes, 55,532 edges, and 1,929 communities after the Tambal Ban CTA/pricing parity correction; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 1m 6s after aligning the Notification Center to the Figma `#F2FCF3` canvas, compact notification cards, denser category tabs, and the persistent five-item navigation shell; existing deprecation/unused-parameter warnings remain.

    command: graphify update .
    result: PASS — graph rebuilt with 32,814 nodes, 55,532 edges, and 1,931 communities after the Notification Center parity correction; existing SQL/zero-node/edge-field warnings remain non-blocking.

    command: node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; python scripts/tasks/validate_task_evidence.py; git diff --check
    result: PASS — design token, component adoption, Figma traceability, evidence, and whitespace gates passed; repository-wide evidence warnings remain advisory and pre-existing.

    command: Push-Location android-app-customer; .\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 3m 33s after FLOW-08 customer shell parity updates for Messages, courier chat, roadside tracking loading/status card, and Support; existing deprecation/unused-parameter warnings remain.

    command: adb devices; adb install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb shell am start -n com.tembus.customer/com.tembus.customer.ui.MainActivity; adb shell dumpsys activity activities; adb exec-out screencap -p > C:\Users\yogis\AppData\Local\Temp\customer-flow08-launch-2.png; adb logcat -d -t 250 | Select-String 'FATAL EXCEPTION|AndroidRuntime|com.tembus.customer'
    result: PARTIAL — Pixel_6_Pro_customer/emulator-5554 was available, APK installed, MainActivity reached the real Login surface, and no customer fatal exception was observed. No UAT credentials were injected in this run, so authenticated Messages/Chat/Support/roadside screenshots are not claimed.

    command: node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; python scripts/tasks/validate_task_evidence.py; git diff --check
    result: PASS — token sync/contrast, component adoption, Figma traceability, task evidence, and whitespace checks passed after FLOW-08 parity updates; repository-wide advisory warnings remain pre-existing.

    command: graphify update .
    result: PASS — graph rebuilt with 32,814 nodes, 55,535 edges, and 1,916 communities after Messages/Chat/Support/roadside tracking parity updates; existing SQL parser/zero-node/edge-extraction warnings remain non-blocking.

## FLOW-09 account/profile shell follow-up

- The Account, Address Book, legal/privacy, Support, and Business/E-Faktur surfaces now use the Figma light-green customer canvas (`#F2FCF3`) and retain the existing server-backed navigation and session contracts. Business copy was changed from an English payment-link title to the customer-facing `Profil Bisnis & E-Faktur`; the invoice card reports server confirmation status only.
- Package flow structure was not changed; this follow-up is limited to shared shell/color alignment around the Figma account/profile family.

- Account header parity was tightened against the selected Figma `TEMBUS - Akun Pengguna` frame: the old white `Akun` app bar was removed in favor of a status-bar-aware `TEMBUS`/location chrome above identity and wallet. Location remains `Pilih area` until a real profile/location contract supplies a value.
- Saved Address parity was tightened against the selected Figma `TEMBUS - Alamat Tersimpan` frame: the canvas now uses `#F7F8F6`, with a search field, `Semua/Rumah/Kantor` filters, server-backed filtered list, and sticky orange `Tambah alamat baru` CTA. CRUD, device-location permission, loading, empty, and error behavior remain owned by the existing repository/view model.
- Direct Figma comparison also corrected the `Pusat Bantuan` and `Privasi & Ketentuan` shells to `#F7F8F6`; the notification and courier-chat shells remain on the Figma `#F2FCF3` canvas.
- Business profile parity was tightened against `TEMBUS - Profil Bisnis & E-Faktur`: the server-backed store identity is now a dark-green hero with explicit `SERVER` status, followed by the honest E-Faktur state and the existing payment-link workflow. NPWP, tax toggles, and other Figma-only fields remain unrepresented because no customer contract currently supplies them.

    command: Push-Location android-app-customer; .\\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 3m 40s after Account, Saved Address, Support, Privacy/Terms, and Business hero parity corrections; existing deprecation/unused-parameter warnings remain.

    command: adb install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb shell am start -n com.tembus.customer/.ui.MainActivity; adb exec-out screencap -p > C:\\Users\\yogis\\AppData\\Local\\Temp\\customer-flow09-login.png; adb logcat -d -t 300 | Select-String 'FATAL EXCEPTION|AndroidRuntime|com.tembus.customer'
    result: PARTIAL — emulator-5554 accepted the latest APK and reached the real Login surface after cold start; the screenshot shows the branded login UI and filtered logcat contains no customer FATAL EXCEPTION. No UAT credentials were injected in this run, so authenticated FLOW-09 screens are not claimed.

    command: adb install -r android-app-customer/app/build/outputs/apk/debug/app-debug.apk; adb shell am force-stop com.tembus.customer; adb shell am start -n com.tembus.customer/.ui.MainActivity; Start-Sleep -Seconds 3; adb exec-out screencap -p > C:\\Users\\yogis\\AppData\\Local\\Temp\\customer-flow09-final-login.png; adb logcat -d -t 300 | Select-String 'FATAL EXCEPTION|AndroidRuntime|com.tembus.customer'
    result: PARTIAL — latest post-color-correction APK installed and cold-started on emulator-5554; MainActivity displayed and final filtered logcat contained no customer FATAL EXCEPTION. This remains login-only smoke because no UAT credentials were injected.

    command: node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; git diff --check
    result: PASS — token sync/contrast, component adoption, Figma traceability, and whitespace checks passed after FLOW-09 updates; repository-wide CRLF normalization warnings remain advisory.

    command: graphify update .
    result: PASS — graph rebuilt with 32,816 nodes, 55,539 edges, and 1,921 communities after FLOW-09 shell changes; existing SQL parser/zero-node/edge-extraction warnings remain non-blocking.

## FLOW-06/FLOW-07 emergency parity follow-up

- Direct Figma inspection of `TEMBUS - Layanan Darurat Tambal Ban` confirmed the emergency shell uses `#F2FCF3`, a safety-first map/location surface, vehicle/issue selection, server-derived offer/price states, and a sticky orange technician CTA. `TambalBanHomeScreen.kt` now renders the exact pale-green shell, an honest map placeholder that never fabricates coordinates, and keeps the existing GPS/server quote/consent behavior.
- `ServiceCategoryScreen.kt` now uses the same `#F2FCF3` emergency canvas and governed top-bar colors for Towing; its route/map-unavailable state, vehicle/condition/photo/consent fields, and server handoff remain unchanged.
- `NearbyCouriersScreen.kt` now follows the Figma offer-selection hierarchy with separate Indonesian towing/montir titles, a server-backed offer summary card, count, price range, and explicit fee disclosure. It still renders only `NearbyCourier` records returned by the repository.
- `CourierDetailScreen.kt` now derives the profile identity from `serviceSubType` (`Montir Siaga` versus `Petugas Derek`) instead of labeling every provider as towing. Its shell uses the same emergency canvas, and its trust copy explicitly describes only the server-backed fields already present in `CourierDetail`; no Figma-only certification claim was invented.
- `ServiceBookingScreen.kt` now uses the exact `#F2FCF3` emergency/offer canvas for both roadside services and colors its top bar consistently, while preserving quote breakdown, consent, payment handoff, location permission, and destination contracts.

    command: Push-Location android-app-customer; .\\gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug; Pop-Location
    result: PASS — BUILD SUCCESSFUL in 2m50s after emergency map/shell, offer-selection, provider-profile identity, and booking-canvas parity updates; existing deprecation/unused-parameter warnings remain.

    command: node scripts/design/validate-tembus-design-tokens.mjs; node scripts/design/validate-tembus-component-adoption.mjs; node scripts/design/validate-tembus-figma-traceability.mjs; python scripts/tasks/validate_task_evidence.py; git diff --check
    result: PASS — token, component-adoption, Figma-traceability, evidence, and whitespace gates passed; repository-wide advisory warnings remain pre-existing.

    command: graphify update .
    result: PASS — graph rebuilt with 32,820 nodes, 55,545 edges, and 1,924 communities; existing zero-node/SQL parser/edge-field warnings remain non-blocking.

## Authenticated local runtime proof — current emergency build

- Account 2 UAT authentication was injected only into the Gradle process from the private vault; no credential, token, or password was written to source, evidence, screenshots, or logs. The rebuilt APK authenticated against the configured UAT API and rendered server-backed Home data.
- Home runtime was captured at `C:\Users\yogis\AppData\Local\Temp\customer-emergency-uat-account2-launch.png`: TEMBUS/location/search shell, wallet balance `Rp 0`, active order, 2x3 service grid, promo surface, and Indonesian five-item navigation were visible. This is authenticated local proof for the current APK, not staging or production proof.
- Tambal Ban runtime was opened from the authenticated Home and captured at `C:\Users\yogis\AppData\Local\Temp\customer-emergency-tambal-uat-location.png`. After location resolution, the screen visibly rendered `GPS akurat siap digunakan`, server-bound location copy, `GPS aktif`, coordinates from the device, `Akurat`, the Tambal Ban/Derek Towing selector, vehicle choices, and the guarded `Panggil Teknisi Sekarang` CTA. No sample location or fake quote was introduced.
- Towing booking runtime was opened from the emergency selector and captured at `C:\Users\yogis\AppData\Local\Temp\customer-emergency-towing-uat-latest.png`. The authenticated surface rendered the towing safety card, vehicle/condition fields, and guarded `Cek Harga Towing` action. This proves navigation/rendering only; no provider quote, booking mutation, or cross-app dispatch was claimed.
- Accessibility trees were dumped for the current Tambal Ban and Towing surfaces. The Tambal tree contained the emergency safety copy, pickup-location labels, resolved GPS state, service selector, vehicle heading, and technician CTA. Filtered logcat contained no customer `FATAL EXCEPTION`; the observed `AndroidRuntime` shutdown line was from the shell tooling, not a customer crash.
- The same authenticated session opened Activity, Account, Notification, and Messages through the persistent bottom navigation. Artifacts: `C:\Users\yogis\AppData\Local\Temp\customer-activity-uat-account2.png`, `C:\Users\yogis\AppData\Local\Temp\customer-account-uat-account2.png`, `C:\Users\yogis\AppData\Local\Temp\customer-notifications-uat-account2.png`, and `C:\Users\yogis\AppData\Local\Temp\customer-messages-uat-account2.png`.
- Activity showed the shared TEMBUS/location/search/wallet chrome, Indonesian activity title and filters, server-backed active Tambal Ban order identity, honest `Lokasi kurir belum tersedia`, orange tracking CTA, and chat/call affordances. Account showed the Figma hierarchy with `TEMBUS`/`Pilih area`, identity, server-backed wallet `Rp 0`, grouped account menu, and selected `Akun` navigation. Notification showed the green shell, category chips, unread count, server notification cards, and selected `Notifikasi`. Messages showed the order-scoped conversation entry and selected `Pesan` navigation.
- These additional captures are local authenticated rendering/navigation proof only. They do not prove courier chat delivery, provider callback/replay, or cross-app state mutation.
- The Home `Towing` tile was then opened separately, proving the actual Figma landing route rather than the `towing_motor` subtype booking route. Artifact: `C:\Users\yogis\AppData\Local\Temp\customer-towing-landing-uat-account2.png`. It rendered `Tembus Derek Towing Darurat`, safety protocol, route/drop-off selectors, honest map-unavailable state, vehicle options, condition heading, and the guarded orange-equivalent towing CTA. No location, quote, or dispatch was fabricated.
- An earlier `Baca` attempt was inconclusive because the remote session refreshed without changing the unread state. After rebuilding the current APK with the same process-only UAT setup, the authenticated `Baca` action completed: the unread badge and unread dots disappeared from the notification list, no inline mutation error was shown, and filtered logcat contained no customer fatal. Artifact: `C:\Users\yogis\AppData\Local\Temp\customer-notification-patch-after.png`. The emulator was returned to authenticated Home at `C:\Users\yogis\AppData\Local\Temp\customer-home-post-notification-readall.png`.

    command: ./gradlew.bat --no-daemon :app:assembleDebug; adb install -r app/build/outputs/apk/debug/app-debug.apk; adb shell am force-stop com.tembus.customer; adb shell am start -W -n com.tembus.customer/.ui.MainActivity; adb shell uiautomator dump; adb exec-out screencap -p; adb logcat -d
    result: PASS — Account 2 authenticated Home and the emergency routes rendered on `emulator-5554`; screenshot/AX artifacts are listed above. Provider mutation, payment callback, staging, and production behavior remain NOT_RUN.

## Notification mutation follow-up

- `NotificationCenterViewModel.markAllRead()` now clears stale UI errors on success. `NotificationCenterScreen` renders an inline `Perubahan belum tersimpan` card with retry when a read-all mutation fails while old notifications remain visible; the previous implementation silently retained the list and hid the error.
- UAT APK rebuild with process-only Account 2 credential and remote API host: `./gradlew.bat --no-daemon :app:assembleDebug`: PASS in 2m42s. Credential values were not printed or persisted.
- Authenticated runtime: `Baca` action removed the unread count and unread dots from the server-backed notification list on `emulator-5554`; `customer-notification-patch-after.png` is the visual artifact and `customer-notification-patch-after.xml` is the AX artifact. No customer `FATAL EXCEPTION` was observed.

## Home promo parity follow-up

- `FigmaPromoBanner` now includes the missing Figma `Rp 8.000` price line between the two-line headline and supporting copy. A CMS-provided `price` or `starting_price` wins; the fallback is presentation copy only and does not alter server pricing or checkout totals.
- Customer Android `:app:testDebugUnitTest :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 3m 16s`, 55 actionable tasks, 15 executed and 40 up-to-date. Existing Kotlin deprecation/unused-parameter warnings remain advisory.
- The rebuilt APK was installed on `emulator-5554` and launched using the actual component `com.tembus.customer/.ui.MainActivity`. Authenticated Home AX output contained `SALDO TEMBUS-PAY`, `Layanan Utama`, `Kirim Paket`, `Kirim Cepat Se-Jabodetabek mulai`, `Rp 8.000`, and `Beranda`. Visual artifact: `C:\Users\yogis\AppData\Local\Temp\customer-home-promo-price.png`; AX artifact: `C:\Users\yogis\AppData\Local\Temp\customer-home-promo.xml`.
- `graphify update .`: PASS — graph rebuilt with 32,822 nodes, 55,547 edges, and 1,908 communities; existing zero-node, SQL-parser, and edge-relation warnings remain non-blocking.
- This runtime check proves the Home visual patch on the local authenticated emulator only. It does not prove provider, cross-app, offline, or account-deletion requirements.

## FLOW-01 process recreation and offline recovery follow-up

- Cold-start/process-recreation smoke: `adb shell am force-stop com.tembus.customer; adb shell am start -W -n com.tembus.customer/.ui.MainActivity` returned `LaunchState: COLD` and authenticated Home with `TEMBUS`, `Layanan Utama`, `Rp 8.000`, and `Beranda`; no customer fatal was present in the filtered logcat. Artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow01-coldstart-home.png` and `customer-flow01-home.xml`.
- Active-tab transition: tapping `Aktivitas` rendered the Figma Activity shell with `Aktivitas` selected, active Tambal Ban order identity, honest `Lokasi kurir belum tersedia`, tracking, chat, and call actions. Artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow01-activity.png` and `customer-flow01-activity.xml`.
- Authenticated offline recovery: Wi-Fi and mobile data were disabled with emulator service controls before force-stop/start. The runtime rendered the cached Home/order surface with `Kamu sedang offline`, `Data yang tersimpan tetap tersedia`, `Coba lagi`, and `Saldo belum tersedia`; artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow01-offline-recovery.png` and `customer-flow01-offline-recovery.xml`.
- After restoring Wi-Fi/data and restarting the same installed APK, the AX tree returned `Rp 0`, `Layanan Utama`, `Kirim Cepat Se-Jabodetabek mulai`, and `Beranda`, with no offline banner. Artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow01-network-restored.png` and `customer-flow01-network-restored.xml`.
- This closes the local FLOW-01 offline/process-recreation proof. It does not prove cross-app mutation, real provider callback/replay, or account deletion.

## FLOW-02 roadside tracking contract correction

- The first authenticated Activity → `Lacak Pesanan` runtime exposed a real parity defect: a Tambal Ban order was rendered with parcel vocabulary (`Kurir menerima order`, `Timeline pengiriman`, `Rincian paket`, and `POD diterima`). This was captured before the correction at `C:\\Users\\yogis\\AppData\\Local\\Temp\\customer-flow02-tracking-from-activity.png` and is retained as the regression baseline.
- Root cause was verified across the stack. PostgreSQL held the order as `service_category = tambal_ban`; the admin tracking-detail query already selects `o.service_category` and `service_sub_type` (`backend/admin-service/src/controllers/order/customerOrderTracking.controller.ts`), but the local `tembus-admin` container was running a stale 18 September image. The service was rebuilt from the current worktree and recreated with `docker compose up -d admin-service`.
- Customer tracking mapping now prioritizes canonical `service_category` before subtype/model, carries that category through stage/timeline/proof rendering, and suppresses the generic package section for roadside/towing orders. Regression tests cover category-only Tambal Ban payloads and technician vocabulary.
- Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` passed (`BUILD SUCCESSFUL`, 55 actionable tasks; 13 executed and 42 up-to-date), the APK was installed on `emulator-5554`, and the Activity active-order `Lacak Pesanan` action was replayed.
- Fixed authenticated runtime artifact: `C:\\Users\\yogis\\AppData\\Local\\Temp\\customer-flow02-tracking-admin-rebuilt.png` with AX tree `customer-flow02-tracking-admin-rebuilt.xml`. The rendered surface contains `Timeline layanan`, `Teknisi menerima order`, `Teknisi tiba dan verifikasi lokasi`, `Perbaikan ban sedang dikerjakan`, and `Layanan selesai`; `Rincian paket`, `POD diterima`, and parcel timeline labels are absent.
- This proves local customer ↔ current admin tracking-detail vocabulary alignment for the tested Tambal Ban order. It does not prove live mitra state mutation, provider callback/replay, or production/staging deployment behavior.
- Backend status-label follow-up: `customerOrderStatusLabel` now accepts canonical `service_category` in addition to subtype, so customer list/detail read models no longer fall back to parcel `Kurir ...` labels when a roadside subtype is blank. `statusLabels.test.ts` covers category-only Tambal Ban, towing subtype, and parcel/food regressions.
- After rebuilding/restarting `tembus-admin` again, the authenticated tracking header and timeline both rendered `Teknisi menerima order`; `Kurir menerima order`, `Kurir menuju titik pickup`, `Rincian paket`, and `POD diterima` were absent from the AX tree. The Activity → tracking → Back replay returned to Activity with `Aktivitas` and `Lacak Pesanan` visible. Artifacts: `C:\\Users\\yogis\\AppData\\Local\\Temp\\customer-flow02-tracking-label-fixed.png`, `customer-flow02-tracking-label-fixed.xml`, and `customer-flow02-backstack-label.xml`.

## Cross-service verification additions

- `go test -v -timeout 5m ./...` in `backend/auth-service`: PASS.
- `go test -v -timeout 5m ./...` in `backend/routing-service`: PASS.
- `go test -v -timeout 5m ./...` in `backend/order-service`: PASS.
- `npm test` in `backend/admin-service`: PASS — 136 test suites and 631 tests passed.
- `go vet ./...` in auth, routing, and order services: PASS.
- `npm run build` in `backend/admin-service`: PASS (`tsc`). The repository has no `npm run lint` script for admin-service, so that target is NOT_AVAILABLE; no lint result is fabricated.

- `docker compose build admin-service`: PASS — rebuilt `tembus-admin-service` from the current worktree after identifying the local container as stale.
- `docker compose up -d admin-service`: PASS — recreated and started `tembus-admin`; its tracking-detail query now serves the canonical roadside category to the customer gateway.
- `npm test -- --runInBand src/controllers/order/customerOrderConnectionOwnership.test.ts` in `backend/admin-service`: PASS — 1 suite and 1 test passed.
- `./gradlew.bat --no-daemon :app:testDebugUnitTest` in `android-app-customer`: PASS — BUILD SUCCESSFUL in 27s; 36 actionable tasks, 1 executed and 35 up-to-date after roadside tracking vocabulary correction.
- `adb -s emulator-5554 shell input tap ...` Activity → `Lacak Pesanan`, followed by `uiautomator dump` and `screencap`: PASS — `customer-flow02-tracking-admin-rebuilt.png` and `customer-flow02-tracking-admin-rebuilt.xml` show technician/timeline copy and no parcel section.
- `graphify update .`: PASS — graph rebuilt with 32,826 nodes, 55,552 edges, and 1,939 communities after the tracking-category correction; existing zero-node, SQL-parser, and edge-relation warnings remain non-blocking.
- `graphify update .`: PASS — graph rebuilt with 32,827 nodes, 55,554 edges, and 1,907 communities after the category-aware backend status-label correction; existing zero-node, SQL-parser, and edge-relation warnings remain non-blocking.
- `adb -s emulator-5554 shell wm size 1365x3120` with Home → Parcel and Home → Activity navigation, followed by `wm size reset`: PASS — compact parcel form and Activity shell kept readable bounds; Activity exposed `Lacak Pesanan`, `Lokasi kurir belum tersedia`, and the five-item navigation. Artifacts: `customer-compact-parcel.png`, `customer-compact-parcel.xml`, `customer-compact-activity.png`, and `customer-compact-activity.xml`.
- Repeated compact Home bounds lookup and Home → Food navigation at the actual `Food` node bounds, followed by `wm size reset`: PASS — compact Food shell exposed `TEMBUS`, search, category/filter chips, honest merchant empty state, and no clipped text. Artifacts: `customer-compact-food-correct.png` and `customer-compact-food-correct.xml`.
- Activity → `Lacak Pesanan` → Android Back: PASS — back-stack returned to Activity with `Aktivitas` and `Lacak Pesanan` visible; artifact `customer-flow02-backstack-label.xml`.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL — implementation, build/test evidence, and authenticated local emulator proof exist; external provider/cross-app mutation and account-deletion gates remain unavailable.
- `REALITY-2026-011`: PASS for this checkpoint — no completion claim is made; wallet sample values were removed instead of being treated as runtime truth, and unproven flow/device work is explicit.

## Unproven / Remaining

`UIUX-2026-008` is BLOCKED after locally actionable work was exhausted. Authenticated Home visual parity, process recreation/offline recovery, notification mutation, unknown-order deep-link safety, roadside tracking contract alignment, account edge states, and compact core-shell readability are proven locally. Resume when the authorized staging/provider environment, real payment callback/replay capability, node-level Figma references where required, and an approved account-deletion contract are available.

## FLOW-09 authenticated account edge-state proof

- Notification Center navigation from authenticated Home rendered the Figma-aligned `Notifikasi` shell with `Semua` and `Baca` filters; artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow08-notifications-final.png` with AX tree `customer-flow08-notifications-final.xml`.
- Messages navigation rendered the Figma-aligned `Pesan` shell and selected bottom-navigation state; artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow08-messages-final.png` with AX tree `customer-flow08-messages-final.xml`.
- Account lower-section navigation exposed the server-backed `Bahasa`, `Keamanan`, `Privasi & Ketentuan`, and `Pusat Bantuan` entries. Security rendered `Keamanan Akun`, encrypted-session status, local PIN/biometric state, and server-verified account/transaction PIN copy; artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow09-security.png` with AX tree `customer-flow09-security.xml`.
- Privacy rendered server-approved `Kebijakan Privasi` and `Syarat & Ketentuan` entries with locale/version metadata and `Buka` actions; artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow09-privacy.png` with AX tree `customer-flow09-privacy.xml`.
- Support rendered the official operational copy, `bantuan@tembus.com`, and the configured WhatsApp CS number with `Tutup`/`Chat WhatsApp CS`; artifact: `C:\Users\yogis\AppData\Local\Temp\customer-flow09-support.png` with AX tree `customer-flow09-support.xml`.
- This proves authenticated local rendering/navigation for the account edge states only. It does not prove external document opening, WhatsApp delivery, account deletion, or any provider/cross-app mutation.

## Final authenticated Home replay

- Debug APK was rebuilt with Account 2 UAT values injected only into the Gradle process from the private vault; values were not printed, committed, or persisted. `./gradlew.bat --no-daemon :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 2m 42s`, 46 actionable tasks, 9 executed and 37 up-to-date.
- The APK was installed on `emulator-5554`, launched through `com.tembus.customer/.ui.MainActivity`, and allowed to settle before capture. Final AX proof contains `TEMBUS`, `SALDO TEMBUS-PAY`, `Layanan Utama`, `Kirim Paket`, `Tambal Ban`, `Towing`, `Kirim Cepat Se-Jabodetabek mulai`, `Rp 8.000`, and `Beranda`. Artifacts: `C:\Users\yogis\AppData\Local\Temp\customer-final-home-authenticated.png` and `customer-final-home-authenticated.xml`.
- Filtered logcat contained `NO_CUSTOMER_FATAL_EXCEPTION`. This is authenticated local/UAT proof only; it is not staging or production proof.

## Compact authenticated shell matrix

- With emulator width set to `1365x3120` (about 390dp at the configured density), authenticated Home remained readable; AX bounds included `TEMBUS`, `Layanan Utama`, and the five-item bottom navigation. Artifact: `C:\Users\yogis\AppData\Local\Temp\customer-compact-home-final.png` and `customer-compact-home-final.xml`.
- Compact Account remained readable with `TEMBUS`, `Pilih area`, wallet `Rp 0`, grouped account sections, and bottom navigation. Artifact: `customer-compact-account-final.png` and `customer-compact-account-final.xml`.
- Compact Notification remained readable with category tabs, server-backed notification cards, `Baca`, and bottom navigation. Artifact: `customer-compact-notifications-final.png` and `customer-compact-notifications-final.xml`.
- Compact Messages remained readable with the order-scoped conversation card and five-item navigation. Artifact: `customer-compact-messages-final.png` and `customer-compact-messages-final.xml`.
- Emulator width was restored with `adb -s emulator-5554 shell wm size reset`. These are layout/readability proofs; they do not add proof for provider callbacks, external document opening, WhatsApp delivery, or cross-app mutation.

## Home recommendation empty-state parity

- Runtime comparison against the Figma Home frame found that an empty backend merchant result removed the entire `Kuliner Terdekat` hierarchy, leaving a visible parity gap below the promo card. The Home component now preserves the section heading and renders a Figma-style, server-honest empty card that opens Food discovery; it never invents merchant names, photos, ratings, or prices.
- The local database currently has approved merchants but none are open, so the authenticated runtime correctly showed `Kuliner Terdekat di Kecamatan Tanah Abang`, `Belum ada merchant di sekitar lokasi ini`, and `Coba ubah kategori atau periksa kembali lokasi perangkat.` after scrolling below the promo. Artifacts: `C:\Users\yogis\AppData\Local\Temp\customer-home-after-promo-scroll.png` and `customer-home-after-promo-scroll.xml`.
- Customer Android verification after the patch: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 3m 26s`, 55 actionable tasks, 16 executed and 39 up-to-date. The rebuilt APK was installed on `emulator-5554` and the empty-state strings were present in the accessibility tree.
- `graphify update .`: PASS — 32,830 nodes, 55,557 edges, and 1,921 communities; existing zero-node, SQL-parser, and edge-relation warnings remain non-blocking.

## Home quick-repeat and promo CTA parity

- A second runtime comparison found two additional state/layout gaps: `Pesan Lagi Cepat` disappeared when the customer had no completed order, and the fixed-height promo card clipped `Klaim Promo` below the card edge.
- Home now renders a server-honest `Pesan Lagi Cepat` empty state (`Belum ada pesanan untuk diulang`) when no terminal order exists, preserving the Figma hierarchy without inventing an order. The promo uses a `176.dp` minimum height instead of a hard fixed height, so localized/CMS copy can expand while keeping the CTA fully visible.
- Authenticated runtime after the patch showed `Klaim Promo`, `Pesan Lagi Cepat`, `Belum ada pesanan untuk diulang`, `Kuliner Terdekat di Kecamatan Tanah Abang`, and the merchant-empty state in one scrolled Home capture. Artifacts: `C:\Users\yogis\AppData\Local\Temp\customer-home-promo-fixed.png` and `customer-home-promo-fixed.xml`.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 3m 02s`, 55 actionable tasks, 12 executed and 43 up-to-date. `graphify update .`: PASS — 32,832 nodes, 55,561 edges, and 1,922 communities; existing graph parser/edge warnings remain non-blocking.

## FLOW-03 booking resume parity

- `BookingViewModel` already persists the customer-entered parcel draft through `SavedStateHandle`, while quote/route price breakdown remains intentionally server-recomputed. The UI step indicator was not persisted, so a process recreation from the review/detail step silently reopened the first panel even when the draft inputs were restored.
- `BookingScreen` now stores `currentStep` with `rememberSaveable`. This keeps the active Figma booking panel aligned with the restored draft after configuration change/process recreation without persisting price, payment, or dispatch truth on the client.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 3m 37s`, 55 actionable tasks, 13 executed and 42 up-to-date. Existing deprecation/unused-parameter warnings remain advisory.
- `graphify update .`: PASS — graph rebuilt with 32,834 nodes, 55,563 edges, and 1,910 communities after the booking-resume correction; existing zero-node, SQL-parser, and edge-relation warnings remain non-blocking.
- This closes the local step-resume defect only. Location denial, route/provider failure, unavailable service, duplicate CTA, and authenticated scheduled-pickup/courier handoff evidence remain part of the open FLOW-03 exit gate.

## Verification gates after emergency entry correction

- `graphify update .`: PASS — graph rebuilt with 32,841 nodes, 55,579 edges, and 1,921 communities. Existing zero-node, missing SQL parser, and edge-relation warnings remain tooling warnings; the graph output was updated.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS.
- `node scripts/design/validate-tembus-component-adoption.mjs`: PASS.
- `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS.
- `py scripts/tasks/validate_task_evidence.py`: PASS — repository-wide advisory warnings remain for older evidence files and do not invalidate this checkpoint.
- `git diff --check`: PASS — only existing CRLF normalization advisories were emitted.

## FLOW-06/07 roadside offer, mitra profile, and tracking parity checkpoint

- Rechecked the Figma roadside frames `TEMBUS - Pilih Petugas Derek` (`node-id=2-855`), `TEMBUS - Profil Montir & Sertifikasi` (`node-id=2-2`), `TEMBUS - Tracking Derek` (`node-id=2-617`), and `TEMBUS - Derek Towing: Konfirmasi & Kunci Tarif` (`node-id=2-6932`). The customer app now follows the same visual hierarchy for the roadside slice: green selection hero, server-backed offers, provider profile, live status hero, progress, provider card, route, and payment context.
- Extended the existing availability contract instead of adding a parallel provider source. `courier_profiles`/`users.photo_url`, `courier_service_prices`, `courier_training_completions`, and verified `courier_documents` now feed the profile and offer response. Existing database columns were verified locally; no fake courier, review, certification, order, or price seed was added.
- Nearby offer cards now show the DB/API vehicle identity, honest rating count state, server price/range, ETA/distance, and availability. Courier profile now uses the DB photo URL with initials fallback, verified/tier state, real service price, real performance metrics, and explicit empty certification/review states when the tables have no records.
- Service tracking now maps `order_number`, courier identity/photo/vehicle/plate, pickup/drop-off addresses, route distance/duration, invoice amount, payment status/method, and ETA from `OrderTrackingDetail`. Missing route/payment data is shown as unavailable; Figma sample addresses, names, prices, and tracking values are not embedded.
- Backend verification: `go test ./...` in `backend/order-service`: PASS. Local schema inspection confirmed `courier_documents.deleted_at` and `courier_training_completions.courier_profile_id` used by the read projection.
- Customer Android verification: `./gradlew.bat --no-daemon :app:compileDebugKotlin`: PASS (`BUILD SUCCESSFUL` in 2m45s); `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug`: PASS (`BUILD SUCCESSFUL` in 1m35s). Existing deprecation/unused-parameter warnings remain advisory.
- APK installed successfully on `emulator-5554`; launcher replay reached the existing Figma-aligned Towing emergency entry without a customer crash. Runtime artifacts: `E:\antigraviti google\SUDAH DEPLOY\LANCAR\android-app-customer\runtime-roadside-towing-entry.png`, `runtime-roadside-towing-entry.xml`, and `runtime-roadside-home.png`. A complete order tracking replay was not fabricated because the local database contains no roadside order; the new tracking screen remains runtime-unproven for an active order.
- `graphify update .`: PASS — graph rebuilt with `32,944 nodes`, `55,814 edges`, and `1,911 communities`; existing zero-node, SQL parser, and edge-relation warnings remain tooling warnings.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS; `node scripts/design/validate-tembus-component-adoption.mjs`: PASS; `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS; `py scripts/tasks/validate_task_evidence.py`: PASS; `git diff --check`: PASS with existing CRLF normalization advisories.
- This checkpoint remains PARTIAL. Remaining proof includes a real authenticated order through offer → profile → payment → dispatch → tracking, mitra/merchant cross-app mutation, provider callback/replay, and staging runtime validation. No production readiness or external provider success is claimed.
- Flow wiring follow-up: `NearbyCouriers` now navigates to `CourierDetail` with the selected subtype and customer coordinates; the profile CTA continues into the existing server-backed `ServiceBooking` route. `:app:compileDebugKotlin`: PASS (`BUILD SUCCESSFUL` in 3m12s).
- Final artifact verification after the route change: `./gradlew.bat --no-daemon :app:assembleDebug`: PASS (`BUILD SUCCESSFUL` in 42s); APK reinstalled on `emulator-5554`, `MainActivity` resumed, and filtered logcat returned `NO_CUSTOMER_FATAL_EXCEPTION`.

## Home promo hardcode removal and DB/CMS source-of-truth correction

- Focused hardcode audit found the Figma campaign fallback in the Home hero: title, description, badge, CTA, and discount presentation were embedded in the Android component. The fallback is removed from both the current Figma Home card and the legacy hero component; no sample campaign is rendered when the server has no eligible campaign.
- Added `database/migrations/20260922000001_customer_home_promo_campaign.sql`. It creates the idempotent admin-editable `TEMBUS-HOME-RAMADAN-2026` campaign in canonical `promo_campaigns` with active status, shipping discount economics, service targets, budget/redemption limits, schedule, moderation state, badge, and CTA copy. The local Up SQL was applied and rerun to verify `INSERT 0 0` on conflict with exactly one row remaining.
- `listCustomerEligiblePromos` now exposes `notification_copy`; `CustomerEligiblePromo` carries it to Android. Home selects `featuredPromo` from the eligible-promos response. Campaign name/description/copy and the monetary discount are rendered from DB/API; CMS hero properties can override presentation copy but cannot override transaction economics. Promo card taps open the existing Promo Center when no CMS deep link is configured.
- Rebuilt `tembus-admin` after the controller change. `docker compose build admin-service`: PASS; `docker compose up -d --no-deps admin-service`: PASS. The compose migration image could not be pulled (`registry denied`), so local verification used the checked-in migration's `--goose Up` SQL directly against `tembus-db`; the repository migration remains the deployment source of truth.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 3m56s`; existing Kotlin deprecation warnings remain advisory. APK installed on `emulator-5554`.
- Runtime Home proof: `C:\Users\yogis\AppData\Local\Temp\customer-promo-db-home-final.png`. The card visibly renders `SPESIAL RAMADAN`, `Kirim Cepat Se-Jabodetabek`, `Diskon Rp 8.000`, server description, and `Klaim Promo`; these values match the seeded DB row and the app log shows `GET /api/v1/customer/promos/eligible ... 200 OK`.
- Runtime Activity proof: `C:\Users\yogis\AppData\Local\Temp\customer-promo-db-activity-final.png`, AX tree `customer-promo-db-activity-final.xml`. XML assertions: `containsSaldo=False`, `containsSearch=False`, `containsAktivitas=True`, `containsPesananAktif=True`, `containsFilterKategori=True`. No customer `FATAL EXCEPTION` appeared in filtered logcat.
- Final gates: `graphify update .` PASS — `32,913 nodes`, `55,735 edges`, `1,919 communities`; design-token, component-adoption, Figma-traceability, task-evidence, and `git diff --check` validators PASS. Known graph parser/zero-node/edge and repository advisory warnings remain non-blocking.
- Status remains `PARTIAL`: FLOW-02..FLOW-09 staging/provider/cross-app/release gates are still unproven. No production callback, merchant/mitra mutation, payment success, or external provider behavior was fabricated.

## Latest Activity and Home data-parity correction

- Direct Figma inspection rechecked the `TEMBUS - Aktivitas & Riwayat` frame. Its hierarchy is location/header, `Aktivitas`, `Lacak dan kelola pesananmu`, status filters, `PESANAN AKTIF`, `Update realtime`, active-order detail, `RIWAYAT PESANAN`, and `Filter Kategori`. It does not contain the Home search bar or the TEMBUS-Pay saldo card.
- `FigmaHomeHeader` now exposes independent `showSearchBar` and `showWalletCard` flags. `OrderHistoryScreen` uses both flags as `false`, and `OrderHistoryViewModel` no longer fetches profile wallet balance for Activity. This removes the prior saldo leakage and aligns Activity with the Figma frame while keeping Home wallet data profile-backed.
- Home food recommendation data is now enriched server-side from the canonical `merchant_menu_items` catalog. `AttachFoodMerchantMenuPreview` selects up to two available menu items per merchant, includes canonical menu-item image fallback from `merchant_menu_item_images`, and returns menu name, price, category, description, availability, inventory, and enforcement state. The Android strip renders those response fields and merchant distance; it does not embed merchant names, prices, or image URLs.
- Local database inspection after the query path was wired: `7` menu items total, `7` active/available, and `7` active/available rows with a photo URL. The local API replay returned `GET /api/v1/food/merchants ... 200 OK` for the emulator location `lat=-6.1989983&lng=106.8149983`.
- Fixed two SQL projection defects found during real replay: the enforcement function result now has the `enforcement_active` alias, and the photo `COALESCE` expression now has the `foto` alias. These were verified by rebuilding/restarting `tembus-order` and replaying the authenticated customer app.
- Latest Home lower-scroll artifact: `C:\Users\yogis\AppData\Local\Temp\customer-final-home-latest-food-2.png`. It shows the Figma-like nearby-food rail with DB-backed food photo, merchant, menu title, category, rating state, `Rp15.000`/`Rp8.000`, and `1.7 km`/`2.0 km` distance badges.
- Latest Activity artifact: `C:\Users\yogis\AppData\Local\Temp\customer-final-activity.png`; accessibility tree: `C:\Users\yogis\AppData\Local\Temp\customer-final-activity.xml`. The AX tree contains `Aktivitas`, `PESANAN AKTIF`, and `Filter Kategori`, and does not contain `Saldo` or the Home search placeholder.
- Verification after the correction: `go test -v -timeout 5m ./...` PASS for order-service; integration tests that require `TEMBUS_*_TEST_DATABASE_URL` were explicitly skipped by their own guards. `./gradlew.bat --no-daemon :app:testDebugUnitTest` PASS and `./gradlew.bat --no-daemon :app:assembleDebug` PASS; the latest APK was installed on `emulator-5554`.
- Design/runtime gates: `graphify update .` completed with `32,910` nodes, `55,729` edges, and `1,909` communities; known zero-node/SQL-parser/edge-relation warnings remain tooling warnings. Design token, component adoption, Figma traceability, and `git diff --check` validators PASS. Filtered emulator logcat had no customer `FATAL EXCEPTION`.
- This closes the reported Activity saldo mismatch and Home missing-photo/price rendering for local authenticated runtime. The task remains `PARTIAL` because the evidence contract still requires compact FLOW-02 through FLOW-09 route/exit proof and authorized staging/provider/cross-app mutation proof; those are not claimed by this checkpoint.

## FLOW-05 payment-method overview parity follow-up

- Direct Figma inspection selected `TEMBUS - Metode Pembayaran & Dompet` (`node-id=2-6624`, fixed `410.59px` width, Hug `1,755.92px`, neutral `#F7F8F6` canvas). The customer Account row previously opened `WalletTopUpScreen`, so the Figma method-management hierarchy was not reachable.
- Added `Screen.PaymentMethods` and `PaymentMethodsScreen`. Profile now separates the wallet-card Top Up action from the `Metode Pembayaran` menu route. The new screen follows the Figma hierarchy: server-backed TEMBUS-Pay balance hero, primary method, debit/credit cards, connected digital wallets, instant checkout methods, security/authorization, and protection disclosure.
- No payment method, masked card, wallet connection, or provider status is fabricated. The existing customer backend exposes an amount-specific payment catalog but no saved-card/digital-wallet management contract; those rows therefore render explicit `Belum terhubung`/`Dipilih saat checkout` states until an owned backend contract exists.
- Serial Android verification after the change: `./gradlew.bat --no-daemon :app:compileDebugKotlin` PASS — `BUILD SUCCESSFUL in 2m54s`; `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 2m01s`. Existing deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK installed on `emulator-5554`; authenticated Home → Akun → `Metode Pembayaran` reached the new route. Runtime artifacts: `C:\Users\yogis\AppData\Local\Temp\payment-methods-parity.png`, `payment-methods-parity-lower.png`, and accessibility trees `payment-methods-parity.xml`, `payment-methods-parity-lower.xml`. The server-backed UAT wallet currently renders `Rp 0`.
- Filtered logcat after the navigation and scroll replay returned `NO_FATAL_EXCEPTION_MATCHES`.
- This proves local authenticated visual/navigation parity and honest empty states only. Saved-method mutations, payment-provider callback/replay, and customer↔merchant↔mitra staging behavior remain release follow-ups. The task remains `PARTIAL` while FLOW-02 through FLOW-09 compact/exit gates continue.

## Repository regression gates after current parity checkpoint

- `make test` and `make lint`: `NOT_RUN` because `make` is not installed in this Windows environment. Equivalent available commands were attempted directly.
- Manual backend tests: `go test -v -timeout 5m ./...` passed for `backend/auth-service`, `backend/routing-service`, and `backend/order-service`; `npm test` passed for `backend/admin-service`.
- Manual backend/static checks: `go vet ./...` passed for auth, routing, and order services. `frontend` `npm run lint` exited successfully with existing repository warnings only (`512 warnings`, `0 errors`). `backend/admin-service` has no `lint` npm script (`NOT_RUN`), so no result is claimed for that missing script.

## FLOW-04 Food Merchant Detail canvas parity follow-up

- Direct Figma inspection selected `TEMBUS Food - Detail Merchant` (`node-id=2-7979`, fixed `390px` width, Hug `1,792px`, `#FFFFFF` surfaces over a `#F2FCF3` canvas). The reference hierarchy is hero/media, merchant identity and operating facts, menu category/list, and a sticky cart summary.
- `MerchantDetailScreen` now uses the shared `CustomerCanvas` token for its outer canvas instead of the generic theme background. This corrects the visible Figma mismatch while preserving server-backed merchant/menu media, availability, cart quantity, price, and checkout actions.
- Serial Android verification after the change: `./gradlew.bat --no-daemon :app:compileDebugKotlin` PASS (`BUILD SUCCESSFUL` in 2m54s); `./gradlew.bat --no-daemon :app:testDebugUnitTest` PASS (`BUILD SUCCESSFUL` in 27s; tasks up-to-date); `./gradlew.bat --no-daemon :app:assembleDebug` PASS (`BUILD SUCCESSFUL` in 32s). Existing deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK installed successfully on `emulator-5554`. Authenticated replay Home → Food → merchant card opened Merchant Detail with real local UAT merchant/media/menu data. Artifact: `C:\Users\yogis\AppData\Local\Temp\food-merchant-canvas-final.png`; AX artifact: `food-merchant-canvas-final.xml`. The capture shows the pale-green Figma canvas, hero image, merchant identity/open state, menu item, and sticky cart summary.
- Filtered emulator logcat after the replay returned `NO_FATAL_EXCEPTION_MATCHES`.
- This proves local authenticated Merchant Detail rendering and navigation only; merchant stock/price mutations, merchant accept/prepare/reject, mitra dispatch, payment callback/replay, and staging cross-app behavior remain unproven external/runtime gates.
- Final checkpoint gates: `graphify update .` PASS — `32,896 nodes`, `55,696 edges`, `1,916 communities`; `node scripts/design/validate-tembus-design-tokens.mjs` PASS; `node scripts/design/validate-tembus-component-adoption.mjs` PASS; `node scripts/design/validate-tembus-figma-traceability.mjs` PASS; `py scripts/tasks/validate_task_evidence.py` PASS; `git diff --check` PASS with existing CRLF normalization advisories.

## FLOW-04 Food discovery and Merchant Detail visual parity follow-up

- Direct Figma inspection selected `TEMBUS Food - Eksplorasi Kuliner & ...` (`node-id=2-5035`, fixed `390px` width, Hug `1,873px`, `#FFFFFF` / `#F7F8F6`). The target hierarchy is TEMBUS Food/location/search, promo, 2x3 food category grid, filter chips, nearby merchant rail, trust surface, and merchant list.
- `FoodHomeScreen` now preserves the promo hierarchy when the CMS manifest contains an incomplete promo item: it falls back to Figma-like `Pesta Diskon Kuliner Hangat` copy without inventing a voucher, price, or eligibility. The extra `FOOD` label was removed from the header so branding matches the Figma composition.
- `TembusCommerceImage` now renders a visible neutral placeholder during media loading/failure via `SubcomposeAsyncImage`; Merchant Detail no longer presents blank hero/menu media when merchant media is unavailable.
- Customer Android verification: `./gradlew.bat --no-daemon :app:compileDebugKotlin` PASS — `BUILD SUCCESSFUL in 2m29s`; `./gradlew.bat --no-daemon :app:testDebugUnitTest` PASS — `BUILD SUCCESSFUL in 1m07s`; `./gradlew.bat --no-daemon :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 57s`. Existing deprecation/unused-parameter warnings remain advisory.
- The latest APK was installed and replayed on `emulator-5554`. Food runtime artifact: `C:\Users\yogis\AppData\Local\Temp\food-home-latest-after-promo.png` with matching `food-home-latest-after-promo.xml`; Merchant Detail artifact: `C:\Users\yogis\AppData\Local\Temp\food-merchant-placeholder-latest.png` with matching `food-merchant-placeholder-latest.xml`. The accessibility/runtime captures show the promo, category/filter/merchant hierarchy, merchant identity/open state, menu item, visible media placeholders, and cart/checkout bar.
- Filtered logcat after Food and Merchant Detail replay returned `NO_FATAL_EXCEPTION_MATCHES`.
- This proves local authenticated/UAT visual/state rendering only; real merchant accept/prepare/reject, courier dispatch, payment callback/replay, and staging cross-app mutation remain unproven release gates.
- `graphify update .`: PASS — graph rebuilt with `32,891 nodes`, `55,691 edges`, and `1,916 communities`; existing zero-node, missing SQL-parser, and edge-relation warnings remain tooling warnings.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS; `node scripts/design/validate-tembus-component-adoption.mjs`: PASS; `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS; `py scripts/tasks/validate_task_evidence.py`: PASS; `git diff --check`: PASS with existing CRLF normalization advisories.

## FLOW-00 Home direct parity replay after Food changes

- Reinstalled the latest customer APK and replayed the authenticated Home on `emulator-5554`. Artifact: `C:\Users\yogis\AppData\Local\Temp\home-current-after-food.png`; AX artifact: `home-current-after-food.xml`.
- The runtime composition now visibly preserves the Figma Home order: TEMBUS/area header, location row, search shell, TEMBUS-Pay card, authoritative active-order card, 2x3 service grid, green/orange campaign card, and orange-selected five-item bottom navigation. This replay used UAT-backed order/address/wallet data and did not inject Figma sample values.
- The capture is a local viewport checkpoint; lower scroll content, authenticated staging mutation, and provider/release gates remain outside this proof.

## FLOW-04 Food checkout canvas parity follow-up

- Direct Figma inspection selected `TEMBUS Food - Checkout Pesanan`; its properties show a fixed `422px` checkout frame using `#FFFFFF` and `#F2FCF3` surfaces. `FoodCheckoutScreen` was still using the neutral Home/Payment canvas `#F7F8F6`.
- Corrected the Food checkout scaffold and scroll surface to the shared `CustomerCanvas` token (`#F2FCF3`). Checkout behavior was unchanged: server quote, expiry, minimum order, voucher, saved/pinned address, location permission, scheduling, receiver notes, and guarded submit remain authoritative.
- Serial verification after the change: `./gradlew.bat --no-daemon :app:compileDebugKotlin` PASS — `BUILD SUCCESSFUL in 23s`; `./gradlew.bat --no-daemon :app:testDebugUnitTest` PASS — `BUILD SUCCESSFUL in 41s`; latest `:app:assembleDebug` PASS — `BUILD SUCCESSFUL in 3m16s` (parallel-cache race was not used as evidence for the final compile/test result).
- Latest APK was installed and replayed on `emulator-5554`: Home → Food → Cart → `Lanjut ke Pembayaran` reached the checkout screen. Artifact: `C:\Users\yogis\AppData\Local\Temp\food-checkout-canvas-final.png`; AX artifact: `food-checkout-canvas-final.xml`. Runtime shows the pale-green checkout canvas, merchant/item summary, immediate/scheduled order selector, voucher field, delivery-address/pin guard, server-total footer, and guarded `Pesan Sekarang` CTA.
- Filtered logcat after the replay returned `NO_FATAL_EXCEPTION_MATCHES`.
- This proves local UAT checkout rendering and guarded state behavior only; location-provider success, merchant stock/price-change response, payment handoff/callback/replay, duplicate-submit behavior against staging, and cross-app merchant/courier mutation remain unproven.

## FLOW-04 Food checkout hierarchy parity follow-up

- Direct Figma inspection selected `TEMBUS Food - Checkout Pesanan` (`node-id=2-8503`, fixed `422px x 1,500px`, `#F2FCF3` canvas with white surfaces). The reference hierarchy places order detail, delivery context, add-menu action, and checkout controls before the deeper form sections.
- `FoodCheckoutScreen` now starts with `Rincian Pesanan`, merchant context, an honest delivery-address context card, and a functional `Tambah menu lain` action returning to the merchant route. The existing editable address/quote form remains below and is relabeled `Lengkapi Alamat & Titik Antar` to make the progressive disclosure explicit.
- Serial Android verification after the change: `./gradlew.bat --no-daemon :app:compileDebugKotlin` PASS — `BUILD SUCCESSFUL in 2m53s`; `./gradlew.bat --no-daemon :app:testDebugUnitTest` PASS — `BUILD SUCCESSFUL in 1m18s`; `./gradlew.bat --no-daemon :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 52s`. Existing deprecation/unused-parameter warnings remain advisory.
- Latest APK replay on `emulator-5554`: Home → Food → Cart → Checkout reached the revised hierarchy. Artifact: `C:\Users\yogis\AppData\Local\Temp\food-checkout-hierarchy-final.png`; AX artifact: `food-checkout-hierarchy-final.xml`. The local UAT cart remained server-backed and the delivery card correctly showed `Pilih alamat pengantaran` until a pin was selected.
- Filtered logcat after replay returned `NO_FATAL_EXCEPTION_MATCHES`.
- Real quote/location success, merchant stock/price-change response, payment callback/replay, duplicate submit against staging, and Merchant/Mitra mutations remain unproven external/runtime gates.
- `graphify update .`: PASS — graph rebuilt with `32,895 nodes`, `55,695 edges`, and `1,901 communities`; known zero-node, missing SQL-parser, and edge-relation warnings remain tooling warnings. Design token, component adoption, Figma traceability, task-evidence, and `git diff --check` validators all passed with existing advisory/CRLF warnings.

## FLOW-00 Home active-order slot parity follow-up

- Runtime comparison against the supplied `TEMBUS - Customer Home` Figma frame showed that the Home shell already had the correct header, location, search, wallet, service grid, promo, and five-item navigation, but the order-context card disappeared when the authoritative active-order query returned an empty list. That made the service grid jump upward and diverge from the Figma hierarchy.
- Added `EmptyActiveOrderCard` in `DashboardOrderSections.kt` and rendered it from `DashboardScreen.kt` only when `activeOrders.isEmpty()`. It preserves the Figma card slot and offers a `Pesan` CTA, while explicitly avoiding synthetic order id, courier, ETA, price, or dispatch state. Existing active-order rendering is unchanged.
- Customer Android verification after the change: `./gradlew.bat --no-daemon :app:compileDebugKotlin`: PASS (`BUILD SUCCESSFUL` in 2m40s); `./gradlew.bat --no-daemon :app:testDebugUnitTest`: PASS (`BUILD SUCCESSFUL` in 1m05s); `./gradlew.bat --no-daemon :app:assembleDebug`: PASS (`BUILD SUCCESSFUL` in 53s). Existing deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK installed successfully on `emulator-5554` and launched with `com.tembus.customer/.ui.MainActivity`. The UAT data had one real active order, so the replay showed the authoritative full active-order card, not the empty fallback. Artifact: `C:\Users\yogis\AppData\Local\Temp\home-empty-order-figma-slot.png` with AX tree `home-empty-order-figma-slot.xml`; the tree includes `TEMBUS`, `SALDO TEMBUS-PAY`, `Layanan Utama`, `Kirim Paket`, `Towing`, and `Beranda`. `Klaim Promo` was below the fixed viewport in this capture and is covered by the existing scrollable Home proof.
- Filtered logcat after the latest launch returned `NO_FATAL_EXCEPTION_MATCHES`. This is local emulator/UAT proof only; it does not prove staging/provider callbacks, merchant/mitra mutations, or production release readiness.
- `graphify update .`: PASS — graph rebuilt with `32,890 nodes`, `55,690 edges`, and `1,927 communities`; existing zero-node source-file, missing SQL-parser, and edge-relation warnings remain tooling warnings.
- Design token, component adoption, Figma traceability, task-evidence, and `git diff --check` validators all passed; repository-wide advisory warnings remain unchanged.

## FLOW-09 Account Figma hierarchy parity correction

- Direct Figma inspection selected `TEMBUS - Akun Pengguna` (`node-id=2-1953`, fixed `390px` width, Hug `1,513px`, `#FFFFFF` / `#F2FCF3` surfaces). The target hierarchy is profile identity, wallet, membership progress, grouped transaction/business settings, service/security preferences, help/legal actions, logout, and Account navigation.
- `ProfileScreen` now follows that hierarchy instead of the previous generic status/referral layout: server-backed profile image fallback, wallet card, `Lihat status dan benefit keanggotaan` membership card backed by `LoyaltyInfo`, three Figma-aligned menu groups, row descriptions, orange outlined logout action, and the existing five-item Account navigation. The membership card keeps an honest generic state when the loyalty response is unavailable; it does not inject Figma sample points or tier values.
- The Account `Profil Bisnis & E-Faktur` row is wired to the existing `BusinessScreen` destination. `Pusat Bantuan & CS 24 Jam` remains wired to the full `SupportScreen`, which was verified in the same authenticated navigation chain. Existing address, language, security, privacy, notification, and wallet routes were preserved.
- A geometry defect was corrected during replay: `ProfileContent` had applied `statusBarsPadding()` on top of Scaffold content insets, producing a visibly oversized top gap. Removing the duplicate inset aligned the header with the Figma frame while preserving system-bar safety.
- Customer Android verification: `./gradlew.bat --no-daemon :app:compileDebugKotlin`: PASS (`BUILD SUCCESSFUL` in 2m21s); `./gradlew.bat --no-daemon :app:assembleDebug`: PASS (`BUILD SUCCESSFUL` in 2m46s); `./gradlew.bat --no-daemon :app:testDebugUnitTest`: PASS (`BUILD SUCCESSFUL` in 59s, 36 actionable tasks, 7 executed and 29 up-to-date). Existing Kotlin deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK was installed on `emulator-5554` and replayed through `com.tembus.customer/.ui.MainActivity`. Runtime artifacts: `C:\Users\yogis\AppData\Local\Temp\account-parity-inset-fixed.png`, `account-parity-redesigned-lower.png`, `account-parity-redesigned-logout.png`, and accessibility tree `account-parity-redesigned.xml`. The lower tree contains the Figma group labels, preference/help/legal rows, logout action, and Account navigation labels.
- Account → `Pusat Bantuan & CS 24 Jam` replay opened the full support destination; artifact: `C:\Users\yogis\AppData\Local\Temp\account-support-route-after-redesign.png`.
- Filtered emulator logcat after the latest runtime replay returned `NO_FATAL_ANDROID_RUNTIME_MATCHES`. This is local authenticated proof only; it does not prove staging/provider behavior.
- `Metode Pembayaran` currently reuses the existing customer `WalletTopUp` destination because no separate payment-method-management route was present in the customer graph. `Kontak Darurat Keluarga` and `Beri Rating TEMBUS` retain their existing local callbacks/entry limitations; no backend capability was fabricated. Cross-app merchant/mitra mutation, provider callbacks/replay, staging data, and account deletion remain unproven release gates.
- `graphify update .`, design token validation, component adoption validation, Figma traceability validation, task-evidence validation, and `git diff --check` are required after this checkpoint; their results are recorded in the subsequent verification section once executed.

## FLOW-09 Account verification gates

- `graphify update .`: PASS — graph rebuilt with `32,888 nodes`, `55,686 edges`, and `1,916 communities`. Existing zero-node source-file, missing `tree_sitter_sql`, and edge-relation warnings remain tooling warnings.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS.
- `node scripts/design/validate-tembus-component-adoption.mjs`: PASS.
- `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS.
- `py scripts/tasks/validate_task_evidence.py`: PASS — repository-wide advisory warnings remain for older evidence files and `UIUX-2026-008` filename/master-task mapping.
- `git diff --check`: PASS — only existing CRLF normalization advisories were emitted.

## FLOW-00 Home bottom-navigation visual parity correction

- Direct Figma inspection selected `TEMBUS - Customer Home` (`node-id=1-2`, fixed `390px` width, Hug `1,584px`, `#FFFFFF` / `#F2FCF3` properties). The supplied Home capture shows a neutral bottom bar with orange active `Beranda` and no green selection pill.
- `TembusBottomNavigation` now accepts optional container/selected/indicator colors. `DashboardScreen` uses the Home-specific Figma treatment (`CustomerHomeCanvas`, `OrangeCta`, transparent indicator) while Account and other screens keep the existing shared green treatment. No parcel flow or order state was changed.
- Customer Android verification: `./gradlew.bat --no-daemon :app:compileDebugKotlin`: PASS — `BUILD SUCCESSFUL in 2m 26s`; `./gradlew.bat --no-daemon :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 38s`.
- Regression verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest`: PASS — `BUILD SUCCESSFUL in 1m 1s`; existing deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK installed successfully on `emulator-5554`; launcher replay reached Home and produced `C:\Users\yogis\AppData\Local\Temp\home-bottom-nav-orange.png`. The runtime capture shows the neutral Figma Home shell, orange active `Beranda`, five-item navigation, service grid, promo, and honest empty repeat-order state. No customer `FATAL EXCEPTION` appeared in filtered logcat.
- `graphify update .`: PASS — graph rebuilt with `32,883 nodes`, `55,673 edges`, and `1,916 communities`; existing zero-node, missing SQL-parser, and edge-relation warnings remain tooling warnings.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS; `node scripts/design/validate-tembus-component-adoption.mjs`: PASS; `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS; `py scripts/tasks/validate_task_evidence.py`: PASS; `git diff --check`: PASS with existing CRLF normalization advisories.
- This proves local Home visual/runtime parity for the corrected navigation treatment. It does not prove authenticated staging data, provider callbacks, cross-app mutations, or release readiness; those remain open task gates.

## FLOW-09 support page Figma parity follow-up

- Direct Figma inspection selected `TEMBUS - Pusat Bantuan` (`node-id=1-770`, fixed `390px` width, `#FFFFFF` / `#F7F8F6` surfaces). The intended hierarchy is a support header, order context, issue-topic cards, 24-hour escalation card, ticket state, and FAQ accordion.
- The Account `Pusat Bantuan` row was still opening the legacy help dialog. `ProfileScreen` and `RootNavGraph` now route that row to the full `SupportScreen` destination, so the Figma-oriented page is reachable from the authenticated customer shell.
- `SupportScreen` now renders server-backed active-order context from `OrderRepository` and `ActiveOrderRecoveryPolicy`, issue topics for order/payment/emergency/account concerns, the 24-hour official support CTA, an honest no-active-ticket state, and expandable FAQ answers. Topic actions preserve the configured official WhatsApp/email handoff; no ticket or provider success is fabricated.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 3m37s`, 55 actionable tasks, 13 executed and 42 up-to-date. Existing Kotlin deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK was installed and replayed on `emulator-5554`: Home → Akun → scroll → Pusat Bantuan opened the full-screen route. The accessibility tree contains `Bantuan cepat untuk order kamu`, `Pesanan aktif terpilih`, `Pilih kendala pesanan ini`, `Kendala Pesanan`, `Pembayaran & Saldo`, `Layanan Darurat`, `Akun & Keamanan`, `SIAGA 24 JAM`, `Status tiket pengaduan`, and `Topik bantuan populer`. Artifacts: `C:\Users\yogis\AppData\Local\Temp\parity-support-final2.png`, `support-final2.xml`, `parity-support-faq-expanded.png`, and `support-faq-expanded.xml`.
- The first FAQ was tapped and expanded; the answer states that status/actions follow the server order data. Filtered logcat after navigation and FAQ interaction contained no customer `FATAL EXCEPTION`. This proves local authenticated support rendering/navigation only; it does not prove WhatsApp delivery, external email delivery, provider callbacks, cross-app mutation, or release readiness.
- `graphify update .`: PASS — graph rebuilt with 32,882 nodes, 55,672 edges, and 1,931 communities; existing zero-node, missing SQL-parser, and edge-relation warnings remain non-blocking.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS; `node scripts/design/validate-tembus-component-adoption.mjs`: PASS; `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS; `py scripts/tasks/validate_task_evidence.py`: PASS; `git diff --check`: PASS with existing CRLF normalization advisories.

## Latest Home visual comparison checkpoint

- The rebuilt authenticated Home now exposes the same Figma composition in the emulator: `TEMBUS`/area header, location line, search shell, TEMBUS-Pay bar, active order, 2x3 `Layanan Utama` grid, green/orange promo card, `Pesan Lagi Cepat`, nearby-food section, and five-item bottom navigation. Top artifact: `C:\Users\yogis\AppData\Local\Temp\parity-home-after-install-2.png`; lower-scroll artifact: `C:\Users\yogis\AppData\Local\Temp\parity-home-lower-after-install.png`.
- The latest runtime data remains server/UAT-backed: the active order, current address, wallet amount, merchant availability, rating, and promo eligibility are not replaced with Figma sample values. When the API has no repeat order or merchant media, the hierarchy remains visible with an honest empty/placeholder state.

## FLOW-06/07 emergency entry parity follow-up

- The Figma towing source frame is `TEMBUS - Derek Towing Darurat` (`node-id=2-4186`). The existing Home `Towing` card already opened the matching emergency landing, but the `Derek Towing` toggle inside Tambal Ban still routed to the older generic `Towing Motor` form.
- `RootNavGraph` now routes every `towing*` selection from Tambal Ban to the same `ServiceCategoryScreen` emergency landing used by the Home card; tire-repair subtypes retain the existing `ServiceBooking` route. No customer-side quote, technician, route, or vehicle availability state was fabricated.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 2m 53s`, 55 actionable tasks, 12 executed and 43 up-to-date. The APK was installed on `emulator-5554` and launched through the package launcher.
- Home → `Towing` rendered the Figma-aligned emergency landing in `C:\Users\yogis\AppData\Local\Temp\parity-towing-from-home.png`. Tambal Ban → `Derek Towing` now renders the same landing in `C:\Users\yogis\AppData\Local\Temp\parity-towing-from-tambal.png`; the corresponding accessibility tree is `parity-towing-from-tambal.xml`.
- The verified accessibility tree contains `PROTOKOL KESELAMATAN TOWING`, `Rute evakuasi`, `TITIK PENJEMPUTAN DARURAT`, `TUJUAN PENGANTARAN / BENGKEL`, `Peta menunggu lokasi yang valid`, `Mobil Penumpang`, `Sepeda Motor / Moge`, and `Panggil Derek Towing Sekarang`. The disabled CTA remains honest until location, vehicle, conditions, consent, and the server quote are valid.
- Filtered emulator logcat after both entry paths returned `NO_CUSTOMER_FATAL_EXCEPTION`.
- This closes the local emergency-entry parity defect. It does not prove authorized mitra dispatch mutation, live routing/provider behavior, payment callback/replay, or staging/production release readiness.

## FLOW-01/02/03 customer canvas color parity follow-up

- The Figma source properties were rechecked for the customer canvas: `TEMBUS - Customer Home` exposes a `390 x 1,584` frame with `#FFFFFF` and `#F2FCF3`, while `TEMBUS - Kirim Paket On-Demand` (`node-id=2-6303`) exposes a `390 x 1,920` frame with the same canvas colors.
- Added the semantic Android token `CustomerCanvas = #F2FCF3` and applied it to the Home, Activity, Payment, Wallet Top Up, and parcel booking canvas/background surfaces. Parcel structure, navigation, view-model state, quote calculation, and backend contracts were intentionally not changed; this checkpoint is color-only for the parcel page.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 3m 09s`, 55 actionable tasks, 12 executed and 43 up-to-date. Existing Kotlin deprecation/unused-parameter warnings remain advisory.
- The rebuilt APK was installed on `emulator-5554` and launched through `com.tembus.customer/.ui.MainActivity`. Home rendered the Figma composition with the pale-green canvas, shared header/search/wallet shell, active order, 2x3 services, promo, and five-item navigation. Artifact: `C:\Users\yogis\AppData\Local\Temp\parity-home-canvas-f2fcf3-afterwait.png`; AX artifact: `parity-home-canvas-f2fcf3.xml`.
- Activity rendered the same pale-green canvas and shared shell with `Aktivitas`, server-backed active order, honest map-unavailable state, tracking/chat/call actions, and selected navigation. Artifacts: `C:\Users\yogis\AppData\Local\Temp\parity-activity-canvas-f2fcf3.png` and `parity-activity-canvas-f2fcf3.xml`.
- Home → Top Up opened the server-backed wallet top-up screen with the same Figma canvas, balance card, nominal choices, payment method card, and guarded payment CTA. Artifacts: `C:\Users\yogis\AppData\Local\Temp\parity-topup-canvas-f2fcf3-homeplus.png` and `parity-topup-canvas-f2fcf3-homeplus.xml`.
- Home → Kirim Paket retained the existing parcel flow and showed the new pale-green canvas behind the unchanged route/detail sheet. Artifacts: `C:\Users\yogis\AppData\Local\Temp\parity-parcel-canvas-f2fcf3-final.png` and `parity-parcel-canvas-f2fcf3-final.xml`.
- The emulator accessibility dumps contained the expected Home/Activity/Top Up/parcel labels. Filtered logcat after the latest launch contained no customer `FATAL EXCEPTION`. This proves local authenticated visual/state rendering only; it does not prove staging/provider callbacks, cross-app mutation, or release gates.
- `graphify update .`: PASS — graph rebuilt with 32,844 nodes, 55,582 edges, and 1,915 communities. Existing zero-node, missing SQL parser, and edge-relation warnings remain tooling warnings.

## FLOW-05 Figma canvas correction

- A direct Figma property audit corrected the previous over-generalization: `TEMBUS - Metode Pembayaran & Dompet` (`node-id=2-6624`) and `TEMBUS - Top Up Saldo TEMBUS-Pay` (`node-id=1-1396`) both use the neutral `#F7F8F6` canvas, while Home/parcel/tracking frames use `#F2FCF3` where applicable.
- `PaymentScreen` and `WalletTopUpScreen` now use `#F7F8F6`; Home and parcel canvas usage remains unchanged. No payment state, invoice URL, balance, or provider contract was modified.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 2m 59s`, 55 actionable tasks, 12 executed and 43 up-to-date.
- The rebuilt APK was installed and launched through `com.tembus.customer/.ui.MainActivity`. Home → Top Up rendered the server-backed balance card, nominal grid, payment-provider disclosure, and guarded CTA with the neutral Figma canvas. Artifacts: `C:\Users\yogis\AppData\Local\Temp\parity-topup-neutral-figma-canvas.png` and `parity-topup-neutral-figma-canvas.xml`; AX confirmed `Top up TEMBUS-Pay` and `Pilih Nominal Top Up`.
- This is local visual/runtime proof only; real provider callback/replay and cross-app payment mutation remain unproven.
- `graphify update .`: PASS — graph rebuilt with 32,845 nodes, 55,583 edges, and 1,919 communities; existing parser/zero-node/edge warnings remain tooling warnings.

## FLOW-00 Home canvas correction

- Pixel-level comparison against the supplied Figma Home capture showed the Home artboard canvas is neutral `#F7F8F6`; the earlier shared `CustomerCanvas = #F2FCF3` token was too broad for Home even though it remains correct for parcel/tracking surfaces.
- Added `CustomerHomeCanvas = #F7F8F6` and applied it only to the Home scaffold, Home scroll surface, and Figma Home header. Parcel/Activity/tracking canvas usage was not changed by this correction.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 3m 21s`, 55 actionable tasks, 13 executed and 42 up-to-date. Existing Kotlin deprecation/unused-parameter warnings remain advisory.
- The rebuilt APK was installed and launched on `emulator-5554`; runtime Home screenshot `C:\Users\yogis\AppData\Local\Temp\parity-home-neutral-canvas.png` shows the Figma-aligned neutral canvas with the existing TEMBUS header/location/search, wallet, active order, 2x3 service grid, promo, and bottom navigation.
- Runtime pixel sample at the Home canvas is `#F7F8F6`, matching the supplied Figma capture. No customer fatal was observed in the filtered launch verification.
- `graphify update .`: PASS — graph rebuilt with 32,845 nodes, 55,583 edges, and 1,919 communities; existing parser/zero-node/edge warnings remain tooling warnings.

## FLOW-08 messages and courier-call parity follow-up

- Direct Figma inspection selected `TEMBUS - Obrolan Kurir` (`node-id=1-1885`, fixed `390 x 1432`, `#FFFFFF` / `#F2FCF3`) and `TEMBUS - Panggilan Suara Kurir` (`node-id=1-944`, fixed `390 x 901.5`, `#FFFFFF` / `#F2FCF3`).
- `MessagesScreen` now follows the Figma inbox hierarchy with a server-backed filter row (`Semua`, `Kurir Aktif`, `Merchant Food`), an active-order conversation card, history count, and filtered empty state. The courier filter uses `OrderActionPolicy.canChat(order.status)` instead of requiring a courier display name, so an active order without a populated courier name remains discoverable.
- The customer call screen was visually refactored from the previous dark gradient to the Figma light composition: `Active VoIP Call` header, masked-call/security badge, order context, participant card, safety protocol panel, route context, scrollable controls, permission/error states, and call-end treatment. Existing ViewModel actions remain authoritative for accept/reject, mute, retry, hangup, and chat; unavailable vehicle/rating/live-route fields are not fabricated.
- Customer Android verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS — `BUILD SUCCESSFUL in 3m44s`, 55 actionable tasks, 15 executed and 40 up-to-date. Existing deprecation/unused-parameter warnings remain advisory.
- Rebuilt APK was installed on `emulator-5554`. Messages runtime proof: `C:\Users\yogis\AppData\Local\Temp\parity-messages-final.png`, `parity-messages-final.xml`, `parity-messages-courier-filter-fixed.png`, and `parity-messages-courier-filter-fixed.xml`. Courier chat proof: `parity-courier-chat-final.png` and `parity-courier-chat-final.xml`.
- Courier-call runtime proof reached the Figma-aligned light permission screen and then the honest local failure state when the call provider was unavailable: `C:\Users\yogis\AppData\Local\Temp\parity-call-light-permission.png`, `call-screen.xml`, and `call-accepted.xml`. The accessibility tree exposed `Active VoIP Call`, `MASKED CALL ACTIVE`, `Panggilan Aman TEMBUS • Nomor Disamarkan`, `Order #... • Kirim Paket Instan`, `Protokol Keselamatan Berkendara`, `Lokasi order tersimpan`, and the error actions. The local call failure is not treated as provider success.
- After the call/chat replay, filtered logcat returned `NO_CUSTOMER_FATAL_EXCEPTION`. No real courier answer, VoIP media connection, or cross-app call mutation is claimed; those remain staging/provider evidence gates.
- `graphify update .`: PASS — graph rebuilt with 32,861 nodes, 55,625 edges, and 1,929 communities; existing zero-node, SQL-parser, and edge-relation warnings remain non-blocking.
- `node scripts/design/validate-tembus-design-tokens.mjs`: PASS.
- `node scripts/design/validate-tembus-component-adoption.mjs`: PASS.
- `node scripts/design/validate-tembus-figma-traceability.mjs`: PASS.
- `py scripts/tasks/validate_task_evidence.py`: PASS — repository-wide advisory warnings remain for older evidence files and do not invalidate this checkpoint.
- `git diff --check`: PASS — only existing CRLF normalization advisories were emitted.

## FLOW-08 camera-only photo capture follow-up

- Customer photo entry is now camera-only across the supported surfaces: roadside Tambal Ban (`Foto kondisi ban`), Towing (`Foto kondisi kendaraan`), and the package/item-photo surface (`FOTO BARANG`). Each surface uses `ActivityResultContracts.TakePicture()` with an app-owned MediaStore destination; no gallery or document picker is exposed.
- Removed the unused customer `READ_EXTERNAL_STORAGE` permission. The legacy `WRITE_EXTERNAL_STORAGE` declaration remains scoped to Android API 28 and below for camera output compatibility; the app does not request or read gallery media.
- Roadside photos continue through the real multipart `POST /api/v1/customer/orders/:id/roadside-photo` contract and are persisted in the order `package_details.vehicle_details.photo_items`/`photo_urls` with ownership, role, max-count, checksum deduplication, terminal-state, and rollback guards.
- Source audit: `rg -n "PickVisualMedia|GetContent|GetMultipleContents|OpenDocument|ACTION_OPEN_DOCUMENT|ACTION_GET_CONTENT|Galeri|gallery" android-app-customer/app/src/main/java -g "*.kt"` returned no matches. Activity-result audit shows only `TakePicture()` for photo capture; remaining launchers are permission/location launchers.
- Customer unit tests: `./gradlew.bat --no-daemon :app:testDebugUnitTest`: PASS — `BUILD SUCCESSFUL in 4m 36s`, 36 actionable tasks, 17 executed and 19 up-to-date. Existing deprecation/unused-parameter warnings remain advisory.
- Customer debug APK: `./gradlew.bat --no-daemon :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 38s`, 46 actionable tasks, 4 executed and 42 up-to-date. APK was installed on `emulator-5554`; package inspection confirms `CAMERA` is granted and `READ_EXTERNAL_STORAGE` is absent.
- Emulator smoke proof: Tambal Ban → `Kamera` opened `com.android.camera2/.CaptureActivity`; Towing → `Kamera` opened the same camera activity; Account → `Profil Bisnis & E-Faktur` → `FOTO BARANG` opened the same camera activity. Accessibility dumps showed `Kamera` and no `Galeri` on the roadside screens. This is local camera/navigation proof; authenticated staging upload plus DB readback for a newly created order remains unproven in this checkpoint.
- Backend regression: `npm run build` in `backend/admin-service`: PASS (`tsc` exit 0); `go test ./...` in `backend/order-service`: PASS across domain, handler, repository, service, worker, and package tests.
- Repository gates after this change: `graphify update .` PASS (`32,963 nodes`, `55,797 edges`, `1,894 communities`, with the known zero-node/SQL-parser/edge-field warnings); design-token, component-adoption, Figma-traceability, task-evidence, and `git diff --check` validators PASS. The task remains PARTIAL because staging/provider/cross-app proof and the broader Figma regression matrix are still open.
- Final camera URI hardening rebuild: `./gradlew.bat --no-daemon :app:compileDebugKotlin :app:assembleDebug`: PASS — `BUILD SUCCESSFUL in 3m 34s`; APK reinstall on `emulator-5554`: PASS (`Success`). A follow-up `graphify update .` found no code-graph topology changes; the known extraction warnings remain non-blocking.

## FLOW-03 aggregator Figma parity follow-up

- Rechecked the Figma frame `TEMBUS - Kirim Paket & Pilihan Ekspedisi` (`node-id=2-5858`) at 390px. The customer aggregator now includes the same header/banner rhythm, destination block, compact provider selector, rounded service quote cards, package category rail, quantity/photo area, insurance, pickup readiness, recipient, payment summary, and sticky confirmation CTA.
- Provider names, availability, capabilities, services, tracking mode, package categories, quote services, ETD, chargeable weight, and customer tariff are still read from the existing public provider/quote/category contracts. No sample provider, price, category, photo URL, or rating was added to the Android source.
- Package photo capture remains camera-only (`TakePicture()`), limited to three images and 5 MB/image. Successful aggregator order creation now uploads the captured package photos through the authenticated multipart `POST /api/v1/customer/orders/:id/package-photo` route; the backend validates ownership/category/terminal state, deduplicates by checksum, persists metadata/URLs, and exposes retry state when the upload fails.
- Files changed for this follow-up: `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/AggregatorScreen.kt`, `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/booking/BookingViewModel.kt`, `android-app-customer/app/src/main/java/com/tembus/customer/data/api/TEMBUSApiService.kt`, `android-app-customer/app/src/main/java/com/tembus/customer/data/repository/OrderRepository.kt`, `android-app-customer/app/src/main/java/com/tembus/customer/data/model/CustomerModels.kt`, `backend/admin-service/src/controllers/order/customerOrderFile.controller.ts`, `backend/admin-service/src/routes/order.routes.ts`, and `database/migrations/20260922000002_aggregator_package_categories.sql`.
- Verification: `:app:compileDebugKotlin --no-daemon` PASS; `:app:assembleDebug --no-daemon` PASS; `npm run build` in `backend/admin-service` PASS; `npm test -- --runInBand src/routes.test.ts` PASS (23/23). The authenticated emulator previously rendered the aggregator screen and opened the real camera flow; after the final APK reinstall the emulator returned to login, so no new authenticated runtime screenshot is claimed for the last compact-provider-only patch.
- This checkpoint remains PARTIAL. The local implementation and build gates are proven; authenticated staging order/payment/provider callback, DB readback for a newly created package-photo order, and customer↔merchant↔mitra mutation remain unproven and are not presented as complete.
- `graphify update .`: PASS — graph rebuilt with 33,098 nodes, 56,074 edges, and 1,946 communities; existing zero-node/source, missing SQL parser, and edge-relation warnings remain tooling warnings.

## FLOW-03 aggregator provider catalog and header parity correction

- The previous runtime comparison showed two remaining visual/behavioral gaps against the Figma frame: the customer provider row only exposed gateway-registered providers, leaving the row visually sparse, and the header `Bantuan` label was not interactive.
- `listCustomerLogisticsProviders` now merges the executable integration-gateway registry with the DB logistics catalog. Configured-but-disabled providers are returned as disabled cards, while gateway availability/capability remains authoritative for selectable providers. The local endpoint now returns JNE, J&T, SiCepat, and AnterAja; only executable providers remain selectable.
- `AggregatorProviderOptionCard` now uses the compact Figma card proportion for every catalog item, hides gateway tracking internals from customer copy, and keeps provider/service data server-backed. The aggregator header title now matches `Kirim Paket & Pilihan Ekspedisi`, and `Bantuan` routes to the existing Support destination through `RootNavGraph`.
- Quote cards now use the Figma hierarchy for selected state: green server price, compact `Terpilih` pill, and the rounded light-green selected surface; no quote value is client-generated.
- Local runtime API verification after rebuilding `admin-service`: `GET http://localhost:3087/api/v1/logistics/providers` returned HTTP 200 with four DB/gateway-merged providers; JNE and J&T were available, SiCepat and AnterAja were explicitly disabled and non-selectable.
- Merge precedence was hardened and reverified through the gateway: a DB-disabled provider remains `available=false` with zero selectable capabilities even if the integration gateway still advertises it.
- Verification: `:app:compileDebugKotlin --no-daemon` PASS; `:app:assembleDebug --no-daemon` PASS; `npm run build` PASS; `npm test -- --runInBand src/routes.test.ts` PASS (23/23); `docker compose up -d --build admin-service` PASS; `git diff --check` PASS with the existing CRLF normalization advisory.
- No authenticated post-install screenshot is claimed for this final patch because the emulator session returned to login after APK installation. The API result and compilation prove the provider catalog wiring, but visual parity still needs one authenticated emulator replay on the rebuilt APK before this flow can be marked complete.
- `graphify update .`: PASS — graph rebuilt with `33,099 nodes`, `56,075 edges`, and `1,948 communities`; existing zero-node/source, missing SQL parser, and edge-relation warnings remain tooling warnings. Design-token, component-adoption, Figma-traceability, and task-evidence validators also passed.

## FLOW-02 Activity Figma hierarchy parity follow-up

- Direct Figma inspection selected `TEMBUS - Aktivitas & Riwayat` (fixed `390px` width, Hug `1,360px`, white / `#F2FCF3` surfaces). The reference hierarchy is the shared customer header, `Aktivitas` title/subtitle, four filter chips with only the active-count emphasis, `PESANAN AKTIF` with realtime label, courier/ETA context, tracking actions, and compact `RIWAYAT PESANAN` cards.
- `OrderHistoryScreen` now keeps the Figma filter copy hierarchy (`Berlangsung (n)` only when active orders exist; other labels are not padded with counts), adds the server-backed courier/vehicle/rating/ETA row to the active-order card, and shows courier initials when no profile image is available without inventing a name or status.
- Food history cards now use the first persisted `food_items.photo_url` through the configured gateway media base when the order snapshot includes it; the icon fallback remains explicit when the backend has no item image. No Figma sample image, merchant, price, ETA, or order data was added to Android source.
- The active card continues to use only `OrderTrackingDetail`/`CustomerTrackingSnapshot` for map coordinates and tracking metadata. When no snapshot exists it remains the honest `Lokasi kurir belum tersedia` state.
- Verification: `:app:compileDebugKotlin --no-daemon` PASS (`BUILD SUCCESSFUL in 2m23s`); `:app:testDebugUnitTest :app:assembleDebug --no-daemon` PASS (`BUILD SUCCESSFUL in 1m05s`, APK assembled at `android-app-customer/app/build/outputs/apk/debug/app-debug.apk`). Existing Kotlin deprecation/unused-parameter warnings remain advisory. The post-change emulator launch was verified on `emulator-5554`, but the session returned to the login screen; no authenticated screenshot is claimed for this latest Activity patch. An attempted credential replay was abandoned after keyboard focus shifted the login form, and the temporary screenshot containing the field input was deleted without adding it to evidence.
- This checkpoint remains PARTIAL. Authenticated emulator proof of the latest card/image layout, staging order data, and customer↔merchant↔mitra mutations remain unproven; the local implementation must continue through the remaining Figma flow gates.

## FLOW-03 aggregator structural parity correction

- Direct Figma inspection of `TEMBUS - Kirim Paket & Pilihan Ekspedisi` (`node-id=2-5858`, fixed 390px frame) confirmed the target order: green `Garansi Tepat Waktu` banner, separate `LOKASI JEMPUT` and `TUJUAN KIRIM` cards, compact provider rail, tall rounded service quote cards, and package specification/photo area.
- `AggregatorScreen` now follows that hierarchy. The old combined pickup/destination summary and generic header were removed from the content flow; pickup and destination are separate Figma-shaped cards, the banner uses the Figma dark-green gradient/copy, provider cards are compact, and quote rows use the Figma pill proportions with server ETA and price.
- Aggregator initialization no longer injects sample pickup, destination, recipient, phone, package, or item values. The view model hydrates pickup/destination from the authenticated customer address book when available and otherwise leaves the form empty for real map/address selection. Package category selection also remains empty until the API returns CMS/database categories; no `doc` fallback is injected.
- Package capture remains camera-only through `TakePicture()`, max three images and 5 MB each. The existing authenticated multipart upload and server-side ownership/checksum/terminal-state validation are unchanged.
- Verification: `.\gradlew.bat :app:compileDebugKotlin --no-daemon` PASS (`BUILD SUCCESSFUL in 2m29s`); `.\gradlew.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon` PASS (`BUILD SUCCESSFUL in 1m38s`); APK install on `emulator-5554` PASS and app process remained alive with no matching `FATAL EXCEPTION` in filtered logcat; `graphify update .` PASS (33,104 nodes / 56,088 edges / 1,922 communities); design-token, component-adoption, Figma-traceability, task-evidence, and `git diff --check` validators PASS with existing advisory/CRLF warnings.
- No authenticated screenshot is claimed for this latest APK because the emulator is currently at the login state after reinstall. Provider callbacks, payment replay, staging order/quote/photo DB readback, and customer↔merchant↔mitra mutation remain unproven. This flow remains `PARTIAL`.

## FLOW-03 aggregator quote presentation data contract

- Removed remaining aggregator quote fallbacks that could create false production data: client category no longer falls back to `doc`, provider/service selection no longer falls back to synthetic codes, aggregator `distanceKm`/`etaMinutes` no longer use sample values, and the backend tariff fallback no longer substitutes a provider, area codes, category, or code-based rate/ETA mapping.
- Added migration `database/migrations/20260923000003_aggregator_service_presentation.sql` with DB-backed `provider_services` presentation metadata (`customer_badge`, `customer_description`, `etd_label`, `min_weight_kg`) and seeded JNE/J&T service rows. The Android model renders badge/description only when the API returns persisted metadata.
- `check-tariff` now enriches gateway quotes from `provider_services` and uses the same DB metadata in its fallback calculation. Local migration execution succeeded (`ALTER TABLE`, `INSERT 0 4`), and the live local endpoint returned JNE quote metadata including `Garansi Tepat Waktu`, `Hemat & Terpercaya`, `Muatan Berat`, persisted descriptions, ETA labels, and server tariffs.
- Verification after this contract change: admin-service `npm run build` PASS; targeted route suite `npm test -- --runInBand src/routes.test.ts` PASS (23/23); Android `:app:compileDebugKotlin --no-daemon` PASS; `:app:testDebugUnitTest :app:assembleDebug --no-daemon` PASS; `docker compose up -d --build admin-service` PASS; local tariff endpoint HTTP 200 with DB metadata; final APK install/process smoke PASS with no matching customer `FATAL EXCEPTION`.
- This remains `PARTIAL`: authenticated rebuilt-APK visual replay, staging/provider callback, payment replay, new-order photo DB readback, and customer↔merchant↔mitra mutation are still unproven.

## FLOW-03 aggregator header parity correction

- A fresh direct Figma inspection of `TEMBUS - Kirim Paket & Pilihan Ekspedisi` (`node-id=2-5858`) found the remaining top-of-screen mismatch: the 390px reference includes a white navigation header above the green guarantee banner, with back navigation, the `Kirim Paket & Pilihan Ekspedisi` title, and a `Bantuan` action.
- `AggregatorScreen` now restores that header using the shared `TembusAppBar`, keeps the existing server-backed help route, and preserves the corrected Figma order below it. The previous implementation had removed the app bar, so the screen began directly with the banner.
- Verification after this patch: `./gradlew.bat :app:compileDebugKotlin --no-daemon` PASS (`BUILD SUCCESSFUL in 2m27s`); `./gradlew.bat :app:assembleDebug --no-daemon` PASS (`BUILD SUCCESSFUL in 1m08s`); `./gradlew.bat :app:testDebugUnitTest --no-daemon` PASS (`BUILD SUCCESSFUL in 54s`); APK reinstall on `emulator-5554` PASS; filtered logcat returned `NO_FATAL_EXCEPTION_MATCHES`.
- Design token, component adoption, Figma traceability, task evidence, and `git diff --check` validators remain PASS (existing advisory warnings unchanged). No authenticated screenshot is claimed for this APK because the emulator session is at login; authenticated visual replay, staging/provider callbacks, payment replay, photo DB readback, and customer↔merchant↔mitra mutation remain open. This flow remains `PARTIAL`.

## FLOW-03 aggregator final header spacing pass

- Added the Figma top breathing room between the restored navigation header and the guarantee banner (`8dp` content inset); no business/data contract was changed.
- Final verification: `./gradlew.bat :app:compileDebugKotlin --no-daemon` PASS (`BUILD SUCCESSFUL in 2m27s`); `./gradlew.bat :app:assembleDebug --no-daemon` PASS (`BUILD SUCCESSFUL in 41s`); final APK reinstall PASS on `emulator-5554`; filtered logcat returned `NO_FATAL_EXCEPTION_MATCHES`.
- This is still local runtime/process proof only. The latest emulator is unauthenticated, so no authenticated visual screenshot or staging/cross-app mutation claim is made.

## FLOW-03 aggregator runtime quote/data parity continuation

- Direct Figma recheck of `TEMBUS - Kirim Paket & Pilihan Ekspedisi` (`node-id=2-5858`) confirmed the 390px vertical hierarchy: white navigation header, green guarantee banner, separate pickup/destination cards, compact provider rail, ordered service cards, package/photo area, and sticky confirmation CTA.
- Added DB-backed provider presentation metadata and ordering through migrations `20260923000004_aggregator_provider_presentation.sql`, `20260923000005_aggregator_provider_area_seed.sql`, `20260923000006_aggregator_jnt_service_catalog.sql`, and `20260923000007_aggregator_service_display_order.sql`. J&T service catalog now exposes Reguler (2–3 Hari), Next Day (1 Hari), and Kargo / Truk (>5 kg) with persisted badges, ETD, minimum weight, area mappings, and customer display order.
- Backend `GET /api/v1/logistics/providers` now merges DB catalog with executable gateway capabilities: J&T and JNE are selectable locally, while SiCepat and AnterAja remain explicit disabled cards because no executable local capability is available. No provider was activated only to fill the Figma rail.
- Backend tariff verification returned HTTP 200 for J&T `CGK10000` → `BDO10000`, 1 kg and returned the DB-ordered service cards: `EZ Reguler (2–3 Hari)` Rp19.000, `NEXT Next Day (1 Hari)` Rp28.000, and `JTR Kargo / Truk (>5 kg)` Rp45.000.
- Authenticated emulator replay on `emulator-5554` rendered the provider/service/package flow. Accessibility output confirmed the service order and values in `service-cards-final.xml`; `aggregator-runtime-service-parity-final.png` confirmed the package section, dimensions, insurance, camera-only photo entry, and server quote summary. The camera surface exposes `Ambil dari kamera` and `Maks. 3 foto · 5 MB/foto`; no gallery action is present.
- Verification: `npm run build` in `backend/admin-service` PASS; `npm test -- --runInBand src/routes.test.ts` PASS (23/23); `docker compose up -d --build admin-service` PASS; `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS; `graphify update .` PASS (32,861 nodes, 55,625 edges, 1,929 communities); design-token, component-adoption, Figma-traceability, task-evidence, and `git diff --check` validators PASS with known advisory warnings.
- This checkpoint remains PARTIAL. No staging push/deploy was performed in this continuation. Authenticated staging order/payment/provider callbacks, DB readback for a newly created package-photo order, and customer↔merchant↔mitra mutation/readback remain unproven; the broader Figma regression matrix is still open.

## FLOW-03 provider catalog service-order consistency

- Hardened `listCustomerLogisticsProviders` so gateway-provided service arrays are sorted with the DB `provider_services.customer_display_order` metadata before reaching any customer client.
- Rebuilt `admin-service` and verified the live local provider endpoint returns J&T services in the same order as the tariff endpoint: `EZ`, `NEXT`, `JTR`.
- Verification: `npm run build` PASS; `npm test -- --runInBand src/routes.test.ts` PASS (23/23); `docker compose up -d --build admin-service` PASS; live `GET http://localhost:3087/api/v1/logistics/providers` returned J&T available with service order `EZ, NEXT, JTR`.
- This does not change the release status: staging deploy, authenticated provider callbacks/payment, fresh order/photo DB readback, and cross-app mutation/readback remain unproven.

## Staging push checkpoint

- Commit `41c0fd588252e9511a9248fbec5383fd6d23c56c` (`feat(customer): align aggregator flows with Figma and DB catalog`) was pushed successfully to `origin/staging`.
- Remote verification: `git ls-remote origin refs/heads/staging` returned the same SHA. This confirms source delivery to the staging branch; it does not by itself prove that a staging runtime deployment, migration application, provider callback, payment replay, or cross-app mutation has completed.

## FLOW-03 aggregator delivery-code removal and CTA color correction

- Removed the aggregator-only `Kode terima paket` card from the customer aggregator form. The standard on-demand booking add-on remains unchanged.
- Aggregator review facts no longer render the delivery-code line. The aggregator order payload also forces `requiresDeliveryCode=false`, including when an older persisted draft contained the previous toggle value; regular booking continues to use its existing delivery-code setting.
- The aggregator review CTA now uses the shared design token `OrangeCta` with `OnOrangeCta` text, while the regular booking review CTA remains green. This matches the attached reference and preserves the documented contrast-safe orange CTA.
- Verification: `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS (`BUILD SUCCESSFUL`); APK install on `emulator-5554` PASS; app launch/process smoke PASS with no matching `FATAL EXCEPTION`; authenticated emulator replay opened the aggregator and accessibility output contained no `Kode terima paket`/`Kode terima` in the aggregator screen. Runtime screenshots: `C:\Users\yogis\AppData\Local\Temp\aggregator-code-removed-top.png` and `C:\Users\yogis\AppData\Local\Temp\aggregator-review-orange-bottom.png`; the latter visibly shows the orange `Kirim TEMBUS Aggregator • Rp19.000` CTA and no delivery-code line.
- `graphify update .`: PASS — graph rebuilt with 33,111 nodes, 56,092 edges, and 1,929 communities; known zero-node, SQL-parser, and edge-relation warnings remain tooling warnings. Design-token, component-adoption, Figma-traceability, and task-evidence validators also passed.
- This change is currently local and not pushed after the previous staging checkpoint. Staging runtime deployment and authenticated end-to-end payment/provider callback verification remain separate gates.

## FLOW-06 Tambal Ban Figma primary-flow parity follow-up (2026-09-23)

- Direct Figma inspection selected `TEMBUS - Layanan Darurat Tambal Ban` at 390px with `#FFFFFF` / `#F2FCF3` surfaces. The primary hierarchy is `Layanan Darurat`, safety banner, pickup location/GPS state, `Tambal Ban` / `Derek Towing` mode, vehicle selection, multi-select problem detail, optional photo/note evidence, server quote range, and the orange technician CTA. The related Figma offer/profile frames remain the existing `NearbyCouriersScreen` and `CourierDetailScreen` routes.
- `TambalBanHomeScreen` now follows that hierarchy. Problem choices use the Figma taxonomy (`Ban Bocor Kena Paku / Benda Tajam`, `Ban Kempis / Kurang Angin`, `Pentil Rusak / Bocor Halus`, `Ban Robek / Pasang Ban Cadangan`) as full-width selectable rows with checkbox state. The CTA requires a real vehicle service, valid GPS coordinates, at least one selected problem, consent, and a non-loading server state.
- The selected problem labels and optional notes are staged through `SavedStateHandle` from Tambal Ban Home → technician offers → technician profile → service booking. `ServiceBookingViewModel` now includes roadside `vehicleDetails` for both Tambal Ban and Towing, so damage and notes are carried in the authoritative order request instead of being dropped for Tambal Ban. Price, ETA, service availability, technician identity, and quote remain API/DB-backed.
- `ServicePhotoEvidencePicker` keeps the requested camera-only behavior and now supports the Figma `Foto Kondisi & Catatan (Opsional)` block. Runtime accessibility output contains `Kamera` and `0/3 foto diambil dari kamera`; no `Galeri` action is exposed. The same camera-only component remains shared by Towing and the existing package flow.
- Local emulator verification on `emulator-5554` after installing `android-app-customer/app/build/outputs/apk/debug/app-debug.apk`: the authenticated Home → `Tambal Ban` route rendered `Layanan Darurat`, GPS state, vehicle cards, the four exact problem labels, `Foto Kondisi & Catatan (Opsional)`, `Catatan (opsional)`, and `Kamera`. Artifacts: `android-app-customer/runtime-tambal-ban-home-location.png`, `android-app-customer/runtime-tambal-ban-home-location.xml`, `android-app-customer/runtime-tambal-ban-detail-new.png`, `android-app-customer/runtime-tambal-ban-detail-new.xml`, and `android-app-customer/runtime-tambal-ban-offers-new.xml`.
- The same rebuilt APK verified the existing Towing emergency landing still renders the Figma hierarchy and camera-only photo state: `PROTOKOL KESELAMATAN TOWING`, `Rute evakuasi`, vehicle cards, driver conditions, `Foto kondisi kendaraan`, `Kamera`, and the server-authoritative quote explanation. Artifacts: `android-app-customer/runtime-towing-main-new.png`, `android-app-customer/runtime-towing-main-new.xml`, `android-app-customer/runtime-towing-detail-new.png`, and `android-app-customer/runtime-towing-detail-new.xml`.
- Verification executed: `./gradlew.bat --no-daemon :app:compileDebugKotlin` PASS; `./gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug` PASS; `go test ./internal/service ./internal/handler` in `backend/order-service` PASS; `node scripts/design/validate-tembus-design-tokens.mjs` PASS; `node scripts/design/validate-tembus-component-adoption.mjs` PASS; `node scripts/design/validate-tembus-figma-traceability.mjs` PASS; `py scripts/tasks/validate_task_evidence.py` PASS with existing repository advisory warnings; `git diff --check` PASS; `graphify update .` PASS with existing zero-node/SQL-parser/edge-relation warnings.
- The remote debug/staging API returned no active technician offers for the authenticated replay, so the live offer/profile/booking mutation was not claimed as proven. No synthetic provider or price was added to make the Figma card appear. Payment and external-provider callback/replay remain explicitly deferred per product scope. This flow remains `PARTIAL` pending staging runtime deployment, active provider data, and end-to-end cross-app/order verification.
