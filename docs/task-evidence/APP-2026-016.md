---
task_id: APP-2026-016
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: "3944e3c6"

tests: PASS
integration: PASS
e2e: PASS
migration: N/A
migration_na_reason: "APP-2026-016 adds contract, Android unit, and browser E2E coverage only; it introduces no persistent schema or stored-data change."
observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: PARTIAL
release_followups: "Run the same runtime experience flows against the authorized staging deployment and representative Android emulator/device after promotion; verify deployed manifest, cache, asset, kill-switch, rollback, and transaction-boundary behavior."

unproven_requirements: NONE
known_blockers: NONE
locally_actionable_remaining: NONE

blocker_resolution_attempts: NONE
unblock_condition: NONE

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE

dependency_chain_blocked: false
next_eligible_task: ADMEXP-2026-001

updated_at: 2026-09-09
---

# Evidence — APP-2026-016

## Acceptance Criteria Source

The original checklist in `task-food-marketplace-parity-2026.md` requires runtime proof for:

- `[x]` First install offline uses packaged default.
- `[x]` Cached manifest renders while network refresh fails.
- `[x]` New compatible revision updates banner/home order without app release.
- `[x]` Invalid/unknown component safely skipped.
- `[x]` Corrupt/oversized asset safely rejected.
- `[x]` Campaign starts/expires by schedule correctly across timezone.
- `[x]` Kill switch removes entry point but active order remains reachable.
- `[x]` Old app ignores unsupported new component and remains usable.
- `[x]` Bad revision rollback restores known-good experience.
- `[x]` Promo shown remotely but server rejects ineligible promo at transaction boundary.

These remain the original task requirements. Staging/device promotion checks are recorded separately as release follow-up because the original APP-2026-016 section asks for the contract and test scenarios, not a staging launch gate.

## Scope Implemented

- Added a backend runtime contract/fuzz corpus covering the component whitelist, protected transaction fields, presentation-only promo payloads, kill-switch selection, and explicit public fallback.
- Added Android repository/validator coverage for first-install offline fallback, stale last-known-good retention during a failed refresh, and oversized asset rejection.
- Added browser E2E coverage for compatible revision refresh without a web/app release and for preserving the active-order dashboard path when experience content is unavailable.
- Reused and verified the existing manifest resolver, immutable revision rollback, schedule normalization, Android renderer skip behavior, startup campaign policy, asset staging, authoritative voucher validation, and observability guardrail implementation.

## Files Changed

- `backend/admin-service/src/services/experienceRuntimeContract.test.ts` — deterministic fuzz corpus and backend runtime boundary tests.
- `android-app-customer/app/src/test/java/com/tembus/customer/config/ExperienceConfigRepositoryTest.kt` — packaged-default and LKG network-failure scenarios.
- `android-app-customer/app/src/test/java/com/tembus/customer/config/ExperienceConfigManagerTest.kt` — oversized asset rejection scenario.
- `frontend/e2e/runtime-experience.spec.ts` — local Chromium E2E for revision refresh and active-order recovery.
- Existing experience implementation and tests exercised: `experienceConfig`, `experienceObservability`, Android validator/renderer/startup policy, and order-service voucher validation.

## Commands / Checks Run

    command: cd backend/admin-service; npm test -- --runInBand src/services/experienceRuntimeContract.test.ts src/services/experienceConfig.test.ts src/services/experienceObservability.test.ts
    result: PASS — 3 suites, 38 tests.

    command: cd backend/admin-service; npm test -- --runInBand
    result: PASS — 102 suites, 450 tests.

    command: cd backend/admin-service; npm run build
    result: PASS — TypeScript compilation.

    command: cd android-app-customer; .\gradlew.bat testDebugUnitTest --tests "com.tembus.customer.config.ExperienceConfigRepositoryTest" --tests "com.tembus.customer.config.ExperienceConfigManagerTest" --tests "com.tembus.customer.config.DynamicHomeRendererTest" --tests "com.tembus.customer.config.StartupCampaignPolicyTest" --no-daemon
    result: PASS — BUILD SUCCESSFUL; 36 actionable Gradle tasks.

    command: cd frontend; npm test -- --run
    result: PASS — 18 test files, 56 tests.

    command: cd frontend; npm run lint
    result: PASS — 0 errors, 503 existing warnings.

    command: cd frontend; npm run build
    result: PASS — Next.js production build and TypeScript checks.

    command: cd frontend; npx playwright test e2e/runtime-experience.spec.ts --project=chromium
    result: INITIAL RUN NOT EXECUTED — Playwright webServer could not start because an existing Next dev server occupied the configured port.

    command: cd frontend; BASE_URL=http://localhost:3004 npx playwright test e2e/runtime-experience.spec.ts --project=chromium
    result: PASS — 2 Chromium tests: compatible revision update and active-order recovery after experience unavailability.

    command: cd backend/order-service; go test ./internal/service -run 'TestVoucherValidate_|TestFood' -count=1
    result: PASS — voucher eligibility and Food service tests passed.

    command: git diff --check and staged diff check
    result: PASS.

## Task-Local Verification

### Tests

Status: PASS

Evidence: Backend fuzz/contract tests reject 64 generated unknown components and protected transaction fields. Android tests cover packaged default, stale LKG retention, schema/component fallback, dynamic renderer skipping, campaign schedule/frequency, and oversized/corrupt asset handling. Full backend and frontend suites also passed.

### Integration

Status: PASS

Evidence: The tested path spans the existing admin-service manifest parser/resolver, Android cache/validator/renderer/startup policy, customer web renderer, and order-service voucher eligibility boundary. The browser E2E exercises a dashboard manifest response and the real active-order rendering path without replacing it with a fake transaction result.

### E2E

Status: PASS

Evidence: Local Chromium E2E passed 2/2. A compatible manifest revision changed from “Revision satu” to “Revision dua” after reload while the active order remained visible. When the experience endpoint returned unavailable, `LCR-RUNTIME-001` and “Order Aktif Terbaru” remained visible and the empty-order state did not appear.

### Migration

Status: N/A

Evidence: No migration or persistent schema change was introduced by APP-2026-016; the existing experience migration behavior belongs to its implementation tasks.

### Observability

Status: PASS

Evidence: Existing runtime telemetry and admin observability tests passed, including cache/fetch/schema/asset/network failure categories, manifest revision/market/app-version dimensions, reliability guardrail separation from marketing events, and rollback action evaluation.

### Security / Privacy

Status: PASS

Evidence: The fuzz corpus proves remote components cannot execute and protected price/payment/eligibility/order/provider fields cannot enter presentation payloads. Existing parser and client tests enforce allowlisted components/routes/assets, bounded payloads, checksum/ETag validation, and no customer identity or authoritative promo state in the manifest.

### Rollback / Recovery

Status: PASS

Evidence: Existing lifecycle tests prove immutable historical revision rollback; observability tests prove high-impact bad revisions auto-rollback at the reliability threshold. The Android repository keeps the previous LKG when network or asset staging fails, and browser E2E proves an unavailable/killed experience does not remove the active-order recovery path.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value: `false`

Reason: APP-2026-016 requires runtime contract, E2E, and fuzz scenarios. It does not explicitly require staging deployment, provider sandbox access, or physical-device acceptance.

### External Runtime Validation

Status: NOT_RUN

Evidence: The E2E was run against a local Next server with controlled API responses. No staging or production result is claimed. Android emulator/device interactive verification is a release follow-up; Android unit/build verification is recorded above.

### Release Readiness

Status: PARTIAL

Evidence: Task-local behavior is complete. Promotion still needs authenticated staging/device smoke against the deployed experience manifest and asset infrastructure.

### Release Follow-ups

- Run the Chromium E2E-equivalent flow against authorized staging.
- Verify packaged-default, LKG, asset integrity/size, schedule, kill switch, and rollback on a representative Android emulator/device after deployment.
- Verify a remotely shown promo cannot authorize an ineligible voucher/order at the deployed transaction boundary.

## Locally Actionable Remaining

NONE

## Blocker Resolution Attempts

NONE. No original APP-2026-016 requirement is externally blocked; the initial Playwright startup issue was resolved by targeting the already-running local Next server.

## External Blockers

NONE

## Owner Action Required

NONE

## Unblock Condition

NONE

## Verification After Unblock

NONE for the original task. Release follow-ups will use the deployed staging URL and available Android runtime tooling when the release gate schedules them.

## Dependency Impact

`false` — ADMEXP-2026-001 is the next dependency-valid control-plane task and is not blocked by the separate staging release follow-up.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PASS`

Evidence: Commit `3944e3c6` adds the missing contract/fuzz and browser E2E coverage. Backend, Android, frontend, browser, and order-boundary verification all passed. Existing implementation tests prove schedule, rollback, kill switch, asset, renderer, and authoritative eligibility semantics.

### REALITY-2026-011 — No Fake Completeness

Status: `PASS`

Evidence: The test suite exercises real parser/resolver/cache/renderer and transaction validation code. Controlled browser responses are clearly local E2E fixtures, not staging/provider proof. No mock response is presented as real transaction, provider, or production behavior.

## Unproven / Remaining

NONE for the original APP-2026-016 requirements. Staging/device promotion validation remains a release follow-up only.

## Next Eligible Task

ADMEXP-2026-001

## Status Decision

All original APP-2026-016 requirements are proven and no locally actionable task work remains; status is COMPLETE.

## Notes / N/A Justification

The only N/A field is migration because this task changes tests and verification coverage only and does not change persistent schema or stored data.
