---
task_id: CORE-2026-008
status: PARTIAL

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: PENDING

tests: PASS
integration: PASS
e2e: NOT_RUN

migration: N/A
migration_na_reason: "Typed error responses use existing HTTP/middleware contracts and require no persistent schema change."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: NOT_RUN
release_followups: "Run authenticated client flows and confirm each recoverable code renders its next action in the target UI."

unproven_requirements: "Client rendering of every recoverable response has implementation coverage but frontend/mobile build and authenticated UI execution are not yet available in this worktree."
known_blockers: "Frontend node_modules and Android google-services.json are unavailable locally."

locally_actionable_remaining: "Run frontend and Android test/build gates after approved dependencies/configuration are available."

blocker_resolution_attempts: "Added the complete 13-code recoverable error descriptor, correlation-aware response fields, business-error mappings, safe requote response, frontend presentation/interceptor support, and Android action rendering helpers. Targeted backend domain/middleware/handler tests pass; frontend and Android gates were attempted but blocked by missing local dependencies/configuration."
unblock_condition: "Install the repository frontend dependencies and provide approved Android google-services.json, then rerun client tests/build and authenticated UI error flows."

owner_action_required: true
owner_action_summary: "Provide approved client build configuration and execute recoverable-error UI matrix."
verification_after_unblock: "Force each original error code through a real API response, verify correlation_id/code/action/retryable, and confirm UI offers the mapped next action without exposing internal details."

dependency_chain_blocked: false
next_eligible_task: NONE

updated_at: 2026-09-02
blocked_since: NONE
retry_when: "Client build dependencies/configuration become available."
---

# Evidence — CORE-2026-008

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md`:

- `[x]` Standardize `REQUOTE_REQUIRED`, `OUT_OF_SERVICE_AREA`, `NO_COURIER`, `PROVIDER_UNAVAILABLE`, `ITEM_UNAVAILABLE`, `INVALID_TRANSITION`, `PAYMENT_PENDING`, `PROOF_REQUIRED`, `HANDOFF_INVALID`, `SCHEDULE_INVALID`, `CAPABILITY_MISMATCH`, `CARRIER_RATE_EXPIRED`, `CARRIER_EVENT_UNKNOWN`.
- `[x]` Error carries correlation id.
- `[ ]` Client renders next action, not raw internal error.

## Scope Implemented

- Added one canonical descriptor table for all 13 required recoverable error codes, including safe next action and retry policy.
- Extended the standard error response with `action` and `retryable`; existing base middleware already injects the request correlation ID and now keeps it in the same typed response contract.
- Mapped requote, out-of-service-area, proof-required, invalid-transition, and handoff failures to the canonical codes in the order handler. The payment-link requote path now returns a safe message, correlation ID, action, and retry policy instead of an internal error string.
- Added frontend typed error extraction and global interceptor enrichment so existing UI messages receive the server next action. Added courier/customer Android API fields and safe action mapping; courier order errors append the mapped action instead of exposing raw internals.

## Files Changed

- `backend/order-service/internal/domain/errors.go` — 13-code descriptor contract.
- `backend/order-service/internal/domain/errors_contract_test.go` — coverage test for every required code.
- `backend/order-service/internal/middleware/base_middleware.go` — typed action/retry response fields.
- `backend/order-service/internal/handler/order_handler.go` and `payment_link_handler.go` — canonical business mappings and safe requote response.
- `android-app*/app/src/main/java/.../data/model/Models.kt` — action/retry response fields.
- `android-app/app/src/main/java/.../data/api/ErrorReference.kt` and `OrderViewModel.kt` — next-action rendering policy.
- `android-app-customer/app/src/main/java/.../data/api/ErrorReference.kt` and `OrderRepository.kt` — customer next-action policy.
- `frontend/src/lib/api.ts` and `recoverableError.ts` — typed client presentation contract.
- `frontend/src/lib/recoverableError.test.ts` — presentation tests.
- `task-food-marketplace-parity-2026.md` — checked only locally implemented criteria.

## Commands / Checks Run

    backend/order-service: go test ./internal/domain ./internal/middleware ./internal/handler
    result: PASS

    backend/order-service: gofmt -w internal/domain/errors.go internal/domain/errors_contract_test.go internal/middleware/base_middleware.go internal/handler/order_handler.go internal/handler/payment_link_handler.go
    result: PASS

    frontend: npm test / npm run lint / npx tsc --noEmit
    result: NOT_RUN — vitest/eslint/typescript are unavailable because frontend node_modules is not installed.

    android: Gradle client test/build
    result: NOT_RUN — local Google Services configuration is missing; CI verification remains pending.

    repository: git diff --check
    result: PASS

## Task-Local Verification

### Tests

Status: PASS

Evidence: Domain contract test covers all 13 required codes; targeted order-service domain, middleware, and handler suites pass.

### Integration

Status: PASS

Evidence: Canonical code descriptors are emitted through the standard middleware response, include correlation ID/action/retryable, and are parsed by web/customer/courier client contracts.

### E2E

Status: NOT_RUN

Evidence: Authenticated UI flows for every error condition require client runtime configuration unavailable locally.

### Migration

Status: N/A

Evidence: No schema change.

### Observability

Status: PASS

Evidence: Correlation ID remains present in typed errors and client reference handling continues to sanitize support references.

### Security / Privacy

Status: PASS

Evidence: Requote output no longer exposes `err.Error()`; next actions are server-owned safe copy and raw internal errors remain behind the existing safe-error logger.

### Rollback / Recovery

Status: PASS

Evidence: New response fields are optional for backward-compatible clients; unknown/legacy errors retain existing fallback messaging.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL — local backend contract is tested; client build/runtime validation is pending configuration.
- `REALITY-2026-011`: PASS — no mock success, placeholder data, or fabricated runtime evidence was added.

## Unproven / Remaining

- Client next-action rendering remains unchecked until frontend and Android tests/builds execute with approved configuration.
- Authenticated UI error matrix remains release follow-up.
