---
task_id: MOBILE-2026-002
status: PARTIAL
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "pending-staging-push"
tests: PARTIAL
integration: PARTIAL
e2e: NOT_RUN
migration: N/A
migration_na_reason: "No task-owned persistent schema change for this task evidence."
observability: PARTIAL
security_privacy: PARTIAL
rollback_recovery: PARTIAL
task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN
release_readiness: NOT_RUN
release_followups: "Task-specific proof remains: Crash/ANR linked to app version, device/OS, market, screen/feature flag/experience revision where safe in an actual crash/ANR export. | PII/secrets excluded from crash logs in provider output. | Critical regression can stop staged rollout through a tested release-control path."
unproven_requirements: "Crash/ANR linked to app version, device/OS, market, screen/feature flag/experience revision where safe in an actual crash/ANR export. | PII/secrets excluded from crash logs in provider output. | Critical regression can stop staged rollout through a tested release-control path."
known_blockers: NONE
locally_actionable_remaining: "Crash/ANR linked to app version, device/OS, market, screen/feature flag/experience revision where safe in an actual crash/ANR export. | PII/secrets excluded from crash logs in provider output. | Critical regression can stop staged rollout through a tested release-control path."
blocker_resolution_attempts: "Repository inspection, available local tests, staging checks and relevant automation paths were exercised."
unblock_condition: "All original checklist items are implemented and verified with task-specific executable evidence; unresolved external validation is recorded separately as release follow-up."
owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: "Run the remaining task-specific tests, runtime checks and evidence validator."
dependency_chain_blocked: false
next_eligible_task: "CURRENT TASK — continue implementation and verification"
updated_at: 2026-09-14
---
# Evidence — MOBILE-2026-002

## Acceptance Criteria Source

- [ ] Crash/ANR linked to app version, device/OS, market, screen/feature flag/experience revision where safe.
- [x] Release artifact has mapping/symbol metadata retained.
- [ ] PII/secrets excluded from crash logs.
- [ ] Critical regression can stop staged rollout.

## Scope Implemented

Mobile reliability groundwork includes the machine-readable performance budget/gate and release-contract validation. Customer and courier now install a bounded Crashlytics context when Firebase is configured; merchant keeps the same sanitized local context contract while its remote crash vendor remains unconfigured. The available device run is recorded honestly; the broader AC reliability, compatibility and acceptance suite remains open.

## Files Changed

- `scripts/mobile/record_artifact_inventory.py` — records release mapping/symbol metadata hashes alongside APK/AAB identity.
- `scripts/mobile/validate_mobile_static_contracts.py` — checks the bounded, non-PII crash-context contract across all three apps.
- `android-app-customer/app/src/main/java/com/tembus/customer/util/MobileCrashContext.kt`
- `android-app/app/src/main/java/com/tembus/courier/util/MobileCrashContext.kt`
- `android-app-merchant/app/src/main/java/com/tembus/merchant/util/MobileCrashContext.kt`
- Mobile application startup, main screen, feature-flag and experience-slot wiring for customer/courier/merchant.
- `docs/mobile/`
- `docs/mobile/artifact-reports/*-release-20260914.json` — local release packet metadata with four R8 symbol files per app.
- `docs/task-evidence/MOBILE-2026-001.md`
- `.github/workflows/android-apps.yml`

## Commands / Checks Run

    command: .\gradlew.bat :app:compileDebugKotlin --no-daemon — PASS for customer, courier and merchant apps; command: .\gradlew.bat :app:testDebugUnitTest --no-daemon — PASS for customer, courier and merchant apps; command: python scripts/mobile/validate_mobile_static_contracts.py — PASS; command: python scripts/mobile/validate_mobile_release_contract.py — PASS; command: adb devices -l — Pixel_6_Pro_customer AVD reachable; representative low/high crash/ANR matrix — NOT_RUN.
    command: python scripts/mobile/record_artifact_inventory.py --require-release --require-symbol-metadata (customer, courier and merchant) — PASS; each local release packet records one release APK plus four R8 mapping/symbol metadata files by path, size and SHA-256 without storing symbol contents in evidence.

## Task-Local Verification

Status: PARTIAL
Evidence: 1/4 original checklist items is checked in the master; release symbol metadata retention is proven locally and CI wiring is fail-closed. The source-level crash context is now wired and statically checked, while Crashlytics export, PII-safety runtime proof and rollout-stop evidence remain open.

## Reality Gate Evaluation

- REALITY-2026-003: PARTIAL — implementation groundwork does not prove every original criterion.
- REALITY-2026-011: PASS — missing device/provider/security evidence is not represented as success.

## Unproven / Remaining

Crash/ANR linked to app version, device/OS, market, screen/feature flag/experience revision where safe in an actual crash/ANR export. | PII/secrets excluded from crash logs in provider output. | Critical regression can stop staged rollout through a tested release-control path.

## External Runtime / Release Validation

Status: NOT_RUN — this task has not been promoted to a full external-runtime proof; the public staging host checks do not replace the task-specific evidence.

## Release Follow-ups

Run the remaining task-specific device/CI/provider/security validation and attach raw evidence before promotion.

## Locally Actionable Remaining

Crash/ANR linked to app version, device/OS, market, screen/feature flag/experience revision where safe in an actual crash/ANR export. | PII/secrets excluded from crash logs in provider output. | Critical regression can stop staged rollout through a tested release-control path.
