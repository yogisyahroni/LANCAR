---
task_id: AGG-2026-012
status: PARTIAL
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 3ba6ba12
tests: PASS
e2e_staging: NOT_RUN
migration: NOT_RUN
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Runtime-configurable status mapping, staging migration execution, live provider payload verification, and production observability/rollback drills remain unproven."
known_blockers: "Docker, staging database, and external provider sandbox are unavailable in this workspace."
locally_actionable_remaining: "Implement runtime-configurable status mapping and local migration/observability/rollback checks."
unblock_condition: "Complete local mapping/configuration work, then run staging migration and live provider verification."
dependency_chain_blocked: false
next_eligible_task: NONE
updated_at: 2026-09-01
---

# Evidence — AGG-2026-012

## Scope Implemented

- Added explicit provider-native status fields to the carrier event contract and database (`provider_status`, code, description, location, and timestamp), while retaining raw aliases for backward compatibility.
- Added a forward-compatible migration with backfill from the existing `raw_*` and `occurred_at` columns.
- Provider adapters now preserve native values and map them to the canonical LANCAR target set at the provider boundary; mapping is provider-specific and tested, but runtime configurability remains pending.
- Order-service normalizes legacy payload aliases, persists both provider-native and canonical values, and keeps unknown canonical states as `UNKNOWN` without lifecycle advancement.
- Customer order detail and receipt responses expose safe carrier updates scoped to the authenticated customer's own order; raw payload is never returned. The web UI shows friendly canonical status plus provider status, code, description, location, and timestamp.

## Files Changed

- `database/migrations/20260901000003_carrier_event_provider_fields.sql` — explicit provider-native storage and compatibility backfill.
- `backend/integration-gateway/internal/domain/logistics_webhook.go` — canonical transport fields.
- `backend/integration-gateway/internal/provider/status_mapper.go` — provider-boundary canonical mapping.
- `backend/integration-gateway/internal/provider/status_mapper_test.go` — mapping, field preservation, and unknown-state tests.
- `backend/integration-gateway/internal/provider/jne_webhook.go`
- `backend/integration-gateway/internal/provider/jnt_webhook.go`
- `backend/integration-gateway/internal/provider/webhook_registry.go`
- `backend/integration-gateway/internal/handler/tracking_webhook_handler.go`
- `backend/order-service/internal/domain/carrier_event.go`
- `backend/order-service/internal/handler/delivery_webhook_handler.go`
- `backend/order-service/internal/repository/carrier_event_repository.go`
- `backend/order-service/internal/service/carrier_event_service.go`
- `backend/admin-service/src/controllers/order/customerOrder.controller.ts` — authenticated web detail carrier event projection.
- `backend/admin-service/src/controllers/order/customerOrderTracking.controller.ts` — authenticated mobile tracking detail carrier event projection.
- `frontend/src/app/(portal)/orders/[id]/OrderDetailContent.tsx` — friendly provider update panel.
- `frontend/src/app/(portal)/orders/[id]/useOrderDetailRuntime.ts`
- `frontend/src/app/(portal)/orders/[id]/orderDetailTypes.ts`
- `frontend/src/app/(portal)/orders/[id]/page.tsx`
- `frontend/src/app/(portal)/resi/[id]/page.tsx` — receipt provider update section.

## Commands / Checks Run

```text
command: go test ./... (backend/integration-gateway)
result: PASS — all packages, including provider mapping and preservation tests.

command: go test ./... (backend/order-service)
result: PASS — all packages, including carrier event lifecycle tests.

command: npm run build (backend/admin-service)
result: PASS.

command: npm run build (frontend)
result: PASS.

command: npm test (frontend)
result: PASS — 4 files, 9 tests.

command: npm test (backend/admin-service)
result: PASS — 38 suites, 202 tests.

command: npm run lint (frontend)
result: PASS — 0 errors; existing warnings remain.

command: git diff --check
result: PASS.

command: graphify update .
result: PASS — graph updated; known SQL parser/large visualization warnings remain.

command: Docker migration / provider sandbox / staging E2E
result: NOT RUN — unavailable in this workspace.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: provider-native and canonical fields are wired through adapter, internal delivery handler, database repository, authenticated customer API, and web receipt/detail UI.
- tests: mapping tests reject unknown-state guessing; Go, TypeScript builds, frontend tests/lint, and admin-service tests pass.
- E2E/staging: not run because Docker, staging database, and provider sandbox are unavailable.
- migration/backfill: migration is authored and includes compatibility backfill, but has not been executed against a staging database.
- observability: existing structured logs remain in the webhook path; production metrics, dashboards, and alert behavior were not verified.
- security/privacy: customer carrier events are selected through the authenticated order ownership predicate and raw payload is excluded from responses.
- rollback/recovery: migration has a down path and code is backward-compatible with raw aliases, but rollback/replay drills were not run.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

Confirm with evidence, not assumptions:

- unknown provider status remains `UNKNOWN` and does not advance order lifecycle;
- staging, provider sandbox, migration execution, and production operational evidence are explicitly not claimed;
- no provider response or database result was fabricated.

## Unproven / Remaining

- Move status mapping rules into a runtime/config-backed provider mapping contract and test it end to end.
- Execute migration and customer detail verification in staging with real carrier event payloads.
- Validate JNE/J&T native code mappings against provider sandbox contracts.
- Add production dashboard/alert verification and run rollback/replay drills.

## Blockers

Docker, staging database, and external provider sandbox access are unavailable in this workspace.

## Notes / N/A Justification

Migration is not `N/A`: the migration file is part of the implementation, but execution is `NOT_RUN` because no staging database is available.
