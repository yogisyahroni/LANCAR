---
task_id: UX-2026-002
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 31c46c14
tests: PASS
e2e: NOT_RUN
migration: N/A
migration_na_reason: "The order-detail shell and typed UI sections reuse the existing Order model and require no schema or persisted-data migration."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PASS
unproven_requirements: "Android device/staging detail walkthroughs for Food, roadside, package, unknown state, and real recovery data remain unproven."
known_blockers: "No connected Android device/staging session with seeded multi-service customer orders was available for this local implementation turn."
locally_actionable_remaining: NONE
unblock_condition: "Provide an authenticated Android device/emulator staging session with seeded multi-service orders, then verify Food variants, roadside/package detail, unknown state, actions, and retry recovery."
dependency_chain_blocked: true
next_eligible_task: NONE
updated_at: 2026-09-01
---

## Scope Implemented

- Preserved one customer order-detail shell while moving service-specific content into `OrderDetailSections.kt` for Food, roadside, and generic/package orders.
- Added `OrderActionPolicy` as the single state/subtype-aware policy for tracking, chat, cancellation, and human-readable status labels.
- Unknown order states now use a safe “Status sedang diperbarui” label and do not expose tracking/chat/cancel shortcuts unless the state is explicitly supported.
- Detail-load errors now provide a visible retry action instead of only rendering raw error text.
- Existing Food item snapshots and variant/notes rendering remain server-derived inside the typed Food section.
- Extracted and tested the section-kind resolver for Food, roadside, and package orders; normalized action/status policies now tolerate API whitespace and remain safe for unknown and terminal states.

## Files Changed

- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderDetailScreen.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderDetailSections.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderActionPolicy.kt`
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/detail/OrderDetailPolicyTest.kt`

## Commands / Checks Run

- `./gradlew :app:compileDebugKotlin --no-daemon` (Windows equivalent `gradlew.bat`) — PASS, `BUILD SUCCESSFUL`; existing Kotlin deprecation/unused warnings remain.
- `./gradlew.bat --no-daemon --no-watch-fs --max-workers=1 --console=plain :app:testDebugUnitTest --tests com.tembus.customer.ui.screens.detail.OrderDetailPolicyTest :app:compileDebugKotlin` — PASS, 3 order-detail policy tests / 0 failures; `BUILD SUCCESSFUL`.
- `git diff --check` — PASS before commit.
- `py scripts/tasks/validate_task_evidence.py` — pending until this evidence file is committed.
- `graphify update .` — commit hook launched a background rebuild; explicit CLI refresh has the known non-termination after AST extraction.

## Reality Gate Evaluation

- `REALITY-2026-003`: BLOCKED. The shared shell, typed sections, action policy, and local tests are real; runtime device/staging data coverage is not captured and no local actionable work remains.
- `REALITY-2026-011`: PASS. Unknown states remain degraded-safe, and section content comes from the existing server-backed `Order` model without placeholder business facts.

## Unproven / Remaining

- Install/run on an Android device and verify Food variants, roadside detail, package detail, and unknown status rendering.
- Verify action visibility for pending, active, terminal, failed, and unknown states against server responses.
- Verify pull-to-refresh and retry behavior after an authenticated network failure.
- Capture runtime evidence without recording customer PII or full addresses.

Local implementation and automated coverage is complete; no local actionable work remains.
