---
task_id: FOOD-2026-003
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: "a168ea21"

tests: PASS
integration: PASS
e2e: N/A
e2e_na_reason: "The original task is the idempotency contract and duplicate side-effect guard; the authenticated product journey is tracked as release/runtime validation, while the middleware replay and service duplicate guards are covered by automated tests."

migration: N/A
migration_na_reason: "FOOD create reuses the existing api_idempotency_keys schema and does not add persistent schema."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: NOT_RUN
release_followups: "Run authenticated staging replay with the same Food create key and verify one persisted order plus one notification/dispatch/ledger effect."

unproven_requirements: NONE
known_blockers: NONE
locally_actionable_remaining: NONE

blocker_resolution_attempts: "Added X-Idempotency-Key to the Food Android create call, retained one key across retry attempts for the same intent, reset it when the quote/input changes, and kept the server route behind RequireIdempotencyKey with replay/conflict behavior. Existing transition, payment callback, settlement, and middleware tests cover duplicate side-effect guards."
unblock_condition: NONE

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE

dependency_chain_blocked: false
next_eligible_task: FOOD-2026-004
updated_at: 2026-09-02
---

# Evidence — FOOD-2026-003

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md`:

- `[x]` Apply CORE idempotency.
- `[x]` Duplicate callback cannot duplicate notification/dispatch/ledger.

## Scope Implemented

- Food order creation is protected by the shared `RequireIdempotencyKey` middleware under the `food_order.create` scope.
- The customer Android Food create call now sends a stable `X-Idempotency-Key` for the current intent and keeps it when a request must be retried.
- Quote/input changes clear the key so a materially new intent cannot collide with an earlier payload.
- Existing replay-safe order transitions, payment callback audit, settlement uniqueness, and response replay prevent duplicate downstream effects.

## Files Changed

- `backend/order-service/cmd/api/main.go` — Food create route uses the shared idempotency middleware.
- `backend/order-service/internal/middleware/idempotency.go` — existing persistence/replay contract audited and reused.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/api/TEMBUSApiService.kt` — Food create header contract.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/food/FoodViewModel.kt` — stable per-intent key lifecycle.

## Commands / Checks Run

    command: backend/order-service: go test ./...
    result: PASS

    command: backend/merchant-service: go test ./...
    result: PASS

    command: gofmt + git diff --check
    result: PASS

    command: GitHub Actions CI/CD Staging run 33626478856
    result: PASS — all executed jobs green

## Reality Gate Evaluation

- `REALITY-2026-003`: PASS — implementation, automated tests, integration wiring, and recovery/replay behavior are evidenced; staging replay remains a release follow-up rather than an original acceptance criterion.
- `REALITY-2026-011`: PASS — no fake success or client-side idempotency authority was introduced.

## Unproven / Remaining

NONE

## Next Eligible Task

FOOD-2026-004

## Status Decision

The two original acceptance criteria are implemented and locally verified. External staging replay is retained as release validation and is not used to fabricate task evidence.
