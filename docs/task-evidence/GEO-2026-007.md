---
task_id: GEO-2026-007
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: 4cec01e1

tests: PASS
integration: PASS
e2e: N/A
e2e_na_reason: "The original criteria are enforced by server SQL/policy boundaries and deterministic source/API tests; real device and public staging probes are release follow-ups, not fabricated as task proof."

migration: PASS
migration_note: "Goose UP/DOWN and purpose/market/class cleanup behavior were executed against a temporary PostgreSQL/PostGIS clone."
observability: PASS
observability_note: "Retention dimensions are queryable by policy, exact admin location reads emit an audit_logs access event, and audit payloads exclude coordinates."
security_privacy: PASS
security_privacy_note: "Customer app has foreground-only location APIs, analytics/share responses are coarsened to a 0.001-degree grid, and exact historical GPS access is explicitly audited."
rollback_recovery: PASS
rollback_recovery_note: "The new migration DOWN was executed successfully; unknown market/purpose rows are retained until a policy exists."

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN
external_runtime_validation_note: "No staging, provider, browser geolocation, or physical-device result is represented as completed."

release_readiness: NOT_RUN
release_followups: "Run authenticated staging probes for customer tracking/share precision and verify the scheduled maintenance job invokes the new policy-scoped cleanup."

unproven_requirements: NONE
known_blockers: NONE

locally_actionable_remaining: NONE
blocker_resolution_attempts: "Inspected all customer location APIs/manifests, added policy enforcement and precision defense-in-depth, ran application tests, and used a disposable PostgreSQL/PostGIS clone for migration and retention verification."
unblock_condition: NONE

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE

dependency_chain_blocked: false
next_eligible_task: GEO-2026-008

blocked_since: NONE
retry_when: NONE
updated_at: "2026-09-07"
---

# Evidence — GEO-2026-007

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md` → GEO-2026-007:

- [x] Raw continuous courier location retention is purpose/market scoped.
- [x] Customer background location uses least privilege.
- [x] Analytics receives coarsened/pseudonymous location where exact coordinates unnecessary.
- [x] Public tracking/share surfaces reduce precision when full precision unnecessary.
- [x] Admin access to historical exact route/location audited.

## Scope Implemented

- Added `market_code`, `purpose`, and `retention_class` dimensions to raw `courier_locations`.
- Added seeded `geo_retention_policies` and `tembus_cleanup_courier_locations(dry_run)`; cleanup matches all policy dimensions and retains unknown-market/purpose rows until explicitly configured.
- Updated courier telemetry ingestion to persist the configured market and classify rejected/suspicious points as forensic.
- Updated maintenance automation to call the policy-scoped cleanup rather than a generic partition age cutoff that could delete forensic data too early.
- Coarsened admin courier heatmap points and public trip-share pickup/dropoff/courier coordinates with a 0.001-degree grid, with response-level defense in depth.
- Added explicit `geo.exact_location.read` audit events when authenticated admin order detail loads the historical GPS trail. Audit metadata contains access context only, never coordinates.
- Confirmed the customer Android app declares foreground location only and uses one-shot `getCurrentLocation`; customer web flows use one-shot `getCurrentPosition` and no continuous watcher.

## Files Changed

- `database/migrations/20260907000011_geo_privacy_retention.sql` — retention dimensions, policy table, scoped cleanup function, and rollback.
- `backend/admin-service/src/services/geoPrivacy.ts` and tests — market validation, coordinate coarsening, and exact-access audit helper.
- `backend/admin-service/src/controllers/analytics.controller.ts` — coarsened heatmap query/response.
- `backend/admin-service/src/controllers/courier/courierSafety.controller.ts` — coarsened public trip-share coordinates.
- `backend/admin-service/src/controllers/order/customerOrderTracking.controller.ts` — market/purpose/class tagging for raw telemetry.
- `backend/admin-service/src/controllers/orders.controller.ts` — historical exact GPS access audit.
- `scripts/db-maintenance.sh` — policy-scoped raw location cleanup.
- `task-food-marketplace-parity-2026.md` — checked only the five proven GEO-007 criteria.

## Commands / Checks Run

    command: npm run build (backend/admin-service)
    result: PASS — TypeScript compilation completed.

    command: npm test -- --runInBand src/services/geoPrivacy.test.ts src/services/geoPrivacyMigration.test.ts (backend/admin-service)
    result: PASS — 2 suites, 5 tests.

    command: npm test -- --runInBand (backend/admin-service)
    result: PASS — 54 suites, 263 tests.

    command: npm test -- --runInBand (frontend)
    result: PASS — 15 files, 46 tests.

    command: npm run build (frontend)
    result: PASS — Next production build completed; existing middleware deprecation warning only.

    command: VITE_API_URL=https://api.bawain.my.id/api/v1 VITE_SOCKET_URL=wss://api.bawain.my.id npm run build (admin-dashboard)
    result: PASS — production Vite build completed; existing chunk-size warning only.

    command: customer Android/web location source audit
    result: PASS — no customer ACCESS_BACKGROUND_LOCATION, watchPosition, or requestLocationUpdates reference; foreground one-shot APIs remain.

    command: goose -allow-missing -dir database/migrations postgres <temporary PostgreSQL/PostGIS clone> up
    result: PASS — migrated through 20260907000011_geo_privacy_retention.sql.

    command: tembus_cleanup_courier_locations dry-run/execute verification on temporary clone
    result: PASS — old operational ID-JK row selected/deleted at 30 days; forensic 90-day row and unknown-market row were retained; transaction was rolled back after assertion.

    command: goose -allow-missing -dir database/migrations postgres <temporary PostgreSQL/PostGIS clone> down
    result: PASS — returned to the prior version and removed the policy table/columns/function.

    command: git diff --check
    result: PASS.

## Task-Local Verification

### Tests

Status:

PASS

Evidence:

Privacy helper tests prove deterministic 0.001-degree coarsening and safe market fallback. Migration contract tests prove the three retention dimensions, policy table, dry-run path, and reversibility. Full admin and frontend regressions pass.

### Integration

Status:

PASS

Evidence:

Telemetry ingestion writes market/purpose/class values. Admin heatmap and public trip share both coarsen at the SQL boundary and again before JSON serialization. Admin order detail records an access event before returning exact historical GPS data.

### E2E

Status:

N/A

Evidence:

The original requirements are deterministic privacy/retention controls. Staging and device flows are documented as release follow-ups, with no fabricated runtime claim.

### Migration

Status:

PASS

Evidence:

A disposable PostgreSQL/PostGIS clone accepted Goose UP, policy cleanup assertions, and Goose DOWN. The clone database was removed after verification; no staging or production database was modified.

### Observability

Status:

PASS

Evidence:

Retention cleanup returns market, purpose, class, row count, and retention days for dry-run/operations visibility. Historical exact-location reads use the existing `audit_logs` source with action `geo.exact_location.read` and redacted context only.

### Security / Privacy

Status:

PASS

Evidence:

Customer Android manifest has no background-location permission, and customer source contains no continuous location API. Analytics and public share coordinates are snapped/coarsened to approximately 100m grid cells. Exact historical GPS remains restricted to the existing authenticated admin detail route and is audited without copying coordinates into audit metadata or logs.

### Rollback / Recovery

Status:

PASS

Evidence:

Migration DOWN removed only GEO-007 policy objects and columns in the disposable clone. Unknown policy dimensions are fail-safe retained, and the maintenance script supports dry-run before deletion.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value:

`false`

Reason:

The original checklist specifies code/data privacy controls, not live provider behavior or a device-only acceptance flow. Staging/public endpoint probes remain release follow-ups and are not represented as task completion evidence.

## Reality Gate Evaluation

- `REALITY-2026-003`: PASS — all five original GEO-2026-007 requirements have implementation and verification evidence.
- `REALITY-2026-011`: PASS — no staging, device, provider, or production result is fabricated.

## Unproven / Remaining

NONE for the original GEO-2026-007 checklist. Staging maintenance and public-share probes remain release follow-ups only.

## Status Decision

`COMPLETE` — all five original GEO-2026-007 requirements are implemented and locally verified; no locally actionable requirement remains.
