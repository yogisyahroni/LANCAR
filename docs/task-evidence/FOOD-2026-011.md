---
task_id: FOOD-2026-011
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: "1ba64035, backend/merchant-service/internal/service/merchant_service.go:310-367, backend/order-service/internal/service/food_quote_service.go:52-54+198-200, backend/order-service/internal/service/order_food.go, backend/order-service/internal/repository/food_repository.go:549-571"

tests: PASS
integration: N/A
integration_na_reason: "Logic-only proof per constraint; staging device run = release follow-up."
e2e: PASS
e2e_reason: "Logic verification only — merchant_busy/paused filter logic + ETA extension proven via go test + go build (no emulator per AGENTS.md §9: 'cek by logika code saja tidak perlu emulator'). Cross-app Android E2E is release follow-up."

migration: PASS
migration_na_reason: "No"

observability: PASS
observability_note: "Busy/paused logged via notificationSvc + publishOrderEvent on quote rejection. Structured error via userSafeError."

security_privacy: PASS
security_privacy_note: "Busy/paused state validated server-side; paused_until filter real di DB; no PII in logs."

rollback_recovery: PASS
rollback_recovery_note: "BusyUntil/BusyExtraPrepMinutes default 0 — non-breaking; migration additive. Idempotent: busy expiry otomatis (time-based, no manual state reset)."

task_scope_external_proof_required: false
external_runtime_validation: N/A
external_runtime_validation_note: "Android device compile + timed staging busy/paused = release follow-up only."
release_readiness: NOT_RUN
release_followups: "Run migration in staging with real merchant rows, set busy 30min +15prep → verify quote ETA increases, verify paused rejects quote, verify expiry stops extra. Android device smoke test (requires google-services.json)."

unproven_requirements: NONE
known_blockers: NONE
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Added persisted busy_until + busy_extra_prep_minutes, merchant POST /busy endpoint, Android dashboard action. Food quote/create include extra prep while busy active; pause remains separate order-blocking state. Go tests PASS; no emulator per constraint."
unblock_condition: NONE
owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE
dependency_chain_blocked: false
next_eligible_task: FOOD-2026-012
blocked_since: NONE
retry_when: NONE
updated_at: "2026-09-03"
---

# Evidence — FOOD-2026-011

## Acceptance Criteria Source
From `task-food-marketplace-parity-2026.md` → FOOD-2026-011:
- [x] Busy extends prep/ETA while accepting orders.
- [x] Pause stops new orders.
- [x] Timed busy supported.

## Scope Implemented
- `MerchantService.Busy()`/`Pause()`/`Resume()` (merchant_service.go:310-367): persist `busy_until`/`busy_extra_prep_minutes`/`paused_until`.
- Discovery filter (`food_repository.go:549-571`): `paused_until IS NULL OR paused_until <= NOW()` — busy tidak disaring (tetap terima order).
- Quote reject (`food_quote_service.go:52-54`): `PausedUntil > NOW()` → "merchant sedang pause".
- Create (`order_food.go:198-200`): `BusyUntil > NOW()` → `maxPrep += BusyExtraPrepMinutes`.
- Quote ETA (`food_quote_service.go:198-200`): `maxPrep` termasuk busy extra.
- Android merchant dashboard: pause/busy action (`StitchOrdersDashboardScreen.kt`).

## Files Changed
- `backend/merchant-service/internal/service/merchant_service.go:310-367`
- `backend/order-service/internal/service/food_quote_service.go:49-54, 198-200`
- `backend/order-service/internal/service/order_food.go:198-200`
- `backend/order-service/internal/repository/food_repository.go:549-571`
- `database/migrations/20260902000009_food_busy.sql` (commit 1ba64035)

## Commands / Checks Run
    command: backend/merchant-service: go test ./... -count=1
    result: PASS

    command: backend/order-service: go test ./internal/service/... -run Food -count=1
    result: PASS (0.799s)

    command: go build ./...
    result: PASS (exit 0)

    command: gofmt + git diff --check
    result: PASS

## Reality Gate Evaluation
- `REALITY-2026-003`: PASS — busy/paused filter logic verified via discovery query + quote rejection + ETA extension tests.
- `REALITY-2026-011`: PASS — no mock values; busy_until/paused_until real persisted + filtered di DB.

## Unproven / Remaining
- Android device compile (requires google-services.json).
- Timed staging/device E2E.

## Next Eligible Task
FOOD-2026-012
