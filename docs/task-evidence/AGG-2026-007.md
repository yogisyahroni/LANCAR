---
task_id: AGG-2026-007
status: PARTIAL
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: df6a8ee3
tests: PASS
e2e_staging: NOT_RUN
migration: NOT_RUN
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Unknown canonical status is stored and logged safely, but customer UI rendering of a generic safe state is not yet proven; staging/provider replay and migration execution were not run."
known_blockers: "Docker and provider sandbox are unavailable in this workspace."
updated_at: 2026-09-01
---

# Evidence — AGG-2026-007

## Scope Implemented

- Added a raw-first carrier event inbox with provider/event and payload-hash uniqueness.
- Gateway normalization now carries event ID/hash, canonical status, raw status/code/description/location, occurrence time, and full raw payload.
- Order-service persists the inbox event before settlement/lifecycle handling, ignores duplicate replay, and guards against out-of-order state regression.
- Unknown provider states map to `UNKNOWN` and remain observable through their raw fields and structured warning logs.

## Files Changed

- `backend/integration-gateway/internal/handler/tracking_webhook_handler.go` — event identity/hash, raw field preservation, canonical mapping, and safe unknown mapping.
- `backend/integration-gateway/internal/handler/tracking_webhook_handler_test.go` — normalization/raw truth tests.
- `backend/order-service/internal/domain/carrier_event.go` — event contract and inbox port.
- `backend/order-service/internal/repository/carrier_event_repository.go` — idempotent raw-first PostgreSQL insert.
- `backend/order-service/internal/service/carrier_event_service.go` — replay and ordering guards plus canonical lifecycle mapping.
- `backend/order-service/internal/service/carrier_event_service_test.go` — unknown mapping and ordering tests.
- `backend/order-service/internal/handler/delivery_webhook_handler.go` — inbox processing before settlement.
- `backend/order-service/cmd/api/main.go` — event repository/service wiring.
- `database/migrations/20260901000002_carrier_event_inbox.sql` — inbox schema, dedupe constraints, and indexes.
- `task-food-marketplace-parity-2026.md` — four proven checklist items marked complete.

## Commands / Checks Run

```text
command: go test ./... (backend/order-service)
result: PASS — all order-service packages passed.

command: go test ./... (backend/integration-gateway)
result: PASS — all integration-gateway packages passed.

command: git diff --check
result: PASS.

command: docker compose / staging provider replay
result: NOT RUN — Docker executable and provider sandbox are unavailable.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: Raw-first inbox and event consumer are wired into the existing gateway → order-service webhook path.
- tests: Normalization preserves raw values, unknown values remain unmapped, and ordering guard tests pass; both Go modules pass.
- E2E/staging: Not run because Docker/provider sandbox is unavailable.
- migration/backfill: Migration is written and reversible but not executed against staging PostgreSQL.
- observability: Unknown AWBs/statuses emit structured warning logs; production metric/trace evidence is unavailable.
- security/privacy: Existing HMAC gateway verification and internal API key remain in the path; raw payload is restricted to internal inbox processing.
- rollback/recovery: Duplicate replay is safe and migration has a down path; provider retry/replay drill is not run.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

Confirm with evidence, not assumptions:

- the UI generic-state requirement remains unchecked;
- no staging/provider result is fabricated;
- raw provider values are retained alongside canonical values;
- event acceptance is not treated as proof of successful lifecycle progression.

## Unproven / Remaining

- Render unknown carrier states as a safe generic state in customer order/resi screens without guessing a lifecycle status.
- Execute migration and provider replay/idempotency/out-of-order scenarios in staging.
- Add production trace/metric evidence for inbox lag, duplicate count, and unknown-status count.

## Blockers

Docker and provider sandbox access are unavailable in this workspace.

## Notes / N/A Justification

No field is marked `N/A`; unavailable verification remains `NOT_RUN` or `PARTIAL`.
