---
task_id: A11Y-2026-012
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: dbdb8619 — explicit landing tracking-input focus indicator and regression test.
tests: PASS
integration: N/A
integration_na_reason: "The matrix documents browser/manual acceptance boundaries; no service contract changed."
e2e: PASS
e2e_note: "The full current Customer accessibility/reflow/keyboard/runtime suite passes 270/270 in Chromium, including the new landing tracking-input focus regression cases; the current registered route/mobile inventory passes Customer 64/64 across 31 routes and Admin 142/142; current Customer reflow/text-spacing inventory passes 93/93 and Admin reflow/text-spacing remains 140/140 plus 70/70; focused Customer keyboard/runtime cases pass 16/16 and Admin keyboard/theme/state cases pass 27/27; the rebuilt Docker runtime passes Customer 266/266 and Admin 470/470; Firefox/WebKit Customer inventory passes 128/128; shared-logo contrast guard passes 2/2; manual tunnel spot checks are recorded below, while remaining manual/provider rows stay PARTIAL/NOT_RUN."
migration: N/A
migration_na_reason: "No persistent schema or stored-data change."
observability: N/A
observability_na_reason: "No runtime telemetry contract changed."
security_privacy: N/A
security_privacy_na_reason: "No data or permission boundary changed."
rollback_recovery: N/A
rollback_recovery_na_reason: "Documentation and browser fixtures are revertible without data recovery."
task_scope_external_proof_required: true
external_runtime_validation: PARTIAL
release_readiness: BLOCKED
release_followups: "Complete the remaining dated manual Light/Dark route-state rows, native assistive-technology output, actual browser zoom and authenticated Admin/Customer review. Keep OTP, real payment-provider and provider-backed campaign rows deferred until the external coordination is complete."
unproven_requirements: "Original manual checklist rows are not all proven: full Light/Dark route coverage, complete keyboard-only primary-action traversal, native screen-reader output, actual browser zoom, authenticated Customer/Admin route-state review and dynamic provider/campaign states remain open; OTP/payment-provider flows are deferred per owner instruction."
known_blockers: "The available in-app browser can provide tunnel screenshots, AX trees and keyboard input but cannot provide native spoken screen-reader output or a controllable real Light-mode session. Authenticated Customer/Admin manual routes require an authorized staging account/session that was not available. OTP, real payment-provider and provider-backed campaign checks require the explicitly deferred external coordination. GitHub staging SSH deployment/validation remains unconfigured, so the current-commit workflow deploy and authenticated validation jobs cannot run."
locally_actionable_remaining: "NONE within the available non-credential browser/desktop surface; remaining original rows require native assistive-technology access, an authorized authenticated session, a real Light/zoom review surface or deferred provider coordination."
blocker_resolution_attempts: "Checked the available tool inventory and used the Codex in-app browser against the public Customer/Admin tunnels; manually opened Customer public/auth/error routes and Admin login, captured AX trees/screenshots, exercised empty-submit validation, tracked a fake invalid resi without credentials, completed a 22-stop Customer Tab traversal and Admin login Tab traversal, and verified the repaired tracking-input focus indicator after Docker rebuild. Confirmed Narrator.exe exists but no native speech-output bridge; corrected ANDROID_HOME/ANDROID_SDK_ROOT, booted Pixel_5_kurir, enabled TalkBack, inspected Chrome WebView semantics and captured a mobile screenshot; ran Chromium 270/270, Customer Docker 266/266, Admin Docker 470/470, Firefox/WebKit 128/128 and the focused keyboard/runtime suites; fixed the discovered logo contrast and landing tracking-input focus defects; checked GitHub staging secrets/variables, which returned no configured deployment/validation entries."
unblock_condition: "Provide an authorized non-secret test session for authenticated routes, a native screen-reader/manual Light/actual-zoom review surface, and complete the agreed external OTP/payment/provider coordination; then record dated PASS/FAIL results for every applicable matrix row with no unresolved P0 accessibility defect."
owner_action_required: true
owner_action_summary: "Provide an authorized staging test session or run the remaining manual reviewer rows with a real Light/Dark browser, actual zoom and native screen reader; configure GitHub staging deployment/validation secrets if current-commit CI deployment is required; coordinate OTP/payment-provider checks separately. Do not send secrets through chat or commit them."
verification_after_unblock: "Re-run the Customer/Admin automated matrices, execute the remaining dated manual matrix rows on the authorized staging session, verify native screen-reader announcements and actual 200% zoom, complete deferred provider checks when coordinated, then run `python scripts/tasks/validate_task_evidence.py`."
dependency_chain_blocked: false
next_eligible_task: NONE
updated_at: 2026-09-11
---

# Evidence — A11Y-2026-012

## Scope Implemented

Created and updated an explicit manual route/state matrix for all 31 Customer and 80 Admin registered routes. The matrix separates local automated proof from checks requiring keyboard-only completion, screen-reader review, visual effective-background inspection, modal/dropdown/toast interaction, 200% zoom and dynamic campaign/provider states. The device Docker/tunnel runtime was also manually spot-checked, and a real Customer focus defect found during that review was fixed and regression-tested.

## Files Changed

- `docs/accessibility/manual-audit-matrix.md`
- `docs/accessibility/wcag-2.1-aa-web-checklist.md`
- `frontend/e2e/accessibility.spec.ts`
- `admin-dashboard/e2e/accessibility.spec.ts`
- `frontend/e2e/reflow.spec.ts`
- `admin-dashboard/e2e/reflow.spec.ts`
- `frontend/src/components/ui/Skeleton.tsx` — valid polite loading status semantics for the Customer page skeleton.
- `admin-dashboard/e2e/visual-regression.spec.ts-snapshots/*.png` — inspected and regenerated Admin Light/Dark visual baselines after intentional required-label/layout changes.
- `frontend/e2e/accessibility.spec.ts`, `admin-dashboard/e2e/accessibility.spec.ts` — representative visible-focus assertions for rendered controls.
- `frontend/src/app/(portal)/payment-links/page.tsx` — payment-link item titles use the next-level heading after the page H1, preventing an H1-to-H4 hierarchy skip.
- `frontend/src/app/globals.css` — the shared white/translucent TEMBUS artwork receives a semantic dark scrim on light surfaces, preserving readable brand contrast.
- `frontend/e2e/accessibility.spec.ts` — Light/Dark runtime guard checks the shared logo's accessible name and effective-background contrast.
- `frontend/src/components/landing/ResiCheckWidget.tsx` — explicit `focus-visible` outline for the landing tracking input after the staging keyboard traversal found missing visible focus.
- `docs/task-evidence/A11Y-2026-012.md` — dated device-staging manual spot checks, limitations, blocker and unblock evidence.

## Commands / Checks Run

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium (frontend)
    result: PASS — 73/73 local Customer route/theme cases, including authenticated middleware-cookie fixtures and all three authenticated order-creation variants.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "@inventory" (frontend)
    result: PASS — historical pre-reconciliation baseline: 54/54 registered Customer route inventory cases in Light/Dark (27 routes × 2).

    command: npm run test:e2e:a11y (admin-dashboard)
    result: PASS — 80/80 local Admin route/theme cases.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "@inventory" (admin-dashboard)
    result: PASS — 140/140 registered Admin route inventory cases in Light/Dark (70 routes × 2).

    command: npx playwright test e2e/reflow.spec.ts --project=chromium (frontend/admin-dashboard)
    result: PASS — Customer 20/20 and Admin 11/11 reflow/text-spacing/zoom-equivalent cases.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "@inventory" (frontend)
    result: PASS — historical pre-reconciliation baseline: 54/54 registered Customer route reflow cases: 27 routes at 320px and 640px CSS (200%-equivalent), with no page errors.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "@inventory" (admin-dashboard)
    result: PASS — 140/140 registered Admin route reflow cases: 70 routes at 320px and 640px CSS (200%-equivalent), with no page errors or unexpected console errors.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "@text-spacing @inventory" (frontend)
    result: PASS — historical pre-reconciliation baseline: 27/27 registered Customer routes at 320px under the WCAG 1.4.12 text-spacing override, with no page errors or document-level horizontal overflow.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "@text-spacing @inventory" (admin-dashboard)
    result: PASS — 70/70 registered Admin routes at 320px under the WCAG 1.4.12 text-spacing override, with no page errors or unexpected console errors.

## Continuation — 2026-09-11 Customer On-Demand loading-state audit

- The current full Customer axe inventory found a real `aria-prohibited-attr` violation in `/orders/new/ondemand` Light: `CustomerPageSkeleton` placed `aria-label="Memuat halaman"` on a plain `<div>` without a valid role. The loading surface now uses `role="status"` and `aria-live="polite"`, preserving the accessible loading announcement while satisfying the permitted-ARIA contract.
- The route was rerun in the authenticated matrix and the full inventory path, in Light, Dark and both System-resolved variants; the Customer mobile shell was also rerun in Light/Dark.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "/orders/new/ondemand|mobile navigation" (frontend)
    result: PASS — 8/8 focused Customer accessibility cases, including the fixed full-inventory On-Demand Light case and mobile Light/Dark shell cases.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "/orders/new/ondemand" (frontend)
    result: PASS — 4/4 route reflow checks at 320px, 640px (200%-equivalent) and text-spacing override.

    command: npx tsc --noEmit; npx eslint src/components/ui/Skeleton.tsx --quiet (frontend)
    result: PASS.

The full Customer inventory was previously interrupted by this defect; the focused rerun is green, while the complete remaining route suite, dated manual Light/Dark observation, keyboard completion, screen-reader review, actual browser zoom, dynamic campaign/provider variants and staging/provider-backed review remain open. No manual PASS is inferred from this automated result.

## Continuation — 2026-09-11 full inventory and visual regression rerun

- After fixing `CustomerPageSkeleton`, the complete current route inventory was rerun. Customer passes `56/56` Light/Dark/mobile cases and Admin passes `142/142` Light/Dark/mobile cases.
- Admin visual regression initially detected intentional drift from the current required-field labels and layout/spacing changes. The actual/diff images were inspected; the differences were not clipping, overlap or contrast loss. The four checked-in Light/Dark Admin baselines were regenerated from the reviewed current render and the suite now passes `3/3`.
- Customer public visual regression passes `2/2`; the route-specific On-Demand reflow suite passes `4/4` at 320px, 640px (200%-equivalent) and text-spacing override.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "@inventory" (frontend)
    result: PASS — 56/56 current Customer Light/Dark/mobile inventory cases.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "@inventory" (admin-dashboard)
    result: PASS — 142/142 current Admin Light/Dark/mobile inventory cases.

    command: npx playwright test e2e/visual-regression.spec.ts --project=chromium --update-snapshots (admin-dashboard)
    result: PASS — 3/3 reviewed Admin Light/Dark login and empty-dashboard visual cases after baseline regeneration.

    command: npx playwright test e2e/visual-regression.spec.ts --project=chromium (frontend)
    result: PASS — 2/2 Customer public visual cases.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "/orders/new/ondemand" (frontend)
    result: PASS — 4/4 On-Demand responsive/text-spacing/200%-equivalent cases.

The complete manual matrix is still not COMPLETE: no authorized screen reader/manual browser surface is available in this environment, and no real provider-backed dynamic campaign/provider execution was performed. These remain explicitly NOT_RUN/PARTIAL rather than fabricated PASS.

## Environment and unblock record — 2026-09-11

- Playwright Chromium is available; the current Customer `64/64` and Admin `142/142` route/theme/mobile inventories, visual suites and representative focus/reflow checks are recorded above. Customer reflow/text-spacing coverage is now `93/93` across 31 registered routes.
- CUA reports no browser or native application handles. Windows `Narrator.exe` is present, but there is no authorized native UI/speech-output bridge in this environment to perform and capture a real screen-reader session.
- `gh secret list --env staging` and `gh variable list --env staging` returned no entries, so the workflow can build/push images but cannot perform SSH deployment or current-commit external validation.
- This is an execution capability limitation for the manual rows, not a claim that the product is inaccessible. The exact reviewer procedure is in `docs/accessibility/manual-audit-matrix.md`; after the reviewer record and staging configuration are supplied, rerun the automated suites and Reality Gate validator.

## Continuation — 2026-09-11 representative focus-indicator proof

- Both route-inventory suites now sample up to twelve visible interactive controls after each route settles, programmatically focus each control, and fail if the computed focus indicator has neither a visible outline nor a box shadow. This is a DOM/rendered-style assertion for focus visibility, not a claim of complete keyboard-only traversal.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "(/orders/new/ondemand|/dashboard|/app-experience/overview) has no" (frontend)
    result: PASS — 4/4 representative Customer Light/Dark route cases with focus-indicator assertions.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "(/dashboard|/cases|/app-experience/overview) has no" (admin-dashboard)
    result: PASS — 14/14 representative Admin matrix/inventory Light/Dark/System cases with focus-indicator assertions.

    command: npx tsc --noEmit; npx eslint e2e/accessibility.spec.ts --quiet (frontend)
    result: PASS.

    command: npx tsc -b; npx eslint e2e/accessibility.spec.ts --quiet (admin-dashboard)
    result: PASS.

Full keyboard-only activation/completion for every representative route, manual screen-reader names/announcements, actual browser zoom, dynamic provider/campaign states and staging/provider-backed execution remain open.

## Continuation — 2026-09-11 complete Customer route registration and narrow-layout fixes

- The Customer inventory was reconciled against the actual `frontend/src/app/**/page.tsx` route set. Four previously omitted auth/callback surfaces are now registered in both Customer accessibility and reflow suites: `/forgot-pin`, `/otp-verify`, `/google-callback` and `/apple-callback`. The Customer inventory is now 31 routes rather than 27.
- The reconciliation exposed two real WCAG 1.4.10 issues at a 320px CSS viewport. `/forgot-pin` now gives the phone input `min-w-0`, preventing the OTP action from expanding the row; `/otp-verify` now uses 44px OTP targets in a 3x2 narrow layout and returns to the six-field row at the `sm` breakpoint. `/apple-callback` now exposes a semantic `main` region and an explicit button type/focus treatment for its callback error action.
- The visible-focus helper intentionally excludes disabled controls because disabled elements are not keyboard-focus targets; enabled visible controls still require a computed outline or box-shadow after focus. This is rendered focus evidence, not a claim of full keyboard traversal.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "@inventory" --workers=1 (frontend)
    result: PASS — 64/64 current Customer Light/Dark/mobile inventory cases across all 31 registered routes; expected callback error-state logs for missing provider tokens did not produce page errors or failed assertions.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "@inventory" --workers=1 (frontend)
    result: PASS — 93/93 current Customer inventory cases: 62 route reflow cases at 320px and 640px CSS (200%-equivalent) plus 31 WCAG 1.4.12 text-spacing cases at 320px, with no page errors or unexpected document-level horizontal overflow.

    command: npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "forgot-pin|otp-verify|google-callback|apple-callback" --workers=1 (frontend)
    result: PASS — 8/8 targeted Light/Dark accessibility cases after the route-registration, landmark and disabled-focus corrections.

    command: npx playwright test e2e/reflow.spec.ts --project=chromium --grep "forgot-pin|otp-verify" --workers=1 (frontend)
    result: PASS — 6/6 targeted 320px, 640px and text-spacing cases after fixing the real narrow-layout overflow.

    command: `node scripts/a11y/check-*.mjs` — each of the 19 listed guards was executed individually (in parallel) from the repository root.
    result: PASS — all 19 Customer/Admin source and token accessibility guards pass.

    command: `npm run test`, `npx tsc --noEmit` and `npm run lint` (frontend; executed independently in parallel)
    result: PASS — 68/68 unit tests, TypeScript check exit 0, and ESLint exit 0 with the repository's existing warning-only findings (500 warnings, 0 errors).

    command: `NEXT_PUBLIC_API_URL=https://api.tembus.id/api/v1 NEXT_PUBLIC_WS_URL=wss://api.tembus.id NEXT_PUBLIC_SOCKET_URL=https://api.tembus.id SERVER_API_URL=https://api.tembus.id/api/v1 npm run build` (frontend; equivalent PowerShell environment assignments)
    result: PASS — Next.js production build compiled successfully, TypeScript completed, and all 29 static pages generated; only the existing middleware-to-proxy deprecation warning was emitted.

The latest local Customer automated evidence is complete for the registered route inventory, but the release gate remains open: dated manual Light/Dark observations, keyboard-only completion, native assistive-technology output, actual browser zoom, dynamic campaign/provider states and current-commit staging/provider-backed execution are still not proven. No manual PASS is inferred from these automated results.

## Continuation — 2026-09-11 keyboard, state and dynamic-content automation

- The focused Customer keyboard and runtime-experience suites pass `16/16`. They cover keyboard-reachable primary actions, field-level error association, navigation/current-location semantics, popovers, dialogs, combobox interaction, pressed state, quantity controls, Escape dismissal/focus restoration, validation errors and runtime campaign media including informative and decorative variants.
- The focused Admin keyboard and theme suites pass `27/27`. They cover login errors, table-row and icon actions, collapsed navigation, notification popover, switch state, confirmation/dialog focus traps, Escape restoration, theme switching, App Experience theme state, table state, map controls/error/loading announcements, chart summaries and status surfaces.

    command: `npx playwright test e2e/keyboard.spec.ts e2e/runtime-experience.spec.ts --project=chromium --workers=1` (frontend)
    result: PASS — 16/16.

    command: `npx playwright test e2e/keyboard.spec.ts e2e/theme.spec.ts --project=chromium --workers=1` (admin-dashboard)
    result: PASS — 27/27.

These results strengthen the automated keyboard/state evidence but do not replace a dated human keyboard-only traversal, real screen-reader output, actual browser zoom observation or provider-backed staging observation.

## Continuation — 2026-09-11 current Docker runtime verification

- The available Docker Desktop daemon was used to rebuild the current Customer frontend image after the payment-link heading fix; the compose stack restarted successfully and the frontend build generated all 29 routes.
- The initial current-runtime Customer run exposed a real semantic defect: `/payment-links` rendered the item title as H4 directly after the page H1. The source was corrected to H2, the image was rebuilt, and the focused route check passed `6/6` across the authenticated/theme and registered Light/Dark cases.
- The complete current-runtime matrices then passed for both web surfaces. The test host intentionally used `localhost` so the existing authenticated fixture cookie domain matches the runtime host.

    command: `C:\Program Files\Docker\Docker\resources\bin\docker.exe compose --env-file .env up -d --build frontend` (repository root)
    result: PASS — current frontend image rebuilt; Next.js production build completed and generated 29 routes; dependent local services returned to running/healthy state.

    command: `BASE_URL=http://localhost:3083 npx playwright test e2e/accessibility.spec.ts --grep "/payment-links" --project=chromium --workers=1` (frontend)
    result: PASS — 6/6 focused payment-link accessibility cases after the H2 correction.

    command: `BASE_URL=http://localhost:3083 npx playwright test e2e/accessibility.spec.ts e2e/reflow.spec.ts e2e/keyboard.spec.ts e2e/runtime-experience.spec.ts --project=chromium --workers=1` (frontend)
    result: PASS — 266/266 current Customer Docker runtime cases, including public/authenticated route inventory, Light/Dark/System, mobile semantics, keyboard, reflow, 200%-equivalent width, text-spacing and runtime campaign states.

    command: `ADMIN_BASE_URL=http://localhost:3084 npx playwright test e2e/accessibility.spec.ts e2e/reflow.spec.ts e2e/keyboard.spec.ts e2e/theme.spec.ts --project=chromium --workers=1` (admin-dashboard)
    result: PASS — 470/470 current Admin Docker runtime cases, including 80 registered routes across Light/Dark/System, mobile semantics, keyboard/dialog paths, 320px/640px reflow, text-spacing, theme, map/chart alternatives and status/icon semantics.

The Docker runtime evidence proves the current local build and automated rendered behavior. It does not promote the manual matrix to PASS: dated human keyboard/contrast/zoom review, native screen-reader output, provider-backed dynamic states and current-commit staging deployment remain open.

## Continuation — 2026-09-11 staging push and CI result

- Commit `8194470d7c777c1cdd3034c7305610e405f72ea7` was pushed to `origin/staging` and the remote branch resolved to that exact SHA.
- GitHub Actions run `34581994914` (`CI/CD Staging`) completed with conclusion `success`. Service tests, frontend build/guards, security scan, observability guard, container SBOM/secret audits and image build/push all passed.
- The `Deploy to Staging` job itself completed as `success`, but its `Write SSH private key` and `Deploy via SSH` steps were `skipped` because the staging deployment secrets are not configured. `Release Validation Pending` completed as `success`; authenticated core, k6 API regression and browser E2E jobs were `skipped`, so this run is not proof of a deployed current commit or provider-backed runtime behavior.

## Continuation — 2026-09-11 final current-commit CI result

- Docs/master update commit `2d1713a98ed5551b7219dcc4acedd4b63c1349e3` is present on `origin/staging`.
- GitHub Actions run `34587860287` (`CI/CD Staging`) completed with conclusion `success`. All executed service, frontend, security, observability, SBOM/secret-audit and image build/push jobs passed.
- The deploy job completed successfully but `Write SSH private key` and `Deploy via SSH` were skipped; `API Regression (k6)`, `Authenticated Core Part A Contract` and `E2E Browser Validation` were skipped because staging deployment/validation configuration is absent. This remains an external release-gate blocker, not an implementation failure.

## Continuation — 2026-09-11 Android emulator/TalkBack and logo contrast remediation

- The available Android tooling was used instead of treating device verification as an automatic blocker. `Pixel_5_kurir` was booted headlessly after setting both `ANDROID_HOME` and `ANDROID_SDK_ROOT` to the installed SDK; Chrome and TalkBack were present, and TalkBack was enabled through Android secure settings.
- Chrome loaded the local Customer runtime at `http://10.0.2.2:3083`. The Android WebView accessibility tree exposed the page name `TEMBUS — On-demand logistics`, the logo as an image with content description `TEMBUS` and source text `tembusweb`, plus named actions such as `Masuk`, `Daftar` and `Kirim Sekarang`.
- Visual inspection of the emulator screenshot found a real contrast defect: the shared white/translucent `tembusweb.svg` artwork was nearly invisible on the pale landing header. The global stylesheet now applies `background-color: var(--token-scrim)` and a small radius to that asset. The refreshed emulator screenshot shows the logo clearly on a dark scrim, while preserving the existing accessible name.

    command: `adb shell settings put secure accessibility_enabled 1` and `adb shell settings put secure enabled_accessibility_services com.google.android.marvin.talkback/com.google.android.marvin.talkback.TalkBackService`
    result: PASS — TalkBack is enabled on the local emulator; speech output itself is not observable through the available bridge.

    command: `BASE_URL=http://localhost:3083 npx playwright test e2e/accessibility.spec.ts e2e/reflow.spec.ts e2e/keyboard.spec.ts e2e/runtime-experience.spec.ts --project=chromium --workers=1` (PowerShell equivalent environment assignment)
    result: PASS — 266/266 current Customer Docker runtime cases after the logo remediation.

    command: `npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "shared logo readable" --workers=1`
    result: PASS — 2/2 Light/Dark logo checks; both visible logo instances have `alt="TEMBUS"`, a computed `rgb(20, 33, 26)` scrim and `16.64:1` white-artwork contrast.

    tool: Playwright Node runtime computed-style probe (frontend)
    result: PASS — both rendered logo instances report `complete: true`, `background: rgb(20, 33, 26)` and `whiteArtworkContrast: 16.64`.

- The emulator/WebView tree and generated screenshot provide code-driven mobile/semantic evidence, but do not constitute dated human keyboard-only completion, native screen-reader speech review, actual browser-zoom review or provider-backed staging validation. Those manual/release rows remain `NOT_RUN`.

## Continuation — 2026-09-11 logo contrast implementation CI result

- Implementation commit `ca3a8533c2a7bdfb9ff2e1b31d719dfac5ad7c69` and the evidence/master update commit `2260c5a7e8d9ba4cbb6f9146fe80df45a622e10f` are present on `origin/staging`.
- Task Evidence Gate run `34591815873` completed successfully.
- CI/CD Staging run `34591815927` completed successfully for the implementation commit. Frontend build/guards, all service tests, migration test, security scan, observability guard, container SBOM/secret audits and Docker image build/push all passed.
- `Deploy to Staging` completed as a workflow success, but `Write SSH private key` and `Deploy via SSH` were skipped after the required-secret check. `Release Validation Pending` completed successfully; API Regression, Authenticated Core Part A Contract and E2E Browser Validation were skipped. Therefore the current implementation is CI-verified, but deployed staging/provider-backed behavior remains unproven.

## Continuation — 2026-09-11 cross-engine focus indicator hardening

- The newly available Playwright Firefox/WebKit engines were installed and used against the current Docker Customer runtime. The first WebKit run exposed a real focus-indicator defect in the shared locale switcher: its `outline-none` class suppressed the global focus fallback, while the utility ring was not rendered by WebKit. The component now uses an explicit `focus-visible:outline-*` treatment while retaining the ring enhancement.
- The first cross-engine failure was reproduced with keyboard Tab: WebKit reported `:focus-visible=true` but no outline or box shadow on the language `<select>`. After the component fix and Docker rebuild, the root Light/Dark focus cases passed in Chromium and WebKit.

    command: `npx playwright install firefox webkit` (frontend)
    result: PASS — the exact Firefox/WebKit executables required by the installed Playwright version were downloaded locally.

    command: `npx playwright test e2e/accessibility.spec.ts --project=chromium --project=webkit --grep '/ has no (light|dark) violations' --workers=1 --reporter=line` (frontend)
    result: PASS — 4/4 root Light/Dark inventory cases after rebuilding the current Docker frontend image.

    command: `npx playwright test e2e/accessibility.spec.ts --project=firefox --project=webkit --grep '@inventory' --workers=1 --reporter=line` (frontend)
    result: PASS — 128/128 Firefox/WebKit Customer inventory and mobile-shell cases across Light/Dark; no remaining cross-engine accessibility, semantics or sampled focus-indicator failures.

    command: `npx tsc --noEmit; npx eslint src/components/i18n/LocaleSwitcher.tsx e2e/accessibility.spec.ts --quiet` (frontend)
    result: PASS.

The new implementation is committed as `c03faa5e` and is queued with the evidence/master update for the required staging push. This cross-engine proof strengthens automated focus coverage but does not replace dated human keyboard-only traversal, native screen-reader speech review, actual browser-zoom review or provider-backed staging validation.

## Staging tunnel manual review — 2026-09-11

Reviewer/tool: Codex through the Codex in-app browser (CUA). Runtime: the device's active Docker stack exposed through the Cloudflare tunnel. Approximate compact viewport: `600x898`. No account credentials, OTP or payment details were entered.

### Runtime reachability

    tool: public HTTP HEAD probes
    result: PASS — `https://app.bawain.my.id`, `https://admin.bawain.my.id`, `https://merchant.bawain.my.id`, `https://api.bawain.my.id/health` and `https://bawain.my.id` returned HTTP 200.

    command: `C:\Program Files\Docker\Docker\resources\bin\docker.exe compose --env-file .env up -d --build frontend`
    result: PASS — current Customer frontend image rebuilt; Next.js generated 29 routes; dependent local services returned to running/healthy state.

### Customer public/auth/error spot checks

- Opened `/`, `/login`, `/daftar`, `/forgot-pin`, `/otp-verify`, `/google-callback`, `/apple-callback`, `/cek-resi`, `/track/invalid`, `/pay/fixture-payment` and `/location-requests/fixture-token` through `https://app.bawain.my.id`.
- The Customer root AX tree exposed the page title, named logo image, header/navigation/main/footer landmarks, headings, labels and named primary actions. The inspected routes had no document-level horizontal overflow at the compact viewport.
- Empty `/login` submit produced the accessible error `Email dan password wajib diisi untuk login dengan password.` without leaving the route.
- A non-sensitive fake tracking value `TB-INVALID-A11Y` produced a visible tracking error state with `Gagal memuat` and `Terjadi kesalahan pada server (kode 401)`; the error remained within the page and no overflow was observed.
- Invalid callback, inactive tracking link and inactive location-link routes exposed their error headings/actions. `/pay/fixture-payment` returned the expected missing-fixture/not-found state; real payment execution was not attempted per instruction.

### Customer keyboard/focus spot check

- On `/`, a keyboard-only Tab traversal reached the logo link, header/auth actions, hero CTA, tracking field, tracking action, service cards, report/locale links and footer links. The traversal completed 22 observable focus stops before cycling.
- The traversal found a real WCAG 2.4.7 defect on `#landing-resi-input`: the field had no visible outline or shadow while focused. The input now has an explicit `focus-visible` outline in `frontend/src/components/landing/ResiCheckWidget.tsx` and a two-case Light/Dark regression test.
- After rebuilding the Docker frontend, the same staging field reported a visible `2px` accent outline plus the focus ring after keyboard focus returned. This is a dated spot-check PASS for the repaired control, not a PASS for every Customer route.

### Admin login spot check

- Opening `https://admin.bawain.my.id` redirected to `/login`. The AX tree exposed the Welcome Back heading, email/password fields, remember-me checkbox, Forgot Password link, submit action and security notice.
- Empty submit produced the native `Please fill out this field.` validation message and focused the email field; no credentials were transmitted.
- Keyboard Tab traversal reached email, password, remember-me, Forgot Password and Sign In with visible focus. This is a login-route spot check only; authenticated Admin operational routes were not opened without an authorized account/session.

### Manual review limitations

- The in-app browser session was effectively dark/system. A controllable real Light-mode browser session and an actual browser zoom control were unavailable; automated Light and 320/640 CSS-width equivalents remain separate evidence, not manual PASS.
- CUA/Android WebView accessibility trees were inspected, and TalkBack was enabled on the local emulator, but spoken screen-reader output was not observable through the available bridge.
- Success states, authenticated Customer/Admin operational routes, dynamic campaign/provider content, OTP and payment-provider execution remain NOT_RUN or deferred. These boundaries are reflected in `docs/accessibility/manual-audit-matrix.md`.

## Current SHA CI result — 2026-09-11

- Current `origin/staging` SHA is `86dd3d2ed2f3dcf50d17faee8aafbe3f8462f8db`.
- Task Evidence Gate run `34601063282` completed with conclusion `success`.
- Security Scan Pipeline run `34601063340` completed with conclusion `success`.
- CI/CD Staging run `34601063249` completed with conclusion `success`. All executed service tests, frontend build/guards, migration, security, observability, container SBOM/secret audits, and Docker image build/push jobs passed.
- `Deploy to Staging` completed as a workflow success, but its SSH write/deploy steps were skipped by the required-secret guard. `API Regression (k6)`, `E2E Browser Validation`, and `Authenticated Core Part A Contract` were skipped because staging deployment/validation configuration is absent. This is not proof of a GitHub SSH rollout or authenticated/provider-backed behavior; the device-local Docker stack and tunnel spot checks above are the staging evidence available in this session.

## Verification after the manual remediation

    command: `npx playwright test e2e/accessibility.spec.ts --project=chromium --grep "landing tracking input" --workers=1 --reporter=line` (frontend)
    result: PASS — 2/2 Light/Dark keyboard focus regression cases.

    command: `npx playwright test e2e/accessibility.spec.ts e2e/reflow.spec.ts e2e/keyboard.spec.ts e2e/runtime-experience.spec.ts --project=chromium --workers=1 --reporter=line` (frontend)
    result: PASS — 270/270 current Customer accessibility, reflow, keyboard and runtime-experience cases.

The local implementation and automated evidence are green. A11Y-012 remains a release-gate `BLOCKED` task because the original manual checklist still contains unproven rows that require an authorized native assistive-technology/Light/actual-zoom surface, authenticated staging access or the explicitly deferred provider coordination.

## Reality Gate Evaluation

### REALITY-2026-003

Status: `PARTIAL` — automated representative coverage is green; manual reviewer sign-off remains open.

### REALITY-2026-011

Status: `PASS` — every unrun manual/provider row is explicitly labeled `NOT_RUN`.

## Unproven / Remaining

The original manual checklist is not fully proven. Remaining rows are full Light/Dark route coverage, complete keyboard-only primary-action traversal, native screen-reader output, actual browser zoom, authenticated Customer/Admin route-state review and dynamic provider/campaign states. OTP and real payment-provider execution are deferred per the owner instruction.

## Owner action required to unblock the release gate

1. Provide an authorized staging account/session for authenticated Customer and Admin manual review, or run the remaining manual rows yourself and record reviewer, date, route, state, viewport, browser, assistive technology and result. Do not paste credentials into chat or evidence.
2. Provide a native screen-reader/manual Light/actual-zoom review surface (Windows Narrator, NVDA or VoiceOver) that can be operated and observed, or attach the resulting dated review record.
3. Coordinate OTP, payment-provider and provider-backed campaign validation separately when those external systems are ready; they are intentionally deferred for this pass.
4. If current-commit GitHub deployment validation is also required, configure `STAGING_SSH_HOST`, `STAGING_SSH_USER`, `STAGING_SSH_PORT`, `STAGING_SSH_PRIVATE_KEY`, optional `STAGING_APP_DIR`, `STAGING_URL` and provider validation credentials in GitHub environment secrets/variables only.
5. After the unblock condition is met, rerun the Customer/Admin automated suites and `python scripts/tasks/validate_task_evidence.py`; only then promote the remaining matrix rows and A11Y-012 master checklist.
