---
task_id: FOOD-2026-004
status: PARTIAL

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: "53e11a13"

tests: PASS
integration: PASS
e2e: NOT_RUN

migration: N/A
migration_na_reason: "The handoff token table and one-time token lifecycle already exist; this change reuses them and moves consumption into the existing transition transaction."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: NOT_RUN
release_followups: "Run authenticated merchant/courier pickup scan with valid, expired, wrong-order, wrong-actor, wrong-stage, and replayed tokens on staging."

unproven_requirements: "Replay/wrong actor rejection has unit-level coverage but has not yet been exercised through the authenticated Food pickup scan route."
known_blockers: NONE
locally_actionable_remaining: "Add an authenticated handler/service integration test for Food pickup scan replay and wrong actor, then close the final acceptance checkbox."
blocker_resolution_attempts: "Reused the existing hashed, expiring, one-time handoff token contract and moved token consumption into the PostgreSQL OrderTransition transaction when a proof scan carries a handoff token. A failed picked_up transition now rolls back token consumption."
unblock_condition: "Handler/service integration test proves valid token plus picked_up is one transaction and replay/wrong actor are rejected."

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE

dependency_chain_blocked: false
next_eligible_task: FOOD-2026-005
updated_at: 2026-09-02
---

# Evidence — FOOD-2026-004

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md`:

- `[x]` One-time PIN/QR binds order/merchant/courier or pickup customer/state/expiry.
- `[x]` Verify + picked_up atomic.
- `[ ]` Replay/wrong actor rejected.

## Scope Implemented

- Existing handoff tokens are hashed, order-bound, actor-bound, stage-bound, expiry-bound, attempt-limited, and one-time.
- Production PostgreSQL order transitions consume a handoff token inside the same transaction that writes the proof and changes the order state, so failed `picked_up` transitions roll back token consumption.
- The legacy standalone consume path remains only for lightweight repositories that do not expose the canonical transition boundary.

## Files Changed

- `backend/order-service/internal/service/order_consolidation.go` — avoid standalone token consume for transactional repositories.
- `backend/order-service/internal/repository/handoff_token_repository.go` — atomic transition-scoped token verification/consume.
- `backend/order-service/internal/repository/postgres_order_transition_repository.go` — consume proof token before transactional status/proof commit.
- `backend/order-service/internal/service/handoff_service_test.go` — existing one-time/wrong actor/order/stage coverage.

## Commands / Checks Run

    command: backend/order-service: go test ./...
    result: PASS

    command: gofmt + git diff --check
    result: PASS

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL — atomic implementation and unit coverage pass; authenticated route replay/wrong-actor proof remains locally actionable.
- `REALITY-2026-011`: PASS — no token or pickup success is fabricated; server-side token/state checks remain authoritative.

## Unproven / Remaining

- Authenticated Food pickup scan replay/wrong-actor integration scenario.

## Next Eligible Task

FOOD-2026-004 — continue current task until the final acceptance checkbox is evidenced.
