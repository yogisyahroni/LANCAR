---
task_id: AGG-2026-006
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 453ddc09 + a62e7571
tests: PASS
e2e_staging: NOT_RUN
migration: NOT_RUN
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Authenticated staging/provider webhook acceptance-to-lifecycle verification, migration execution, production trace/metric evidence, and provider sandbox replay were not run."
known_blockers: "Docker and provider sandbox are unavailable in this workspace."
locally_actionable_remaining: NONE
unblock_condition: "Provide reachable staging PostgreSQL, Redis/provider sandbox, and authenticated webhook credentials; then run migration and acceptance-to-lifecycle E2E verification."
dependency_chain_blocked: true
next_eligible_task: NONE
updated_at: 2026-09-01
---

# Evidence — AGG-2026-006

## Scope Implemented

- Added durable `aggregator_awb_attempts` state with a unique order and idempotency key.
- Added provider-capability-driven first-mile mode resolution for `lancar_pickup`, `provider_pickup`, and `customer_dropoff`.
- Added authenticated courier/warehouse handoff recording. `lancar_pickup` requires actor, GPS coordinates, and evidence URLs.
- Added provider acceptance recording endpoint and persistence for provider reference/time.
- Wired normalized provider acceptance events to the persisted carrier handoff recorder before the lifecycle consumer runs; missing AWB/handoff state remains retryable and does not break legacy order lifecycle updates.
- Passed the AWB idempotency key and first-mile mode across order-service to integration-gateway.

## Files Changed

- `backend/order-service/internal/domain/carrier_handoff.go` — AWB attempt, first-mile, and handoff contracts.
- `backend/order-service/internal/service/carrier_handoff_service.go` — capability validation, durable AWB creation, proof validation, and acceptance recording.
- `backend/order-service/internal/repository/carrier_handoff_repository.go` — PostgreSQL persistence with unique replay-safe records.
- `backend/order-service/internal/handler/carrier_handoff_handler.go` — authenticated handoff and internal provider-acceptance endpoints.
- `backend/order-service/internal/service/carrier_event_service.go` — acceptance event wiring into handoff persistence and normalized lifecycle updates.
- `backend/order-service/internal/service/carrier_event_service_test.go` — acceptance forwarding and non-aggregator compatibility contracts.
- `backend/order-service/internal/domain/carrier_handoff.go` and `backend/order-service/internal/repository/carrier_handoff_repository.go` — retry-classifiable missing-state errors.
- `backend/order-service/internal/service/payment_link_service.go` — payment completion uses durable AWB orchestration.
- `backend/order-service/internal/infrastructure/integration_gateway_client.go` — forwards idempotency/mode to the integration boundary.
- `backend/integration-gateway/internal/domain/provider.go` and `backend/integration-gateway/internal/handler/logistics_handler.go` — preserve the orchestration fields at the provider boundary.
- `database/migrations/20260901000001_aggregator_awb_handoff.sql` — AWB attempt, handoff, indexes, and provider capability configuration.
- `task-food-marketplace-parity-2026.md` — first five proven checklist items marked complete; lifecycle consumer remains open.

## Commands / Checks Run

```text
command: go test ./... (backend/order-service)
result: PASS — all order-service packages passed.

command: go test ./... (backend/integration-gateway)
result: PASS — all integration-gateway packages passed.

command: git diff --check
result: PASS.

command: graphify update .
result: PASS with known extraction warning (659 graph edges missing confidence); graph rebuilt with 17,252 nodes and 31,705 edges.

command: docker compose / staging E2E
result: NOT RUN — Docker executable is unavailable in the workspace.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: Durable AWB attempt and handoff repository/service/handler are implemented and wired in `cmd/api/main.go`; normalized provider acceptance events now call the handoff acceptance recorder before lifecycle status processing.
- tests: Service tests cover successful AWB replay, first-mile capability use, proof rejection, and unknown provider acceptance rejection; both Go modules pass.
- E2E/staging: Not run because Docker/provider sandbox is unavailable.
- migration/backfill: Migration is written and reversible but not executed against a database in this workspace.
- observability: Structured logs exist in the payment-link AWB path; no production trace/metric evidence is available.
- security/privacy: Handoff recording requires authenticated operational roles; provider acceptance requires the internal API key; evidence URLs are persisted as references.
- rollback/recovery: Migration has a down path and failed AWB state is persisted; crash-recovery/provider sandbox drill is not run.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

Confirm with evidence, not assumptions:

- authenticated staging/provider acceptance-to-lifecycle verification remains unchecked;
- no test or staging/provider result is fabricated;
- the new endpoint is wired to authenticated/internal handlers;
- local service tests prove the acceptance recorder and normalized lifecycle consumer are both invoked; external provider delivery is not claimed as verified.

## Unproven / Remaining

- Execute authenticated provider webhook acceptance-to-lifecycle E2E in staging.
- Execute the migration against staging PostgreSQL.
- Run a real provider sandbox/create-AWB replay and authenticated courier handoff E2E.
- Add production trace/metric evidence for AWB attempt and handoff transitions.

## Blockers

Docker and provider sandbox access are unavailable in this workspace. All local implementation and contract coverage is complete; external staging/provider execution is required to prove the remaining acceptance criterion.

## Notes / N/A Justification

No field is marked `N/A`; every missing verification is explicitly `NOT_RUN` or `PARTIAL`.
