---
task_id: MOBILE-2026-010
status: PARTIAL
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "f7722e12 + 2593b14 + b3e0c593 + 55911d78"
tests: PASS
integration: PASS
e2e: PARTIAL
migration: N/A
migration_na_reason: "No task-owned persistent schema change for this task evidence."
observability: PASS
security_privacy: PASS
rollback_recovery: PARTIAL
task_scope_external_proof_required: false
external_runtime_validation: PARTIAL
release_readiness: PARTIAL
release_followups: "Task-specific proof remains: payment network switch/reconciliation and end-to-end order recovery. | Performance budget and accessibility/golden tests green on representative matrix. Runtime TalkBack/golden and physical-device follow-up remain open."
unproven_requirements: "Payment network switch/reconciliation and end-to-end order recovery. | Performance budget and accessibility/golden tests green on representative matrix."
known_blockers: NONE
locally_actionable_remaining: "Payment network switching/reconciliation and end-to-end order recovery. | Fix and remeasure runtime performance p95/jank. | Runtime accessibility/TalkBack and golden capture."
blocker_resolution_attempts: "Repository inspection; targeted Dynamic Experience/payment-resume tests; three-app upgrade install/launch; three-app trim-memory/background resume; three-app network loss/reconnect smoke; static accessibility audit; actual low/mid performance verification; release customer high-tier performance measurement; staging customer login with fresh debug APK bound to api.bawain.my.id; active-order tracking open; force-stop and explicit MainActivity relaunch with server-backed active-order recovery observed; mounted tracking connectivity transition with airplane mode after live callback fix."
unblock_condition: "All original checklist items are implemented and verified with task-specific executable evidence; unresolved external validation is recorded separately as release follow-up."
owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: "Run the payment network-switch/reconciliation and end-to-end order recovery flows, rerun the full performance verifier with all tiers, execute runtime accessibility/golden checks, then run the evidence validator."
dependency_chain_blocked: false
next_eligible_task: "CURRENT TASK — continue implementation and verification"
updated_at: 2026-09-15
---
# Evidence — MOBILE-2026-010

## Acceptance Criteria Source

- [x] Fresh install/offline launch.
- [x] Upgrade from minimum supported prior version.
- [x] Process death during active order then recovery.
- [ ] Network switch/loss during payment/order tracking.
- [x] Low-memory/background resume.
- [x] Dynamic Experience revision fallback.
- [ ] Performance budget and accessibility/golden tests green on representative matrix.

## Scope Implemented

Mobile reliability groundwork includes the machine-readable performance budget/gate and release-contract validation. Runtime evidence now covers prior-to-current package upgrade for all three apps, trim-memory/background resume for all three apps, generic offline/reconnect launch for all three apps, and targeted Dynamic Experience fallback tests. Customer dashboard recovery now renders every server-first recoverable order from the encrypted Room fallback after restart, with a dedicated policy test that excludes terminal/cancelled states. Payment and tracking screens render the network recovery banner while offline and reconcile from the server after connectivity returns; tracking keeps the banner visible above the active-order map with an explicit retry action (`b3e0c593`), and `NetworkStatus` now reports live loss on the main looper without trusting a stale network handle (`55911d78`). On the staging AVD, a freshly rebuilt debug APK bound to `https://api.bawain.my.id/api/v1/` authenticated successfully and opened the tracking route. With tracking mounted, device airplane mode produced `Active default network: none` and an AX-visible `Kamu sedang offline`/`Coba lagi` banner above the map; after Wi-Fi/data restoration the banner disappeared and no fatal Android runtime log was observed. This proves the tracking offline presentation/recovery affordance, but the payment-specific flow and server re-fetch after reconnect remain unproven. The performance evidence is fail-closed: the actual low/mid report and customer high release measurement fail configured budgets, so the full release gate remains open.

## Files Changed

- `scripts/mobile/`
- `docs/mobile/`
- `docs/mobile/release-acceptance-reports/mobile-acceptance-2026-09-14.md`
- `docs/task-evidence/MOBILE-2026-001.md`
- `docs/task-evidence/MOBILE-2026-003.md`
- `docs/mobile/performance-reports/local-emulator-mid-20260914.json`
- `.github/workflows/android-apps.yml`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/ActiveOrderRecoveryPolicy.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/DashboardViewModel.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/main/DashboardScreen.kt`
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/screens/main/ActiveOrderRecoveryPolicyTest.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/payment/PaymentScreen.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/tracking/TrackingScreen.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/util/NetworkStatus.kt`
- `docs/task-evidence/MOBILE-2026-010.md`

## Commands / Checks Run

    command: python scripts/mobile/validate_mobile_release_contract.py — PASS; command: python scripts/mobile/validate_mobile_static_contracts.py — PASS; command: python scripts/mobile/test_performance_gate.py — PASS contract fixture; command: python scripts/tasks/check_mobile_accessibility.py — PASS; command: .\gradlew.bat :app:testDebugUnitTest --console=plain --no-daemon (customer) — PASS, BUILD SUCCESSFUL in 3m31s; command: .\gradlew.bat :app:assembleDebug --console=plain --no-daemon with `DEBUG_BASE_URL=https://api.bawain.my.id/api/v1/` and temporary local UAT environment injection — PASS, BUILD SUCCESSFUL in 3m06s; command: `adb install -r` + explicit MainActivity UAT deep-link to staging — PASS; command: mounted tracking route + device airplane mode — PASS, `Active default network: none` and AX tree contained `Kamu sedang offline`, cached-data explanation, and `Coba lagi`; command: restore Wi-Fi/data — PASS, `Active default network` available, banner removed, no fatal Android runtime log; command: `python scripts/mobile/verify_performance_report.py --budgets scripts/mobile/performance_budgets.json --report docs/mobile/performance-reports/local-emulator-low-mid-20260914.json` — FAIL on missing high tier and low/mid p95 exceedances; command: release customer high 20-sample measurement — collected, verifier FAIL on jank p95; payment/order-specific success flow, server re-fetch proof after reconnect, runtime TalkBack speech output and golden capture — NOT_RUN/UNPROVEN.

## Task-Local Verification

Status: PARTIAL
Evidence: 5/7 original checklist items are checked in the master; upgrade, low-memory/background resume, generic network recovery, Dynamic Experience fallback, and active-order process-death recovery are reproduced. The available staging AVD and canonical API host now also prove the mounted tracking offline recovery banner and its removal after connectivity restoration. Payment-specific network switching/reconciliation, end-to-end order recovery, and the full release-matrix gates remain open.

## Reality Gate Evaluation

- REALITY-2026-003: PARTIAL — active-order process-death recovery and mounted tracking offline presentation are proven on the staging AVD, but payment/order-specific reconciliation and the full performance/golden matrix are not proven.
- REALITY-2026-011: PASS — missing device/provider/security evidence is not represented as success.

## Unproven / Remaining

Payment network switch/reconciliation and end-to-end order recovery. | Performance budget and accessibility/golden tests green on representative matrix.

## External Runtime / Release Validation

Status: PARTIAL — local package upgrade/recovery, fallback, active-order process-death recovery, and mounted tracking offline presentation are proven on the available AVD using a freshly built APK pointed at the canonical staging API; public staging host checks do not replace payment-specific reconciliation evidence or the failed performance/golden gate.

## Release Follow-ups

Fix the measured startup/jank regressions, then rerun all required tiers; add payment network-switch/reconciliation, end-to-end order recovery, TalkBack and golden evidence before promotion.

## Locally Actionable Remaining

Payment network switch/reconciliation and end-to-end order recovery. | Fix and remeasure performance budget p95/jank. | Runtime accessibility/TalkBack and golden tests.
