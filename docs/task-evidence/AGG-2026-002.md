---
task_id: AGG-2026-002
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: 5e269fbe
tests: PASS
integration: PASS
e2e: NOT_RUN

migration: N/A
migration_na_reason: "No database schema or stored data migration was introduced; existing server-side maps provider configuration, cache, and observations remain authoritative."

observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL

task_scope_external_proof_required: true
external_runtime_validation: NOT_RUN

release_readiness: BLOCKED
release_followups: "Run authenticated staging E2E for address search, GPS reverse geocoding, normalized city/district/postal fields, provider mapping persistence, cache reuse, and audit observation."

unproven_requirements: "Live authenticated staging verification of provider-location mapping persistence, cache reuse, audit observation, and cross-surface address behavior was not observable in this environment."
known_blockers: "Docker CLI/runtime and authenticated staging access are unavailable in the current Windows environment."

locally_actionable_remaining: NONE

blocker_resolution_attempts: "Inspected the existing public maps routes and backend provider configuration, removed all direct browser Nominatim calls from the aggregator address flows, ran full admin/frontend tests, type checks, lint, and production builds; no runnable Docker/staging runtime was available for live verification."
unblock_condition: "Provide a runnable staging/Docker stack with authenticated merchant/customer access and seeded maps provider data so the documented runtime checks can be executed."

owner_action_required: true
owner_action_summary: "Run the authenticated staging location-normalization E2E and attach request/response plus cache/audit evidence without exposing provider credentials."
verification_after_unblock: "Browser address search and GPS reverse geocode must call only LANCAR maps endpoints; response must keep display_label separate from city/district/postal_code; the selected provider mapping must be server-controlled, cacheable, and auditable."

dependency_chain_blocked: true
next_eligible_task: NONE

updated_at: 2026-09-02
blocked_since: 2026-09-02
retry_when: "Staging/Docker runtime and authenticated test access become available."
---

# Evidence — AGG-2026-002

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md`:

- `[x]` Remove direct browser third-party geocode.
- `[x]` Normalize display label separately from city/district/postal/provider code.
- `[ ]` Provider-location mapping is server controlled/cacheable/auditable.

## Scope Implemented

- Routed aggregator GPS reverse geocoding through the existing server endpoint `GET /maps/reverse-geocode`; the browser no longer calls Nominatim directly.
- Updated `AddressPicker`, `AggregatorWizard`, and `AggregatorForm` to consume the server response and preserve a distinct display label plus normalized city, district, and postal code fields.
- Extended the admin maps provider normalization for OSM and TomTom search/reverse responses so provider output exposes structured address parts without replacing the display label.
- Preserved the existing backend-owned provider configuration, cache, fallback policy, and observation path. The listed integration-gateway/order-service maps files were inspected and were not changed because public geocoding is already routed through admin-service in this repository.

## Files Changed

- `backend/admin-service/src/services/mapsProviderConfig.ts` — normalize display label and city/district/postal fields for provider results.
- `backend/admin-service/src/services/mapsProviderConfig.test.ts` — regression assertions for normalized OSM reverse/search fields.
- `frontend/src/components/orders/AddressPicker.tsx` — use backend reverse geocode and preserve normalized address parts.
- `frontend/src/components/orders/AggregatorForm.tsx` — use backend reverse geocode for current-location selection.
- `frontend/src/components/orders/AggregatorWizard.tsx` — use backend reverse geocode for current-location selection.
- `frontend/src/components/orders/OrderSchemas.ts` — typed district and normalized suggestion fields.
- `task-food-marketplace-parity-2026.md` — checked only the two criteria proven by local implementation/tests.

## Commands / Checks Run

    backend/admin-service: npm test -- --runInBand
    result: PASS — 47 suites, 238 tests

    backend/admin-service: npm run build
    result: PASS

    frontend: npm test
    result: PASS — 12 files, 39 tests

    frontend: npx tsc --noEmit
    result: PASS

    frontend: npm run lint -- --quiet
    result: PASS — 0 errors; 501 existing warnings remain

    frontend: NEXT_PUBLIC_API_URL=https://api.bawain.my.id/api/v1 SERVER_API_URL=https://api.bawain.my.id/api/v1 NEXT_PUBLIC_SOCKET_URL=https://api.bawain.my.id npm run build
    result: PASS — production build completed; existing Next Edge-runtime and middleware deprecation warnings remain

    repository: git diff --check
    result: PASS

    repository: docker compose ps
    result: NOT_RUN — Docker CLI/runtime unavailable in this environment

## Task-Local Verification

### Tests

Status: PASS

Evidence: Admin maps provider tests cover normalized OSM search/reverse output, and the complete admin/frontend test suites pass after the endpoint and schema changes.

### Integration

Status: PASS

Evidence: The browser flows use the existing API client and public `/maps/reverse-geocode` route. The admin-service maps provider configuration owns provider translation and normalized response fields.

### E2E

Status: NOT_RUN

Evidence: No authenticated staging/Docker runtime was reachable; no live provider, cache, or audit result is claimed.

### Migration

Status: N/A

Evidence: No schema or stored data change was introduced.

### Observability

Status: PARTIAL

Evidence: Existing backend provider observation/cache paths remain in use; live cache-hit and audit-observation evidence was not collected without a running stack.

### Security / Privacy

Status: PASS

Evidence: Third-party geocoding remains server-side, so provider access policy and credentials are not exposed to browser code.

### Rollback / Recovery

Status: PARTIAL

Evidence: The change is isolated to location normalization and client endpoint wiring and can be reverted as one implementation commit. Live recovery and cache behavior were not exercised.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: PARTIAL / BLOCKED

Local implementation, automated tests, type checks, lint, and builds pass. Authenticated staging verification of server-controlled mapping, cache reuse, audit observation, and cross-surface behavior remains unproven.

### REALITY-2026-011 — No fake completeness gate

Status: PASS

No provider, address, cache, audit, staging, or E2E result was fabricated. The two locally proven criteria are checked; the runtime-dependent criterion remains unchecked.

## Unproven / Remaining

- Authenticated staging address search and GPS reverse-geocode flow.
- Live proof that provider-location mapping is persisted/selected server-side, cacheable, and auditable.
- Cross-surface parity between customer web, merchant, and any mobile caller using the same normalized response contract.
- Runtime observability and rollback/recovery evidence on a running stack.

## External Runtime / Release Validation

Status: BLOCKED

The local implementation and automated checks are complete. Authenticated staging/Docker E2E remains unproven because the runtime is unavailable. Do not mark AGG-2026-002 COMPLETE until the unblock condition and verification-after-unblock checks are attached.
