---
task_id: AGG-2026-004
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "3149960a + 3be9d9f8"
tests: PASS
e2e_staging: NOT_RUN
migration: N/A
migration_na_reason: "The real-create/payment handoff reuses existing order and payment tables; no schema or data migration is introduced."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Authenticated staging proof of persisted order/payment sequencing and refresh/retry rehydration is not available; carrier AWB handoff remains AGG-2026-006 scope."
known_blockers: "Docker CLI/runtime is unavailable locally and no authorized authenticated staging/provider runtime was available for the required browser, payment, and persistence scenarios."
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Ported the real create flow onto the latest staging baseline, added server-success guards for order and payment responses, stable idempotency keys, session-backed order rehydration, server-confirmed payment polling, an explicit unsupported-COD fail-closed guard, frontend/admin builds and tests, and checked Docker availability."
owner_action_required: true
owner_action_summary: "Run the authenticated staging aggregator flow with a real enabled provider and payment sandbox, then inspect order/payment persistence and refresh recovery."
verification_after_unblock: "Create one aggregator order from the selected quote, capture redacted request/idempotency references, verify the order row exists before payment navigation, initialize payment, refresh during pending payment, confirm server-owned status, and verify no order is created or success navigation occurs on failed API responses."
unblock_condition: "Authorized staging web/API/database access, an enabled aggregator provider service, and a configured payment sandbox are available for the documented authenticated scenarios."
dependency_chain_blocked: true
next_eligible_task: AGG-2026-010
blocked_since: 2026-09-01
retry_when: "Retry when authenticated staging/payment sandbox access and service-visible database access are available."
updated_at: 2026-09-01
---

# Evidence — AGG-2026-004

## Scope Implemented

- Replaced the manual aggregator success delay and unconditional success
  navigation with the authenticated persisted customer-order mutation.
- Builds the create payload from the selected server tariff, resolved pickup
  and dropoff coordinates, recipient/package facts, provider, native service
  code, and an idempotency key retained for the same user intent.
- Requires an explicit successful server response and persisted order ID before
  any payment or navigation state is shown.
- Initializes the payment session only after the order reference is returned;
  payment confirmation and subsequent short polling read server-owned status.
- Persists the pending order reference in session storage and rehydrates it
  through the authenticated order-detail endpoint after refresh.
- Unsupported aggregator COD is disabled in the UI and rejected in the payload
  builder because the current backend payment contract only supports the
  configured non-COD payment path. No order is created for that unsupported
  option.

## Files Changed

- `frontend/src/components/orders/AggregatorWizard.tsx` — real submit,
  transaction recovery, payment handoff, and fail-closed payment option.
- `frontend/src/hooks/useCreateAggregatorOrder.ts` — typed payload builder,
  server-success guards, and stable request helpers.
- `frontend/src/components/orders/PaymentModal.tsx` — server-confirmed
  payment state and idempotent confirmation polling.
- `frontend/src/tests/unit/useCreateAggregatorOrder.test.ts` — quote payload,
  idempotency, unsupported payment, and missing-success response tests.
- `backend/admin-service/src/controllers/deliveryServices.controller.ts` —
  resolves the enabled aggregator service category for the existing endpoint.
- `backend/admin-service/src/courierV2P2Contract.test.ts` — service lookup
  regression coverage.
- `task-food-marketplace-parity-2026.md` — marks only the three locally
  proven checklist items.

## Commands / Checks Run

```text
frontend: npm test
result: PASS — 4 test files, 10 tests.

frontend: npm test -- --run src/tests/unit/useCreateAggregatorOrder.test.ts
result: PASS — 1 test file, 4 tests.

frontend: npx tsc --noEmit
result: PASS.

frontend: npm run lint
result: PASS — 0 errors; existing repository warnings remain.

frontend: npm run build
result: PASS — Next production build and route generation completed.

backend/admin-service: npm run build
result: PASS — TypeScript compilation completed.

backend/admin-service: npm test -- --runInBand
result: PASS — 36 suites, 198 tests.

repository: git diff --check
result: PASS.

repository: docker version; docker compose ps
result: NOT_RUN — Docker CLI is not installed in this environment.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: PASS for the real create mutation, quote-derived payload,
  persisted order reference, idempotency headers, payment-after-create
  sequencing, refresh rehydration code, and server-confirmed payment state.
- tests: PASS for frontend unit/typecheck/lint/build and admin-service build,
  regression test, and full Jest suite listed above.
- E2E/staging: NOT_RUN; Docker and authorized authenticated staging/payment
  runtime were unavailable, so no provider or payment result is claimed.
- migration/backfill: N/A; no persistent schema/data migration was introduced.
- observability: PARTIAL; existing request/correlation/payment logging is
  reused, but a live staging trace/metric reference was not collected.
- security/privacy: PASS; existing authenticated ownership routes are reused,
  idempotency keys are sent on create/payment mutation, unsupported COD fails
  closed, and browser callbacks cannot mark payment paid without server status.
- rollback/recovery: PARTIAL; session-backed refresh recovery and a reversible
  branch are present, but recovery was not exercised against a running stack.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

Evidence:

- The manual `setTimeout` success path and unconditional `orders?success=true`
  navigation are removed from the manual aggregator flow.
- Missing/unsuccessful server order and payment responses are errors; missing
  order IDs or payment sessions cannot enter a success state.
- Payment confirmation reads server-owned status and keeps the order pending
  when the gateway callback arrives before the webhook.
- No staging, provider, payment, screenshot, or persistence evidence is
  fabricated in this report.

## Unproven / Remaining

- Authenticated staging must prove the order row is persisted before payment
  navigation and that the same transaction is recovered after refresh.
- A configured payment sandbox/webhook must prove pending, success, and failed
  payment branches against persisted state.
- Carrier AWB/first-mile creation is intentionally not implemented here; its
  provider-driven sequencing belongs to `AGG-2026-006`.
- The master checklist leaves payment/AWB sequencing and refresh/retry boxes
  unchecked until those applicable runtime gates are proven.

## Blockers

Docker CLI is unavailable locally, and no authorized authenticated staging
runtime/database/payment sandbox was available for the required E2E evidence.
The task is marked `BLOCKED` only after local implementation and verification
were exhausted; it must not be treated as complete.

## Owner Action Required

1. Provide authorized access to the staging web/API and service-visible
   database, plus a configured payment sandbox for the aggregator service.
2. Log in with a non-production test account and select an actually enabled
   aggregator provider/tariff; do not place credentials, tokens, raw PII, or
   payment payloads in the repository or evidence.
3. Execute the verification matrix described in the frontmatter, capturing
   only redacted request IDs/order IDs and persisted status facts.
4. Report the run reference and redacted results so the task can be resumed and
   the remaining boxes can be evaluated.

## Notes / N/A Justification

Migration is `N/A` because the task reuses the existing order/payment schema;
AWB and first-mile persistence are a later provider-handoff task.
