---
task_id: PKG-2026-004
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 0cafd996 + d9654849 + 07675441 + d8b0af9f + 02ef5bd4 + fa86902d + fc8bdde2
tests: PASS
e2e: NOT_RUN
migration: PARTIAL
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Authenticated staging courier flow, deployed migration, database trigger behavior, production observability, and rollback execution have not been observed."
known_blockers: "Authenticated staging and service-visible database access are unavailable in this session."
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Audited the real admin-service proof path and order-service boundary, added local contract tests, ran backend/Go/Android checks, ran graphify update, and checked available IDE tooling. No staging credentials or reachable service-visible database were available."
unblock_condition: "Provide authenticated staging courier credentials plus reachable admin/order-service and service-visible database endpoints, then run the documented E2E and migration checks."
owner_action_required: true
owner_action_summary: "Project owner must provide staging access/configuration without placing secrets in the repository or evidence file."
verification_after_unblock: "Execute accepted -> pickup_arrived -> face verification -> package identity/count/condition proof -> picked_up, assert rejected pre-arrival proof, verify append-only package_scans, and capture deployed migration/observability evidence."
dependency_chain_blocked: true
next_eligible_task: "Independent P0 tasks only; pickup-dependent work resumes after PKG-2026-004 reaches COMPLETE."
blocked_since: 2026-09-01
retry_when: "When authenticated staging and service-visible database access are available."
updated_at: 2026-09-01
---

# Evidence — PKG-2026-004

## Scope Implemented

- Added the authoritative `pickup_arrived` transition for on-demand pickup and made the Android courier flow expose an explicit arrival confirmation before face, scan, photo, or custody completion.
- Added a server-side proof guard that rejects pickup evidence before arrival with `ERR_PICKUP_ARRIVAL_REQUIRED` and records the rejected attempt.
- Preserved package identity/count validation and proof ordering in the existing admin-service proof path; the legacy order-service consolidation path now rejects on-demand pickup before `pickup_arrived`.
- Added deterministic idempotency keys to mobile status updates and required idempotency on the status route.
- Added a database migration that installs the arrival policy and rejects updates to `package_scans` during retention, while retaining the existing expiry cleanup behavior.

## Files Changed

- `android-app/app/src/main/java/com/tembus/courier/domain/CourierFlow.kt` — arrival-gated state machine and action/title copy.
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/order/` — arrival action callback wired through order detail UI.
- `android-app/app/src/main/java/com/tembus/courier/data/` — status idempotency header and deterministic key.
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/pod/ProofOfDeliveryViewModel.kt` — client no longer claims a fixed liveness score.
- `android-app/app/src/test/java/com/tembus/courier/domain/CourierFlowResolverTest.kt` — arrival ordering coverage.
- `backend/admin-service/src/controllers/courier/_shared.ts` — authoritative pre-arrival proof rejection.
- `backend/admin-service/src/controllers/courier/courierAccount.controller.ts` — arrival status/event handling.
- `backend/admin-service/src/routes/order.routes.ts` and route tests — status idempotency enforcement.
- `backend/admin-service/src/*Courier*.test.ts` — proof and custody contract coverage.
- `backend/order-service/internal/domain/order.go` and `internal/service/order_consolidation.go` — on-demand custody gate.
- `database/migrations/20260901000002_on_demand_pickup_arrival.sql` — transition policy and append-only scan trigger.
- `task-food-marketplace-parity-2026.md` — four local acceptance criteria checked with evidence boundary documented here.

## Commands / Checks Run

```text
npm test -- --runInBand src/onDemandCourierProof.e2e.test.ts src/courierV2P2Contract.test.ts src/routes.test.ts
result: PASS — 3 suites, 28 tests

npm test -- --runInBand
result: PASS — 36 suites, 197 tests

go test -timeout 5m ./...
result: PASS

go vet ./...
result: PASS

./gradlew :app:testDebugUnitTest --tests com.tembus.courier.domain.CourierFlowResolverTest :app:compileDebugKotlin
result: PASS — BUILD SUCCESSFUL

git diff --check
result: PASS; only expected CRLF normalization warnings were emitted

graphify update .
result: PASS — graph updated; existing SQL parser and edge-relation warnings remain non-blocking tooling warnings
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: PASS locally; the Android, admin-service, order-service, migration, E2E test, and workflow changes are present through `fc8bdde2` on `staging`.
- tests: PASS for targeted backend contracts, full admin-service suite, Go tests/vet, and Android resolver/build checks.
- E2E/staging: NOT RUN for the required authenticated courier custody flow. The staging browser suite did pass its public/customer scenarios after the redirect-test hardening: run `33514888905`, including `/profil` -> `/login`; this does not prove courier pickup custody.
- migration/backfill: PARTIAL; migration is present and statically reviewed, but apply/rollback against the service-visible deployed database was not observed.
- observability: PARTIAL; rejection and custody events are emitted in code, but deployed dashboards/alerts and retention behavior were not observed.
- security/privacy: PASS locally; server remains authoritative, proof is gated by status, client liveness is not fabricated, and no credentials are stored in this evidence.
- rollback/recovery: PARTIAL; SQL down path is present, but execution and recovery evidence require the deployed database.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

Confirm with evidence, not assumptions:

- no stub/TODO is being presented as complete;
- no production fake/static success remains in the implemented path;
- the Android arrival action is wired through the order detail flow;
- authoritative server status and proof validation are used;
- no test, log, provider, staging, or production evidence is fabricated.

## Unproven / Remaining

- Run the authenticated staging flow and prove that pre-arrival proof is rejected and the ordered custody flow succeeds with real persisted data.
- Apply and roll back the migration against the database actually used by the services; verify the trigger and expiry-retention exception.
- Verify deployed audit/event visibility, dashboard/alert coverage, and operational recovery evidence.

## Blockers

The local implementation and tests have no remaining actionable work. Completion is blocked by missing authenticated staging courier access and service-visible database/observability access. Staging CI reached the deployment pipeline, but the deploy step was skipped because `STAGING_SSH_HOST` was not configured; the migration test used an ephemeral CI database and is not evidence of the service-visible production database. The owner must provide the access/configuration through the approved secret channel; secrets must not be pasted into this file or committed. After access is available, execute the verification listed in `verification_after_unblock` and update this evidence to `COMPLETE` only when both Reality gates pass.

## Notes / N/A Justification

No frontmatter field is `N/A`; fields that require external runtime proof are explicitly marked `NOT_RUN` or `PARTIAL`.

## Staging pipeline evidence

- PR #14 merged the E2E navigation hardening to `staging` at `fc8bdde2`.
- Staging CI run `33514888905` completed successfully: tests, migration up/down in the ephemeral CI database, observability guard, security scan, all nine container audits, image build/push, API regression, and browser validation all passed.
- Browser validation ran 8 scenarios; the final result was 6 passed after two transient retries. The previously failing `/profil` protected-route scenario passed in 1.1 seconds after switching navigation readiness to `domcontentloaded`.
- The `Deploy to Staging` job was green but explicitly skipped the SSH deploy because `STAGING_SSH_HOST` was unset. Therefore this run proves the pipeline and public endpoint behavior, not a fresh container rollout or service-visible migration.
- The prior run `33511833550` failed only on the `/profil` browser navigation timeout; the code-level test fix was validated locally and then in run `33514888905`.
