---
task_id: OPS-2026-001
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "cb1a5df6"
tests: PASS
e2e_staging: NOT_RUN
migration: N/A
migration_na_reason: "The operational timeline reads existing orders, payments, refunds, audit events, dispatch/proof records, and carrier_event_inbox data; no schema or persisted-data migration was introduced."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PASS
unproven_requirements: "Authenticated staging/Docker verification of all operational filters, event ordering, provider raw-event visibility, and role boundary remains unproven."
known_blockers: "Docker CLI/runtime is not installed in this Windows environment, and no reachable authenticated staging session is available for the required runtime verification."
locally_actionable_remaining: NONE
unblock_condition: "Install/start Docker or provide a reachable authenticated staging environment with seeded orders, payment/refund records, dispatch/proof events, and carrier events; then execute the documented filter/timeline/raw-event scenarios."
dependency_chain_blocked: true
next_eligible_task: NONE
updated_at: 2026-09-01
---

# Evidence — OPS-2026-001

## Scope Implemented

- Added service/category, subtype, provider/carrier, merchant, courier, and payment-state filters to the admin order query and dashboard.
- Added a single operational timeline that merges order state events, payment, refund, dispatch, proof, provider carrier events, and override reason in chronological order.
- Added an admin-only provider raw-event panel. Customer order/detail/tracking endpoints do not select or return `raw_payload`.
- Protected the `/admin/orders*` route family with the admin identity and role boundary; the route regression test verifies unauthenticated rejection and authenticated ops access.

## Files Changed

- `admin-dashboard/src/components/ActiveOrdersTable.tsx` — operational filters, timeline, and provider raw-event panel.
- `backend/admin-service/src/controllers/orders.controller.ts` — server-side filter/query and operational detail data.
- `backend/admin-service/src/routes/order.routes.ts` — admin auth/role protection scoped to order routes.
- `backend/admin-service/src/routes.test.ts` — authentication regression coverage.

## Commands / Checks Run

- `npx tsc -b` from `admin-dashboard` — PASS.
- `VITE_API_URL=https://api.example.test VITE_SOCKET_URL=wss://socket.example.test npm run build` from `admin-dashboard` — PASS; URL values were build-time validation inputs only and no external request was claimed.
- `npm run lint` from `admin-dashboard` — PASS with 0 errors; existing warnings remain.
- `npm run build` from `backend/admin-service` — PASS.
- `npm test -- --runInBand` from `backend/admin-service` — PASS, 39 suites / 207 tests.
- `python scripts/tasks/validate_task_evidence.py` — PASS before this task's checklist/evidence update; rerun after the update is required.
- `git diff --check` — PASS.
- `graphify query "OPS-2026-001 admin operational order timeline filters payments refunds carrier raw events route auth"` — returned the relevant order controller, admin API, carrier inbox, and auth nodes.
- `graphify update .` — AST extraction completed for 797 files, then the CLI did not terminate after extraction and was stopped; no clean graphify exit is claimed.
- `docker version` — NOT AVAILABLE: Docker is not installed/available on this workstation.

## Locally Actionable Remaining

`None.` All implementation, local query wiring, authorization, build, lint, and regression-test work for this task is complete.

## External Blockers

The remaining acceptance proof requires an authenticated staging/Docker runtime with real seeded operational records. Docker is unavailable locally and no usable staging session is exposed in this turn.

## Unblock Condition

Provide a reachable authenticated staging environment (or install/start Docker) with seeded orders covering each service, payment/refund states, dispatch/proof events, and provider carrier events. Execute and capture the filter, chronological timeline, raw-event role-boundary, and customer-surface non-leak scenarios.

## Dependency Impact

`dependency_chain_blocked: true` for dependent operational exception-queue work until this task's runtime contract is verified. No downstream task is marked eligible by this evidence.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`.

- Local implementation and automated checks are real and passing.
- Authenticated staging/Docker behavior with real orders and provider events is not available.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`.

- Provider raw payload is selected only by the authenticated admin detail endpoint.
- Customer-facing order handlers do not select `raw_payload`.
- No staging result, provider response, screenshot, or runtime record is fabricated.

## Unproven / Remaining

- Run all six filters against real persisted operational data.
- Confirm chronological ordering across state, payment, refund, dispatch, proof, provider, and override events in the rendered admin detail view.
- Confirm authorized ops can inspect raw provider payload while normal customer surfaces cannot receive it.
- Capture the authenticated runtime result and rerun the validator.

## Next Eligible Task

`None.` OPS-2026-002 remains blocked by the dependency rule until OPS-2026-001 reaches COMPLETE.
