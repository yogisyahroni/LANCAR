---
task_id: PKG-2026-005
status: BLOCKED

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: "PENDING"

tests: PASS
integration: PASS
e2e: NOT_RUN

migration: N/A
migration_na_reason: "Tracking freshness and client contract fields reuse existing location, route, and trip_share_tokens storage; no schema change is required."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: BLOCKED
release_followups: "Run authenticated customer/courier tracking flows and verify fresh, stale, reconnect, token-expiry, and revocation behavior with redacted evidence."

unproven_requirements: "Offline reconnect using an authoritative snapshot has not been observed on an authenticated device/runtime."
known_blockers: "No authorized authenticated staging/device runtime is available for offline/reconnect validation; local Android configuration is missing google-services.json."

locally_actionable_remaining: NONE
blocker_resolution_attempts: "Audited the existing admin tracking boundary, route provider contract, Android polling/reconnect flow, public share-token queries, and privacy fields. Added explicit freshness metadata to the server snapshot/public response and stale-state rendering in Android/Web/public tracking. Backend tracking tests, admin TypeScript build, frontend TypeScript/lint, and focused frontend tests pass."
unblock_condition: "Provide an authorized authenticated staging/device runtime and approved Android client configuration, then execute the offline/reconnect and token lifecycle matrix."

owner_action_required: true
owner_action_summary: "Run authenticated tracking with a real order, force a GPS update older than the freshness threshold, disconnect/reconnect clients, expire/revoke a share token, and attach redacted response/device evidence."
verification_after_unblock: "Verify ETA/provider provenance, stale flag and age, no contact leakage, scoped token acceptance/rejection, and reconnect recovery from REST snapshot rather than realtime event mutation."

dependency_chain_blocked: true
next_eligible_task: PKG-2026-006

blocked_since: 2026-09-02
retry_when: "When authenticated staging/device runtime and approved Android configuration are available."
updated_at: 2026-09-02
---

# Evidence — PKG-2026-005

## Acceptance Criteria Source

From `task-food-marketplace-parity-2026.md`:

- `[x]` ETA backend sourced.
- `[x]` Show GPS staleness.
- `[x]` Mask contact by lifecycle/privacy policy.
- `[x]` Public tracking token scoped/expiring/revocable.
- `[ ]` Offline reconnect uses snapshot.

## Scope Implemented

- Tracking snapshots expose provider-derived ETA plus explicit location freshness metadata: `location_stale`, age in seconds, and a reason when the last valid GPS point is unavailable or expired.
- Android customer, authenticated Web, and public tracking surfaces distinguish a fresh live point from a stale last-known point without replacing the coordinate with fabricated data.
- Public tracking validates the URL-safe token format before querying the hash-backed, scoped, expiring, revocable `trip_share_tokens` record. Public responses do not expose phone contacts; authenticated order tracking keeps contact access in the existing in-app/privacy boundary.
- Existing route provider observations and fallback provenance remain visible through the backend response; no client-side ETA is invented.

## Files Changed

- `backend/admin-service/src/services/onDemandTracking.ts` — freshness contract on authoritative tracking snapshots.
- `backend/admin-service/src/services/onDemandTracking.test.ts` — fresh/stale/unavailable GPS contract tests.
- `backend/admin-service/src/controllers/courier/courierSafety.controller.ts` — public token validation and freshness/ETA metadata.
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/TrackingModels.kt` — typed freshness and ETA source fields.
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/tracking/TrackingViewModel.kt` — stale-state propagation.
- `frontend/src/app/(portal)/orders/[id]/orderDetailTypes.ts` — typed tracking freshness fields.
- `frontend/src/app/(portal)/orders/[id]/OrderDetailContent.tsx` — stale last-known location presentation.
- `frontend/src/app/track/[token]/page.tsx` — public ETA and stale GPS presentation.
- `task-food-marketplace-parity-2026.md` — checked only four locally proven criteria.

## Commands / Checks Run

    backend/admin-service: npm test -- --runInBand src/services/onDemandTracking.test.ts
    result: PASS — 1 suite / 7 tests.

    backend/admin-service: npm run build
    result: PASS.

    frontend: npx tsc --noEmit
    result: PASS.

    frontend: npm test -- --run src/lib/realtimeEventGuard.test.ts src/lib/recoverableError.test.ts
    result: PASS — 2 files / 4 tests.

    frontend: npm run lint
    result: PASS — 0 errors; existing repository warnings remain.

    android-app-customer: .\gradlew.bat :app:testDebugUnitTest :app:compileDebugKotlin
    result: BLOCKED — approved google-services.json is unavailable locally.

    repository: git diff --check
    result: PASS.

## Task-Local Verification

### Tests

Status: PASS

Evidence: The tracking policy suite covers the fresh/stale boundary and unavailable-location state; existing route-provider tests cover backend ETA provenance and safe fallback labeling.

### Integration

Status: PASS

Evidence: The admin snapshot, public token response, Android model/view model, and Web tracking types/renderers carry the same freshness fields. Public tracking continues to use the hashed scoped token query and excludes phone contacts.

### E2E

Status: NOT_RUN

Evidence: Authenticated device/browser offline-reconnect, token expiry, and token revocation execution is unavailable in this session.

### Migration

Status: N/A

Evidence: Existing `trip_share_tokens`, route snapshot, and location tables already contain the required persistence; this change adds no migration.

### Observability

Status: PASS

Evidence: Route provider observation/provenance remains server-side, and freshness reason/age is returned as structured response data rather than inferred silently by the UI.

### Security / Privacy

Status: PASS

Evidence: Public token input is format-validated and compared by hash; expiry and revocation remain enforced by SQL; phone contacts are not returned by the public tracking response.

### Rollback / Recovery

Status: PASS

Evidence: New response fields are optional/additive, stale points remain explicitly labeled, and existing REST snapshot polling remains the recovery path.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL — implementation and local contract checks pass, but authenticated offline/reconnect runtime proof is unavailable.
- `REALITY-2026-011`: PASS — no fake coordinates, ETA, contact data, token validity, or runtime evidence was added.

## Unproven / Remaining

- The original offline reconnect criterion remains unchecked until an authenticated device/browser demonstrates reconnect recovery from the authoritative REST snapshot.
- Run the token lifecycle matrix against staging, including expiry and revocation, and verify the stale label from a real delayed GPS point.

## Blockers

Local implementation work for the four checked criteria is complete. Completion is blocked by unavailable authenticated staging/device runtime and missing approved Android Google Services configuration. No credentials or fabricated runtime data were added.
