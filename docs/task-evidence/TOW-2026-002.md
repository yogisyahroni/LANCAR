---
task_id: TOW-2026-002
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 2aa42b1a
tests: PASS
e2e: NOT_RUN
migration: N/A
migration_na_reason: "Matching uses existing courier_profiles, courier_vehicles, courier_service_capabilities, active_jobs, and delivery_service_products fields; no schema change was introduced."
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Staging/Docker matching with real approved courier records, vehicle categories, active jobs, zones, and concurrent dispatch is not verified."
known_blockers: "Docker and staging backend are unavailable in this workspace."
locally_actionable_remaining: NONE
unblock_condition: "Make Docker/staging backend and seeded approved courier records available, then run authenticated normal/preferred and concurrent dispatch verification."
dependency_chain_blocked: true
next_eligible_task: NONE
updated_at: 2026-09-01
---

# Evidence — TOW-2026-002

## Scope Implemented

- Normal dispatch now uses an approved `courier_vehicles` record bound to the enabled service capability.
- `towing_motor` requires a pickup/van vehicle category and `towing_mobil` requires a towing-truck category, with the legacy courier profile vehicle category used only as a compatibility fallback.
- Dispatch candidates remain constrained by approved/online status, recent location freshness, pickup assignment radius, active-job count, batching policy, and active-job stage policy.
- Preferred-courier dispatch uses the same vehicle, service capability, freshness, radius, and active-job guards; if the preferred courier fails those guards, the caller falls back to the normal queue.
- A contract test asserts that the preferred query contains the towing vehicle and dispatch safety predicates.
- Contract coverage also asserts the normal dispatch query joins approved vehicles/capabilities/zones, requires paid orders, fresh online couriers, active-job/radius limits, and excludes existing active offers.

## Files Changed

- `backend/admin-service/src/controllers/courier/courierOnDemand.controller.ts`
- `backend/admin-service/src/courierV2P2Contract.test.ts`
- `task-food-marketplace-parity-2026.md`

## Commands / Checks Run

```text
command: npm test -- --runInBand src/courierV2P2Contract.test.ts (backend/admin-service)
result: PASS — 1 suite, 8 tests passed, including preferred and normal towing matching guard coverage.

command: npm run build (backend/admin-service)
result: PASS — TypeScript compilation completed successfully.

command: git diff --check
result: PASS.

command: graphify update .
result: PASS — graph refreshed; known warnings remain for missing tree_sitter_sql and oversized HTML visualization.

command: Docker/staging/concurrent real-courier dispatch verification
result: NOT RUN — Docker executable and staging runtime are unavailable in this workspace.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

- Matching guards are implemented in both normal queue dispatch and customer-selected preferred dispatch.
- Vehicle category and service capability are checked against the actual courier vehicle record.
- Active-job and radius policy is evaluated server-side from persisted policy/state.
- Local contract tests and TypeScript build pass.
- Local preferred/normal matching guard coverage is complete; no further local implementation remains for this task.
- Real database records, zone coverage, concurrent dispatch races, and staging behavior remain unverified.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

- The staging/Docker verification checkbox remains unchecked.
- The contract test verifies query wiring only; it is not presented as a live dispatch result.
- No courier availability, vehicle record, or dispatch success was fabricated.

## Unproven / Remaining

- Run staging with at least one valid towing-motor courier, one valid towing-mobil courier, incompatible vehicles, stale/offline couriers, out-of-radius couriers, and active-job-limit cases.
- Verify concurrent preferred and normal queue dispatch cannot create duplicate active offers.
- Verify the actual seed/profile data uses canonical vehicle categories (`pickup`, `van`, `towing_truck`).

## Blockers

Docker and staging backend access are unavailable in this workspace. This is the only remaining proof gap; `locally_actionable_remaining` is `NONE`.
