---
task_id: GLOB-2026-014
status: BLOCKED

reality_2026_003: PARTIAL
reality_2026_011: PASS

implementation_ref: "8fa24108 + d3d582fc"

tests: PASS
integration: PASS
e2e: PARTIAL

migration: N/A
migration_na_reason: "GLOB-2026-014 adds no persistent schema or stored-data change; it reuses the existing market/compliance, finance, provider and region migrations."

observability: PARTIAL
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: true
external_runtime_validation: PARTIAL

release_readiness: BLOCKED
release_followups: "Authenticated current-commit staging smoke, payment/provider sandbox-to-live cutover rehearsal, mobile customer/courier/merchant E2E, approved k6 capacity window with resource headroom, and live region failover/reconciliation evidence."

unproven_requirements: "Market config/provider/payment readiness as a launch gate, full customer/courier/merchant localized E2E, provider/payment sandbox-live cutover, measured load/capacity acceptance, and live region failover remain unproven."
known_blockers: "The staging deploy job cannot SSH because GitHub Actions secret STAGING_SSH_HOST is unset; provider/payment sandbox-live references and an approved staging load window are unavailable; no usable Android emulator/device session is available on this host."

locally_actionable_remaining: NONE

blocker_resolution_attempts: "Implemented repository launch preflight, launch runbook and checklist; ran the deterministic region fence/promotion/replay drill; ran full admin tests, order-service tests and integration-gateway tests; ran local PostgreSQL market/compliance readiness inspection; ran customer web Chromium localization E2E; probed public staging health/market/version endpoints; ran public staging Chromium localization against https://app.bawain.my.id; inspected the staging CI deploy log; checked GitHub repository/environment secret inventories and confirmed no STAGING_SSH_HOST or SSH deploy key; checked Docker, k6, Android Studio MCP, ADB and four available AVDs; retried emulator startup with SDK environment, alternate AVD, explicit port, read-only mode, software acceleration and a clean ADB restart. The final Pixel_5_kurir run booted QEMU internally in 40235ms but emitted repeated `adb.exe: no emulators found` warnings and never appeared in `adb devices`."
unblock_condition: "Configure authorized staging SSH deployment and provider/payment secret references, provide an approved capacity-test window with runtime metrics, and provide an authorized Android emulator/device that reaches adb device state."

owner_action_required: true
owner_action_summary: "Owner must configure the staging deployment/provider/runtime prerequisites through secure platform channels; no secret should be pasted into this repository, task evidence, logs or chat."
verification_after_unblock: "Rerun the staging workflow against the same branch/commit, confirm deployed=true and current-commit health/version/market responses, run authenticated old/current customer-courier-merchant smoke, rehearse payment/provider sandbox-live cutover and rollback, run the approved k6 profile with headroom metrics, execute live region failover/fencing/replay/reconciliation, then attach redacted evidence and re-run the task validator."

dependency_chain_blocked: true
next_eligible_task: NONE — this is the final market launch gate.

blocked_since: 2026-09-09
retry_when: "When staging SSH deployment, provider/payment environment access, approved load window and authorized mobile runtime are available."
updated_at: 2026-09-09
---

# Evidence — GLOB-2026-014

## Acceptance Criteria Source

The original checklist in `task-food-marketplace-parity-2026.md` requires:

- Market config/compliance/payment/maps/providers/tax/support/on-call/data residency complete.
- Localized customer/courier/merchant flows pass E2E.
- Currency/tax/refund/payout reconciliation passes.
- Provider and payment sandbox/live credential cutover rehearsed.
- Load/capacity and region-failover checks pass.
- Kill switches and rollback tested before launch.
- Market can be disabled/degraded without shipping a new app binary.

## Scope Implemented

- Added `scripts/market_launch_readiness.py` as a deterministic repository-contract preflight. It validates the existing market control plane, compliance boundary, money/tax/reconciliation contracts, payment/maps/logistics readiness boundaries, support/on-call ownership, data residency, capacity profile, localized client contract, kill-switch path, rollback runbook and region catalog; it can also execute the deterministic region failover/fence/promotion/outbox-replay drill.
- Added `docs/runbooks/launch-new-market.md` with ownership, evidence, payment/provider cutover and rollback handling, localized client verification, capacity/failover procedure, canary/kill-switch/abort rules and secret handling.
- Added `docs/checklists/market-launch-readiness.md` as the launch record without creating a second market source of truth. It records the actual local/CI/staging probe limitations and keeps hard launch rows `NOT_RUN` until owner/environment evidence exists.
- Added a regression test proving an already-active market fails closed with typed `MARKET_CONFIGURATION_UNAVAILABLE` after it is paused; no app binary update is required for the degradation.
- Marked only the reconciliation, local kill-switch/rollback, and no-binary market-disable requirements checked in the master blueprint. External launch-gate requirements remain unchecked.

## Files Changed

- `scripts/market_launch_readiness.py` — repository preflight and deterministic drill entry point.
- `docs/runbooks/launch-new-market.md` — new-market operational runbook.
- `docs/checklists/market-launch-readiness.md` — launch-gate record and current verification record.
- `backend/admin-service/src/services/marketConfig.test.ts` — paused-market fail-closed regression test.
- `task-food-marketplace-parity-2026.md` — only the three requirements proven by local evidence were checked.

## Commands / Checks Run

    command: python scripts/market_launch_readiness.py --validate --drill
    result: PASS — 14 repository/drill checks; external runtime explicitly NOT_RUN.

    command: python scripts/mobile/check_localization.py
    result: PASS — 3 Android apps, 202 web keys, ID/EN parity and RTL-ready direction contract.

    command: python scripts/region_failover_drill.py --validate; python scripts/region_failover_drill.py --drill
    result: PASS — catalog validation and 6-check deterministic fence/promotion/replay/deferred-notification drill.

    command: npm test -- --runInBand (backend/admin-service)
    result: PASS — 95 suites, 393 tests.

    command: npm run build (backend/admin-service)
    result: PASS.

    command: go test ./... (backend/order-service)
    result: PASS — all order-service packages.

    command: go test ./... (backend/integration-gateway)
    result: PASS — all integration-gateway packages.

    command: npx playwright test e2e/localization.spec.ts --project=chromium (frontend, BASE_URL=http://localhost:3004)
    result: PASS — 3 customer web localization/direction/long-copy tests.

    command: .\gradlew.bat test (android-app)
    result: PASS — BUILD SUCCESSFUL; courier debug/release unit-test tasks completed. This is local logic/build evidence, not interactive device E2E.

    command: .\gradlew.bat test (android-app-customer)
    result: PASS — BUILD SUCCESSFUL; customer debug/release unit-test tasks completed. This is local logic/build evidence, not interactive device E2E.

    command: .\gradlew.bat test (android-app-merchant)
    result: PASS — BUILD SUCCESSFUL; merchant debug/release unit-test tasks completed. This is local logic/build evidence, not interactive device E2E.

    command: npx playwright test e2e/localization.spec.ts --project=chromium (frontend, BASE_URL=https://app.bawain.my.id)
    result: FAIL for staging release validation — 1 test failed because the public deployment has no `Pilih bahasa` locale selector; 2 RTL/long-copy tests passed. This is the currently deployed older app, not proof of the pushed commit.

    command: local PostgreSQL readiness query
    result: PASS — local `id-jk` readiness true with no reason codes; 9 active compliance requirements, 4 active data policies and 5 enabled services. This does not prove staging readiness.

    command: GET https://api.bawain.my.id/health
    result: PASS — HTTP 200 from the currently deployed public service.

    command: GET https://api.bawain.my.id/api/v1/markets/config?market_code=id-jk
    result: PASS — HTTP 200 public response; current service returned config version 1 but null launch/readiness/provider-reference fields, so it is not treated as launch proof.

    command: GET https://api.bawain.my.id/api/v1/system/latest-version?type=merchant with compatibility headers
    result: FAIL for current-commit validation — HTTP 400; customer/courier returned 200 without compatibility metadata, showing the pushed GLOB-2026-013 code is not deployed.

    tool: GitHub Actions run 34300594353 for commit d44bb6a3
    result: PASS for repository verification; Admin/Go/frontend/build/migration/security/region jobs, security scan and container audits passed. Deploy guard logged missing `STAGING_SSH_HOST` and set `deployed=false`; Release Validation Pending passed, browser E2E and API Regression (k6) were skipped.

    tool: Android Studio MCP, ADB and Android emulator
    result: NOT_RUN for interactive E2E — MCP configuration call did not produce a usable session; ADB had no device. Four AVDs were listed; alternate emulator startup reached host startup but failed to produce an adb device, another retry reported too many emulator instances, and the clean-ADB Pixel_5_kurir retry logged internal boot completion in 40235ms while still reporting `no emulators found`. No device result is claimed.

    command: git diff --check
    result: PASS.

## Task-Local Verification

### Tests

Status: PASS

Evidence: Full admin-service tests passed with 95 suites/393 tests. Full order-service and integration-gateway Go test suites passed. Courier, customer and merchant Android debug/release unit-test suites also completed successfully. The new paused-market fail-closed regression is included in the admin suite.

### Integration

Status: PASS

Evidence: Admin TypeScript build passed; existing service boundaries, market/compliance contracts, finance/provider tests and deterministic region recovery are wired and exercised. Local PostgreSQL readiness was read from the canonical schema without changing data. CI run 34300594353 also passed repository build, migration, security, container-audit and region-contract jobs for the pushed branch.

### E2E

Status: PARTIAL

Evidence: Customer web Chromium localization E2E passed 3/3 tests, and courier/customer/merchant Android debug/release unit-test suites passed. Courier and merchant interactive flows, mobile order/payment/payout/settlement journeys and full localized market flow were not run because no usable Android runtime/device was available. CI browser E2E was skipped because the staging deploy output was `deployed=false`.

### Migration

Status: N/A

Evidence: GLOB-2026-014 adds no schema or stored-data migration. Existing market/compliance/finance/region migrations are reused; no new migration result is represented as this task's work.

### Observability

Status: PARTIAL

Evidence: The runbook requires correlation ID, canary, SLO, provider, payment and reconciliation evidence, and existing service catalog/observability contracts were validated. Live staging metrics, provider dashboards and capacity resource headroom were not available because the current commit was not deployed and no approved load window existed.

### Security / Privacy

Status: PASS

Evidence: Launch docs explicitly prohibit keys, service-account JSON, private keys, raw payment/provider payloads, customer contact data and auth tokens in evidence. The staging workflow masks secrets and the security scan for the pushed commit passed. No secret was added to code, logs or evidence.

### Rollback / Recovery

Status: PASS

Evidence: The deterministic region drill passed initial write, primary write-fence, approved standby promotion, outbox replay deduplication and deferred non-critical notification checks. Existing pricing/payout kill-switch tests passed, and the new paused-market resolver regression confirms typed fail-closed degradation without a client binary. Live cloud failover and operator rollback remain release blockers, not fabricated as passed.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value: `true`

Reason: This original GLOB is explicitly a P0 market launch readiness/release gate and includes E2E, provider/payment cutover rehearsal, measured load/capacity, region-failover acceptance and pre-launch rollback/kill-switch proof.

### External Runtime Validation

Status: PARTIAL

Evidence: Public staging health returned HTTP 200, but the current deployment did not contain the pushed compatibility contract: merchant latest-version returned HTTP 400 and customer/courier responses lacked compatibility metadata. Public staging Chromium also failed the locale-selector assertion (1/3 tests), confirming the exposed app is not the locally verified current frontend. GitHub run `34300594353` job `Deploy to Staging` completed its guard successfully while logging that `STAGING_SSH_HOST` was unset and setting `deployed=false`; actual current-commit staging validation therefore did not occur.

### Release Readiness

Status: BLOCKED

Evidence: Repository and local logic gates are implemented and passing, but the hard launch gate cannot pass while staging deployment, provider/payment cutover, mobile E2E, measured capacity and live failover evidence are unavailable. The market must remain non-active or paused.

### Release Follow-ups

- Configure authorized staging SSH deployment and rerun the staging workflow until `deployed=true` for the target commit.
- Run authenticated current/legacy client compatibility and localized customer/courier/merchant flows.
- Provide redacted payment and logistics provider sandbox/live cutover plus rollback/reconciliation records.
- Run the approved k6 capacity profile with latency/error thresholds, queue/provider saturation and measured resource headroom.
- Execute the live region-failover runbook with fencing, RPO/RTO, replay/dedupe, transaction smoke and reconciliation evidence.

## External Blockers

- GitHub Actions secret `STAGING_SSH_HOST` is unset, so the workflow cannot deploy the pushed `staging` commit to the VPS. The workflow's deploy guard explicitly recorded this and skipped downstream staging E2E/k6 jobs.
- No authorized payment/provider sandbox-live credentials, secret-manager references and operator cutover window are available in this session. Local fixtures/tests prove mapping and failure semantics only; they cannot prove provider availability or credential cutover.
- No approved staging load-test window/resource-headroom environment is available. `k6` is not installed locally, and running an unapproved load test against a public endpoint would not be a safe or valid substitute.
- No usable Android runtime is available: `adb devices` is empty; Android Studio MCP did not provide a usable run session; AVD startup retries did not reach an adb `device` state, including an alternate AVD/port/read-only/software-acceleration attempt.

## Blocker Resolution Attempts

All locally actionable work was exhausted: repository preflight and runbook/checklist were added; local DB readiness and full backend suites passed; courier/customer/merchant Android unit-test suites passed; public staging was probed without credentials; CI deploy logs were inspected; Docker/k6/ADB/Android Studio availability was checked; and emulator alternatives were attempted. No safe path remains to manufacture provider, live capacity or mobile runtime evidence.

## Available Tools Checked

- Git and GitHub Actions/`gh`: push and repository CI inspection available; deploy secret is absent.
- GitHub repository and `staging` environment secret inventories: no `STAGING_SSH_HOST` or SSH deploy key was exposed to the workflow environment.
- Local PostgreSQL/psql/goose: available and used for read-only readiness plus prior migration verification.
- Docker: CLI not installed.
- k6: not installed.
- Android SDK/ADB/emulator: installed/listed, but no usable device session.
- Android Studio MCP: checked, no usable run configuration/session returned.
- Playwright/Chromium: available and used for customer web localization E2E.

## Owner Action Required

### Why Owner Action Is Required

The remaining original acceptance criteria require access-controlled staging deployment, provider/payment accounts, an approved capacity window and an Android runtime. Those resources cannot be created or safely operated from this repository-only session.

### What Is Missing

- GitHub Actions staging SSH host and matching deployment credentials.
- Staging secret-manager references for payment, maps and logistics providers, with sandbox/live operator access.
- An approved load-test window, k6 runner and staging resource metrics.
- An authorized Android emulator/device that reaches `adb devices` as `device`.

### What The Owner Must Do

1. In the repository's GitHub Actions secrets, configure `STAGING_SSH_HOST` and `STAGING_SSH_PRIVATE_KEY`; configure `STAGING_SSH_USER` and optional `STAGING_SSH_PORT` when they differ from workflow defaults. Ensure the matching public key is authorized on the staging deploy account.
2. On the staging secret manager/VPS environment (not Git), configure the required payment, maps, logistics and tax/provider references and verify DNS/TLS for the workflow's staging API URL. Do not paste secret values into chat, source, evidence, screenshots or logs.
3. Schedule an approved staging QA/load window and make k6 plus metrics/resource headroom available. Do not run the load profile against the public endpoint outside that window.
4. Start/provide an authorized Android emulator or physical device with the customer, courier and merchant builds installed; confirm `adb devices` reports the serial as `device`.
5. Re-run the staging workflow for the target `staging` commit and retain only redacted run IDs, correlation IDs, aggregate metrics and provider cutover records.

### Where It Must Be Configured

- GitHub repository → Settings → Secrets and variables → Actions for `STAGING_SSH_HOST`, `STAGING_SSH_PRIVATE_KEY`, and optional SSH values.
- Existing staging VPS secret manager/config path used by `.github/workflows/staging.yml` (the repository runbook references `/opt/tembus/secrets/.env.production` for the server-side secret file; use the actual deployment path if it differs).
- Payment/maps/logistics provider consoles and the approved secret manager, never Git.
- Approved QA/load environment and the Android host/emulator/device.

### Security Handling

SSH private keys, payment keys, maps keys, provider tokens, service accounts and auth credentials are secret. Never commit or paste them into the repository, evidence, screenshots, logs or chat. Evidence must contain only secret-manager references, redacted run IDs, aggregate results and safe correlation metadata.

### What The Agent Needs Afterwards

A successful workflow run ID with `deployed=true`, the reachable current-commit staging URL, redacted provider/cutover evidence, the approved load-test result and an `adb devices` serial in `device` state. No secret values are needed in chat.

### Verification After Unblock

The agent will run current-commit health/market/version probes, authenticated compatibility and localized client E2E, provider/payment cutover and rollback/reconciliation probes, approved k6 capacity/failover tests, inspect logs/metrics for correlation and then update this evidence and the master checklist. Only after all seven original boxes have direct evidence will status move to `COMPLETE`.

## Dependency Impact

`dependency_chain_blocked: true`. GLOB-2026-014 is the final market launch gate; no dependent task is advanced while the external launch criteria remain unproven.

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `BLOCKED`

Evidence: The repository contract, local finance/reconciliation verification, deterministic recovery logic, fail-closed market disable path, browser customer localization E2E, full backend tests, CI verification and operational runbook are real and recorded. The original release-gate requirements for current-commit staging, provider/payment cutover, mobile E2E, measured capacity and live failover remain unproven because the required external resources are unavailable.

### REALITY-2026-011 — No Fake Completeness

Status: `PASS`

Evidence: Only three original checklist items with direct local proof are checked. Customer web-only E2E is labeled partial; the public staging health result is not relabeled as current-commit deployment; CI's `success` guard is not relabeled as `deployed`; mocks, fixtures, deterministic drills and local readiness are not represented as provider/live/cloud proof.

## Unproven / Remaining

- Market config/compliance/payment/maps/provider/tax/support/on-call/data-residency launch sign-off in the target staging environment.
- Full localized customer, courier and merchant E2E, including order/payment/payout/settlement surfaces.
- Provider and payment sandbox/live credential cutover and rollback rehearsal.
- Measured capacity/load headroom and live region-failover/RPO/RTO/reconciliation evidence.

## Status Decision

`status: BLOCKED` because the original GLOB is explicitly a P0 release gate, external proof is part of its acceptance criteria, all safe local implementation and verification work is complete, and the remaining criteria require staging/provider/load/device resources not available in this session.
