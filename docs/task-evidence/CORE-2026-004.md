---
task_id: CORE-2026-004
status: COMPLETE
reality_2026_003: PASS
reality_2026_011: PASS
implementation_ref: d1b9e692
tests: PASS
integration: PASS
e2e: N/A
e2e_na_reason: "The original CORE-2026-004 acceptance is a database-backed lifecycle invariant; isolated PostgreSQL integration verification covers the applicable task scope."
migration: PASS
migration_na_reason: ""
observability: PASS
security_privacy: PASS
rollback_recovery: PASS
task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN
release_readiness: NOT_RUN
release_followups: "Authenticated staging race/replay validation remains release follow-up; it is not an original CORE-2026-004 acceptance criterion."
unproven_requirements: NONE
known_blockers: NONE
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Implemented and committed the canonical transaction boundary, verified the migrated schema on local PostgreSQL, ran full Go tests/vet, and exercised isolated concurrent delivery, replay, proof rollback, admin reason/audit, and courier assignment scenarios."
unblock_condition: NONE
owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE
dependency_chain_blocked: false
next_eligible_task: "CORE-2026-006"
blocked_since: NONE
retry_when: NONE
updated_at: 2026-09-02
---

# Evidence — CORE-2026-004

## Acceptance Criteria Source

- [x] Allowed transition defined per service + actor.
- [x] Optimistic version/row locking prevents race.
- [x] Terminal state tidak mundur karena delayed/replayed event.
- [x] State + audit + required proof/ledger effects transactional.
- [x] Duplicate event idempotent; invalid transition typed error.
- [x] Admin override reasoned/audited.

## Scope Implemented

- Added a canonical `OrderTransitionRepository` boundary that locks the order,
  validates lifecycle and actor authorization, persists state/audit/proof/ledger
  effects in one PostgreSQL transaction, and commits only after all effects pass.
- Added typed proof-required, ledger-required, admin-reason-required, invalid,
  and concurrent transition errors.
- Added idempotency keys and unique indexes for transition events and package
  proofs, including concurrent replay protection.
- Routed merchant food acceptance/rejection, scheduled food transitions,
  carrier events, courier assignment/ghost release, payment timeout, generic
  status updates, and delivery confirmation through the canonical boundary.
- Kept the existing `state_version` trigger and added the transition audit,
  proof idempotency, and admin reason schema migration.

## Files Changed

- `backend/order-service/internal/domain/order_state_machine.go` — explicit
  lifecycle and actor matrix.
- `backend/order-service/internal/domain/order_transition.go` — transition
  contract and optional assignment effects.
- `backend/order-service/internal/repository/postgres_order_transition_repository.go` —
  transactional state/audit/proof/ledger implementation.
- `backend/order-service/internal/repository/postgres_order_transition_integration_test.go` —
  isolated PostgreSQL concurrency, replay, proof, admin, and assignment tests.
- `backend/order-service/internal/repository/food_repository.go` — food status
  mutations routed through the boundary.
- `backend/order-service/internal/repository/postgres_repository.go` — timeout,
  assignment, ghost-release, and package-scan schema corrections.
- `backend/order-service/internal/repository/merchant_settlement_repository.go` —
  delivery confirmation routed through the boundary.
- `backend/order-service/internal/service/*.go` and `internal/handler/*.go` —
  actor-aware/idempotent callers and typed error mapping.
- `database/migrations/20260902000003_atomic_order_transitions.sql` — audit,
  idempotency, and admin-reason schema support.

## Commands / Checks Run

```text
go test ./... (backend/order-service) — PASS
go vet ./... (backend/order-service) — PASS
go test ./internal/repository -run 'TestPostgresTransition|TestPostgresAssignCourier' -count=1 with isolated PostgreSQL clone — PASS
Local PostgreSQL schema checks for transition columns, indexes, and admin reason constraint — PASS
graphify update . — PASS (graph rebuilt; SQL parser warning documented by tool)
git diff --check — PASS
```

## Task-Local Verification

### Tests

Status: PASS

Evidence: Full `go test ./...` and `go vet ./...` pass for the order-service
after the final implementation commit.

### Integration

Status: PASS

Evidence: An isolated PostgreSQL database cloned from the local `tembus`
schema passed concurrent delivery (one commit/one replay), exactly-once audit,
proof and delivery ledger assertions, missing-proof rollback, typed admin
reason enforcement, reasoned admin audit, and idempotent courier assignment.

### E2E

Status: N/A

Evidence: No original CORE-2026-004 checklist item requires a UI or external
provider E2E flow. Authenticated staging validation is tracked separately as
release follow-up.

### Migration

Status: PASS

Evidence: Local PostgreSQL contains all seven transition audit columns, the
three required indexes, and `order_admin_override_reason_check` from the
CORE-004 migration contract.

## Reality Gate Evaluation

- `REALITY-2026-003`: PASS — all six original checklist requirements are
  implemented and locally verified.
- `REALITY-2026-011`: PASS — no staging or production result is represented as
  completed; external runtime validation remains explicitly separate.

## Unproven / Remaining

NONE for the original CORE-2026-004 acceptance scope. Authenticated staging
race/replay checks remain release follow-up only.
