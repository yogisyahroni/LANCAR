---
task_id: ADMEXP-2026-016
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: 52f30803

tests: PASS
integration: PASS
e2e: N/A
e2e_na_reason: "The original task requires revision/audit control-plane behavior; authenticated staging GUI smoke remains a release follow-up under GLOB-2026-014 and was not represented as local proof."

migration: N/A
migration_na_reason: "No schema or stored-data change was required. The task reuses the existing immutable manifest revision and append-only audit tables/triggers."

observability: PASS
security_privacy: PASS
rollback_recovery: PASS

task_scope_external_proof_required: false
external_runtime_validation: NOT_RUN

release_readiness: PARTIAL
release_followups: "Deploy the implementation to staging and run authenticated audit-filter, field-diff and compatible-rollback GUI smoke under GLOB-2026-014."

unproven_requirements: NONE
known_blockers: NONE

locally_actionable_remaining: NONE

blocker_resolution_attempts: NONE
unblock_condition: NONE

owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE

dependency_chain_blocked: false
next_eligible_task: ADMEXP-2026-017

updated_at: 2026-09-10
---

# Evidence — ADMEXP-2026-016

## Acceptance Criteria Source

- [x] Every draft/publish/approval/reject/pause/rollback/kill-switch action records actor + timestamp + reason.
- [x] Diff shows previous vs new content, targeting, schedule, assets, deep links, flags and token changes.
- [x] Historical published revision is immutable.
- [x] Admin can filter history by campaign, market, surface, actor, date and action.
- [x] Audit history cannot be deleted through normal Experience UI.
- [x] Rollback link only appears for compatible known-good revisions.

## Scope Implemented

- Extended the server preview diff with field-level previous/new values for content, targeting, schedule, assets, deep links, flags and design tokens. The current published revision is loaded from the canonical manifest store before comparison; the public resolver remains the source of live selection.
- Added audit API filters for campaign/manifest, market, surface, actor UUID, action and created-at range. The overview now exposes those filters and renders actor, timestamp and reason for each event.
- Kept pause semantics on the existing manifest kill-switch lifecycle. `kill_switched` and `kill_switch_cleared` are audited with the authenticated/system actor, reason and database timestamp; all other lifecycle functions already route through the same audit writer.
- Preserved database-level immutable historical payloads and append-only audit rows. The normal Experience UI has no delete path for revisions or audit history.
- Restricted rollback controls to older historical revisions with matching market, surface and schema, excluding kill-switched targets. The backend enforces the same compatibility check and revalidates the target checksum before publishing it.

## Files Changed

- `backend/admin-service/src/services/experienceConfig.ts` — field-level diff, audit filters and compatible rollback enforcement.
- `backend/admin-service/src/services/experienceConfig.test.ts` — previous/new diff, audit filter and integrity-aware rollback coverage.
- `backend/admin-service/src/controllers/experienceConfig.controller.ts` — forwards audit filter query parameters.
- `admin-dashboard/src/components/experience/RevisionDiffPreview.tsx` — renders field-level previous/new values.
- `admin-dashboard/src/components/experience/RevisionHistory.tsx` — displays audit reasons and only compatible known-good rollback actions.
- `admin-dashboard/src/components/experience/types.ts` — diff field-change contract.
- `admin-dashboard/src/pages/AppExperience.tsx` — passes current published revision to history and preserves existing UI contract strings.
- `admin-dashboard/src/pages/AppExperienceOverview.tsx` — campaign/market/surface/actor/date/action audit history filters and table.
- `admin-dashboard/src/pages/AppExperienceEditor.tsx` — preserves service-visibility component filtering while retaining the existing Home Layout contract.
- `admin-dashboard/src/components/experience/ExperiencePreview.tsx` — preserves equivalent enabled-section filtering and existing contract coverage.
- `task-food-marketplace-parity-2026.md` — checked only the six proven ADMEXP-2026-016 requirements.

## Commands / Checks Run

    command: npx jest --runInBand --forceExit --silent src/services/experienceConfig.test.ts (backend/admin-service)
    result: PASS — 1 suite, 53 tests.

    command: npx jest --runInBand --forceExit --silent src/homeLayoutEditorContract.test.ts src/bannerPromoContentEditorContract.test.ts src/campaignIntroEditorContract.test.ts (backend/admin-service)
    result: PASS — 3 suites, 6 tests.

    command: npx jest --runInBand --forceExit --silent (backend/admin-service)
    result: PASS — 108 suites, 496 tests.

    command: npm run build (backend/admin-service)
    result: PASS — TypeScript production build completed.

    command: npx tsc -b --pretty false (admin-dashboard)
    result: PASS — dashboard typecheck completed.

    command: npx eslint src/pages/AppExperienceOverview.tsx src/pages/AppExperience.tsx src/pages/AppExperienceEditor.tsx src/components/experience/ExperiencePreview.tsx src/components/experience/RevisionHistory.tsx src/components/experience/RevisionDiffPreview.tsx src/components/experience/types.ts (admin-dashboard)
    result: PASS — 0 errors; 5 warning-only findings remain, including the existing AppExperience effect/unused value and Overview purity/dependency warnings.

    command: $env:VITE_API_URL='https://api.bawain.my.id/api/v1'; $env:VITE_WS_URL='wss://api.bawain.my.id'; $env:VITE_SOCKET_URL='https://api.bawain.my.id'; npm run build (admin-dashboard)
    result: PASS — 1,644 modules transformed; existing large-chunk warning remains.

    command: git diff --check
    result: PASS — no whitespace errors.

## Task-Local Verification

### Tests

Status:

PASS

Evidence:

The service suite proves field-level previous/new diff output, canonical current-revision lookup, audit filtering by all requested dimensions, checksum-aware rollback, and the existing lifecycle/audit behavior. The full Admin regression suite passed 108/108 suites and 496/496 tests.

### Integration

Status:

PASS

Evidence:

The authenticated audit endpoint forwards filters into the existing Experience authorization boundary and canonical `experience_manifest_audit` query. The dashboard consumes the same response and links each event to the relevant immutable manifest revision.

### E2E

Status:

N/A

Evidence:

No authenticated staging/browser session was available in this task-local run. The production dashboard build, TypeScript check and source contract tests prove the GUI wiring; live operator smoke is tracked separately by GLOB-2026-014.

### Migration

Status:

N/A

Evidence:

No migration was added. Existing database triggers prevent mutation/deletion of published historical payloads and audit rows, and the implementation uses those tables without creating a duplicate audit source.

### Observability

Status:

PASS

Evidence:

Audit records retain action, actor, reason, correlation ID, previous/new state, revision metadata and database `created_at`. The GUI renders actor, timestamp and reason, while existing structured request/correlation logging remains on the controller path.

### Security / Privacy

Status:

PASS

Evidence:

Audit filtering remains behind the existing Experience read capability and validates UUID/action/date inputs. The diff is built from allowlisted presentation manifest fields; no customer identity or secret is added. Revision mutation/deletion remains blocked by the database immutability boundary, and rollback compatibility is enforced server-side rather than only by the GUI.

### Rollback / Recovery

Status:

PASS

Evidence:

Rollback continues to use the transactional advisory lock and immutable historical payload. The service rejects current/newer, incompatible, kill-switched or checksum-invalid targets; the dashboard hides the action for non-compatible revisions. Existing rollback tests and the full regression suite passed.

## External Runtime / Release Validation

### Is external proof required by the original TASK-ID?

Value:

`false`

Reason:

ADMEXP-2026-016 is a revision/audit control-plane task. Staging GUI execution is a release gate tracked by GLOB-2026-014, not an original acceptance item for this task.

### External Runtime Validation

Status:

NOT_RUN

Evidence:

No staging or device claim is made here.

### Release Readiness

Status:

PARTIAL

Evidence:

Task-local implementation is complete. Release readiness still requires deployment and authenticated staging smoke under GLOB-2026-014.

## Reality Gate Evaluation

- REALITY-2026-003: PASS — all six original checklist items are linked to this evidence and verified locally.
- REALITY-2026-011: PASS — no staging, provider, device or production result is represented as local proof.

## Unproven / Remaining

NONE for ADMEXP-2026-016. `ADMEXP-2026-017` is the next eligible task; GLOB-2026-014 remains a separate external release gate.
