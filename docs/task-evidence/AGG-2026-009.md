---
task_id: AGG-2026-009
status: PARTIAL
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: 569c1194
tests: PASS
e2e_staging: NOT_RUN
migration: NOT_RUN
observability: PARTIAL
security_privacy: PASS
rollback_recovery: PARTIAL
unproven_requirements: "Provider/service cards do not yet receive a complete capability, limitation, or provider-authoritative chargeable-weight contract; staging browser proof is not available."
known_blockers: "Docker, staging database, and provider sandbox are unavailable in this workspace."
updated_at: 2026-09-01
---

# Evidence — AGG-2026-009

## Scope Implemented

- Reworked the manual customer aggregator flow into four visible stages: Pick Up, Receiver / Package, Bandingkan Kurir, and Review & Bayar.
- Removed customer-facing options for SiCepat and AnterAja because no real adapter is wired for those providers in the current integration gateway.
- Replaced hardcoded city placeholders and the fixed `CGK` origin with provider-owned area codes loaded from `provider_area_mappings`.
- Added the authenticated customer endpoint `/api/v1/logistics/locations?provider=jne|jnt`, routed through the API gateway.
- Added a visually distinct first-mile section for LANCAR pickup and carrier handoff.
- The Review & Bayar stage can only continue to order creation after a real tariff response and the final submit path still requires the persisted order reference before payment continuation.

## Files Changed

- `frontend/src/components/orders/AggregatorWizard.tsx` — four-stage flow, provider-backed area selection, first-mile presentation, and persisted-order review gate.
- `frontend/src/components/orders/AggregatorForm.tsx` — provider-backed area selection and removal of unsupported/mock options.
- `backend/admin-service/src/controllers/logisticsLocations.controller.ts` — authenticated lookup backed by `provider_area_mappings`.
- `backend/admin-service/src/controllers/logisticsLocations.controller.test.ts` — unsupported-provider, populated, empty, and missing-table cases.
- `backend/admin-service/src/routes/public.routes.ts` and `backend/admin-service/src/controllers/index.ts` — endpoint wiring.
- `backend/api-gateway/src/index.ts` — admin-service proxy and resilience policy wiring.
- `task-food-marketplace-parity-2026.md` — persisted-order criterion marked complete.

## Commands / Checks Run

```text
command: npm test -- --runInBand (frontend)
result: PASS — 4 files, 9 tests.

command: npm run build (frontend)
result: PASS — production routes generated, including /orders/new/aggregator.

command: npm run lint (frontend)
result: PASS — 0 errors; existing warnings remain.

command: npm test -- --runInBand (backend/admin-service)
result: PASS — 38 suites, 202 tests.

command: npm run build (backend/admin-service)
result: PASS.

command: npm run build (backend/api-gateway)
result: PASS.

command: git diff --check
result: PASS.

command: graphify update .
result: PASS — graph updated; known SQL parser/large visualization warnings remain.

command: Docker / staging browser E2E / provider sandbox
result: NOT RUN — Docker executable and external provider sandbox are unavailable.
```

## Reality Gate Evaluation

### REALITY-2026-003 — Evidence-based Definition of Done

Status: `PARTIAL`

Evidence:

- implementation: The customer route now requests provider-owned area codes from the database and prevents unsupported providers from being offered.
- tests: The new endpoint has four passing unit cases; frontend, admin-service, and API gateway builds pass.
- E2E/staging: Not run because Docker/staging/provider sandbox is unavailable.
- migration/backfill: Existing admin migration `008_super_aggregator_3pl.sql` defines `provider_area_mappings`, but deployment execution and mapping population were not verified here.
- observability: Endpoint failures return explicit stable error codes; no staging metric/trace evidence is available.
- security/privacy: Endpoint is behind `requireMobileOrWebAuth`; provider input is allowlisted and values are parameterized.
- rollback/recovery: Code is isolated and the existing migration remains reversible; browser recovery proof was not run.

### REALITY-2026-011 — No fake completeness gate

Status: `PASS`

Confirm with evidence, not assumptions:

- only the persisted-order criterion is marked complete in the master checklist;
- carrier capability/limitation completeness and staging E2E remain open;
- no city rows, provider capabilities, or staging result were fabricated.

## Unproven / Remaining

- Extend the tariff contract with provider/service name, ETA source, authoritative chargeable weight, capabilities, and limitations, then render those fields in every carrier card.
- Source first-mile mode from the provider capability registry rather than explanatory UI copy alone.
- Populate and verify real `provider_area_mappings` in staging.
- Run authenticated browser E2E from quote through persisted order and payment recovery.

## Blockers

Docker, staging database, and provider sandbox access are unavailable in this workspace.

## Notes / N/A Justification

No field is marked `N/A`; missing external verification remains `NOT_RUN` or `PARTIAL`.
