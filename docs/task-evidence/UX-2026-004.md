---
task_id: UX-2026-004
status: BLOCKED
reality_2026_003: PARTIAL
reality_2026_011: PASS
implementation_ref: "e5b9c925"
tests: PASS
e2e: NOT_RUN
migration: N/A
migration_na_reason: "Notification event ordering is stored in a new device-local SharedPreferences file; no server schema or persisted order data migration is required."
observability: PASS
security_privacy: PASS
rollback_recovery: PASS
unproven_requirements: "Authenticated FCM/WebSocket/device runtime verification with real versioned lifecycle events remains unproven. Background notification delivery and notification tap behavior need staging evidence."
known_blockers: "No connected Android device/staging session with configured push delivery and live order lifecycle events was available in this local implementation turn."
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Inspected the FCM service, WebSocket consumer, MainActivity allowlist, RootNavGraph routing, and device-local version store; added pure local deep-link/version policy coverage; ran the customer unit tests and compile successfully; checked graphify and the repository test/evidence tooling."
owner_action_required: true
owner_action_summary: "A staging owner must provide an authenticated Android runtime with configured FCM/WebSocket delivery and a real order lifecycle producer so delivery ordering and tap reconciliation can be observed."
verification_after_unblock: "Run the customer app on the configured device/emulator, emit two real versioned lifecycle events in reverse order through staging FCM and WebSocket, verify the stale event is ignored, then tap detail/tracking/chat notifications from foreground and background and confirm the latest authenticated API snapshot is rendered."
unblock_condition: "A configured Android runtime and staging order lifecycle event source are available, with a reproducible order ID and permission to send/test FCM/WebSocket events."
dependency_chain_blocked: true
next_eligible_task: OPS-2026-001
blocked_since: 2026-09-01
retry_when: "Retry when staging FCM/WebSocket credentials and an Android runtime are available."
updated_at: 2026-09-01
---

## Scope Implemented

- Order-service push payloads now carry `service_code`, `service_sub_type`, `target`, `event_version`, and an order deep link whenever the persisted order snapshot provides the corresponding identity/timestamp.
- Customer FCM and WebSocket consumers share a device-local per-order event-version guard; an older numeric `event_version` is ignored and therefore cannot replace the current foreground notification or trigger a stale resync hint.
- Customer notification models and WebSocket parsing retain service, target, and version metadata for routing and diagnostics.
- `tembus://orders/{id}` now opens the customer order detail route. The detail ViewModel fetches the authoritative order snapshot on entry; tracking continues to fetch current order/tracking snapshots, so notification text is never treated as order truth.
- Deep-link validation now allowlists the supported `tembus://orders`, `tembus://booking`, and debug UAT shapes before extracting an order identifier.

## Files Changed

- `backend/order-service/internal/service/push_service.go`
- `backend/order-service/internal/service/push_service_contract_test.go`
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/NotificationModels.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/service/TEMBUSFirebaseMessagingService.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/MainActivity.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/MainViewModel.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/navigation/RootNavGraph.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/navigation/CustomerDeepLinkPolicy.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/util/NotificationEventVersionStore.kt`
- `android-app-customer/app/src/test/java/com/tembus/customer/ui/navigation/CustomerDeepLinkPolicyTest.kt`
- `android-app-customer/app/src/test/java/com/tembus/customer/util/NotificationEventVersionPolicyTest.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/util/SocketManager.kt`

## Commands / Checks Run

- `go test ./internal/service ./internal/handler/...` from `backend/order-service` — PASS.
- `./gradlew.bat :app:compileDebugKotlin --no-daemon` from `android-app-customer` — PASS, `BUILD SUCCESSFUL`; existing warnings remain.
- `./gradlew.bat :app:testDebugUnitTest --no-daemon` from `android-app-customer` — PASS, `BUILD SUCCESSFUL`.
- `./gradlew.bat --stop; ./gradlew.bat --no-daemon --offline --no-watch-fs --max-workers=1 --console=plain :app:testDebugUnitTest --tests com.tembus.customer.ui.navigation.CustomerDeepLinkPolicyTest --tests com.tembus.customer.util.NotificationEventVersionPolicyTest :app:compileDebugKotlin` from `android-app-customer` — PASS, `BUILD SUCCESSFUL` in 2m42s; 2 deep-link policy tests and 2 notification-version policy tests passed.
- `git diff --check` — PASS.
- `graphify update .` — AST extraction completed for 802 files; the CLI did not terminate after extraction and was stopped after the known non-termination point. No clean graphify exit is claimed.
- `python scripts/tasks/validate_task_evidence.py` — pending until this evidence is saved.

## Reality Gate Evaluation

- `REALITY-2026-003`: PARTIAL. Backend contract tests and Android unit/build checks are real; live FCM/WebSocket delivery and device tap flows remain unavailable without the configured staging runtime/event source.
- `REALITY-2026-011`: PASS. Push payloads are derived from the persisted order snapshot, stale events are rejected by version, and deep-link screens reconcile through the existing authenticated API rather than trusting notification text.

## Unproven / Remaining

- No locally actionable requirements remain. The remaining proof is staging/device runtime evidence only.
- Owner action: (1) configure the authorized staging FCM/WebSocket project and test order; (2) provide/run an Android runtime with the app configuration; (3) emit versioned events in normal and reverse order; (4) record redacted delivery/tap results without committing secrets or PII.
- Security: never place FCM credentials, tokens, API keys, or customer/order PII in this evidence, logs, screenshots, or commits.
