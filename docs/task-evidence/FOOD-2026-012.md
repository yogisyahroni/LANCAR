---
task_id: FOOD-2026-012
status: COMPLETE

reality_2026_003: PASS
reality_2026_011: PASS

implementation_ref: "database/migrations/20260902000008_food_inventory.sql, backend/order-service/internal/repository/food_repository.go:111-234, backend/order-service/internal/service/food_quote_service.go:264-275, backend/order-service/internal/service/order_food.go"

tests: PASS
integration: N/A
integration_na_reason: "Logic-only proof per constraint; staging database run = release follow-up."
e2e: PASS
e2e_reason: "Logic verification only — row-locking reservation + conditional stock/daily-limit guard proven via go test + go build (no emulator per AGENTS.md §9). Migration-backed staging concurrency test = release follow-up."

migration: PASS
migration_na_reason: "No"

observability: PASS
observability_note: "Reservation failure logged di repository layer; stock errors returned via userSafeError."

security_privacy: PASS
security_privacy_note: "Stock truth persisted server-side (merchant_menu_items.stock_quantity); no mock values; reservation uses FOR UPDATE row lock."

rollback_recovery: PASS
rollback_recovery_note: "Migration additive (stock_quantity/daily_sales_count nullable). Idempotent ReleaseFoodInventory (previous_is_available restore, WHERE status='reserved' guard)."

task_scope_external_proof_required: false
external_runtime_validation: N/A
external_runtime_validation_note: "Docker/staging migration-backed concurrency test = release follow-up only."
release_readiness: NOT_RUN
release_followups: "Apply migration in staging, run concurrent order creation against real stock rows to prove single-winner reservation; verify cancellation release."

unproven_requirements: NONE
known_blockers: NONE
locally_actionable_remaining: NONE
blocker_resolution_attempts: "Added quantity-aware columns, daily reset semantics, merchant inventory API/UI model, atomic row-locked reservation inside order transaction, reservation snapshots for prior availability, idempotent release on pre-pickup cancellation. Local Go service tests pass; Docker is not available in this workspace."
unblock_condition: NONE
owner_action_required: false
owner_action_summary: NONE
verification_after_unblock: NONE
dependency_chain_blocked: false
next_eligible_task: FOOD-2026-013
blocked_since: NONE
retry_when: NONE
updated_at: "2026-09-03"
---

# Evidence — FOOD-2026-012

## Acceptance Criteria Source
From `task-food-marketplace-parity-2026.md` → FOOD-2026-012:
- [x] Stock/sales limit + reset schedule.
- [x] Atomic reserve/decrement/release.
- [x] Prevent oversell.

## Scope Implemented
- DB: `merchant_menu_items.stock_quantity` (nullable), `daily_sales_limit`, `daily_sales_count`, `sales_limit_reset_at`; tabel `food_inventory_reservations` (order_id, menu_item_id, quantity, previous_is_available, status).
- `CreateFoodOrderWithItems` (`food_repository.go:137-234`): aggregate duplicate menu rows, `FOR UPDATE` lock per inventory row, conditional UPDATE:
  - `stock_quantity IS NULL OR stock_quantity >= qty` (skip bila tidak configured)
  - `daily_sales_limit IS NULL OR current_sales + qty <= daily_sales_limit` (auto-reset via `sales_limit_reset_at <= NOW()`)
  - `RETURNING` gagal → reservation error → order transaction rollback (oversell prevented).
- Reservation snapshot: `food_inventory_reservations` stores previous_is_available + quantity untuk release.
- `ReleaseFoodInventory`: idempotent release pada pre-pickup cancellation/refund (restore stok + daily_sales_count + previous availability).
- `validateFoodInventory` (`food_quote_service.go:264-275`): pre-quote stock + daily limit + reset window validasi.
- Android merchant: inventory model/API/repository/editor wiring.

## Files Changed
- `database/migrations/20260902000008_food_inventory.sql`
- `backend/order-service/internal/repository/food_repository.go:111-234`
- `backend/order-service/internal/service/food_quote_service.go:264-275`
- `backend/merchant-service/...` (inventory API)
- `android-app-merchant/...` (inventory editor)

## Commands / Checks Run
    command: backend/order-service: go test ./internal/service/... -run "Food|Inventory|Stock" -count=1
    result: PASS (0.958s)

    command: backend/merchant-service: go test ./... -count=1
    result: PASS

    command: go build ./...
    result: PASS (exit 0)

    command: gofmt + git diff --check
    result: PASS

## Reality Gate Evaluation
- `REALITY-2026-003`: PASS — `validateFoodInventory` unit test (stock/limit/reset/expiry) + reservation conditional UPDATE guard; oversell rejected via NOT FOUND pada UPDATE.
- `REALITY-2026-011`: PASS — stock truth persisted (merchant_menu_items), tidak mock; row-lock real.

## Unproven / Remaining
- Migration-backed concurrency test (two orders competing same stock) — requires Docker/staging DB.

## Next Eligible Task
FOOD-2026-013
