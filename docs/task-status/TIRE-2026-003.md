# TIRE-2026-003 — Quote + on-site adjustment approval

**Status:** DONE — implementation/code path

**Source task:** `task-food-marketplace-parity-2026.md`

> Master task intentionally left untouched. This file is the implementation-status record requested for later manual comparison on device.

## Checklist status

- [x] Initial quote snapshot.
  - Adjustment proposal fails closed when the order has no authoritative `quote_id` / `pricing_snapshot`.
  - The adjustment record retains the initial quote id, initial pricing snapshot, original order total, delta, and proposed total.
- [x] Extra material/work becomes structured adjustment.
  - Courier proposes typed `material` / `labor` line items with code, label, quantity, unit price, and computed line total.
  - Server recomputes and validates totals and applies a safety cap.
- [x] Customer explicitly approves price delta.
  - Customer order detail loads pending adjustments, shows line-item breakdown, original total, delta, and proposed total.
  - Approval and rejection are explicit actions; rejection requires a reason.
- [x] Approval + money + audit is atomic/idempotent.
  - Approval transaction locks adjustment/order, validates customer ownership and stale totals, updates the order total once, records approved delta/financial state, and inserts an audit record before commit.
  - Proposal and decision use persisted idempotency keys/fingerprints in addition to shared HTTP idempotency middleware.

## Main implementation files

### Backend
- `backend/order-service/internal/domain/service_adjustment.go`
- `backend/order-service/internal/service/service_adjustment_service.go`
- `backend/order-service/internal/repository/service_adjustment_repository.go`
- `backend/order-service/internal/handler/service_adjustment_handler.go`
- `backend/order-service/cmd/api/main.go`
- service-adjustment database migration under `database/migrations/`

### Customer Android
- `android-app-customer/app/src/main/java/com/tembus/customer/data/model/ServiceAdjustmentModels.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/data/api/TEMBUSApiService.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/data/repository/OrderRepository.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderDetailViewModel.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/OrderDetailScreen.kt`
- `android-app-customer/app/src/main/java/com/tembus/customer/ui/screens/detail/ServiceAdjustmentSection.kt`

### Courier Android
- `android-app/app/src/main/java/com/tembus/courier/data/model/ServiceAdjustmentModels.kt`
- `android-app/app/src/main/java/com/tembus/courier/data/api/TEMBUSApiService.kt`
- `android-app/app/src/main/java/com/tembus/courier/data/repository/OrderRepository.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/service/TambalBanFlowViewModel.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/service/TambalBanFlowScreen.kt`
- `android-app/app/src/main/java/com/tembus/courier/ui/screens/service/ServiceAdjustmentProposalCard.kt`

## Verification observed

- Backend baseline commit: `ea18d39ae55f6eef47c2441c055ba1b82012a069` (`test: cover roadside adjustment safety cap`).
- CI/CD Staging run `34007862440`: **SUCCESS**.
- Backend tests cover service validation/safety limits and the transactional implementation is present on current `staging`.
- Current `staging` also contains the customer approval/rejection UI and courier proposal UI described above.

## Manual device comparison

Recommended device flow:
1. Create a Tambal Ban order from a server-authoritative quote.
2. Assigned technician starts the roadside workflow and proposes an extra material/labor item.
3. Customer opens order detail and sees the pending adjustment without the old total being silently replaced.
4. Reject once and verify total stays unchanged.
5. Create another proposal, approve it, and verify the final displayed total becomes the proposed total.
6. Retry the same decision request / repeat tap scenario and verify no duplicate monetary change occurs.

## Not claimed by this status file

- No claim that a physical-device end-to-end run has been performed in this session.
- No claim that external payment collection for the newly approved delta has been live-settled; approved delta is represented as `pending_collection` for downstream collection/reconciliation.
- No master-task checkbox was modified by this file.
