import { Plus, Save, Trash2 } from 'lucide-react'
import AssetPicker from '../components/experience/AssetPicker'
import TargetingEditor from '../components/experience/TargetingEditor'
import { hasAudienceConstraints, type ExperienceForm, type ExperienceSection } from '../components/experience/types'

type Props = {
  value: ExperienceForm
  onChange: (value: ExperienceForm) => void
  onSave: () => void
  onCancel: () => void
  saving?: boolean
  disabled?: boolean
  isRevision?: boolean
}

const inputClass = 'mt-1 w-full rounded-xl border border-white/10 bg-white/5 px-3 py-2.5 text-sm text-zinc-100 outline-none transition focus:border-primary/60 focus:ring-2 focus:ring-primary/20 disabled:opacity-50'

const componentOptions = [
  ['hero_banner', 'Hero banner'],
  ['campaign_strip', 'Campaign strip'],
  ['info_card', 'Info card'],
  ['notice', 'Notice'],
  ['campaign_intro', 'Campaign intro'],
  ['service_grid', 'Service grid'],
  ['spacer', 'Spacer'],
] as const

const newSection = (component: string): ExperienceSection => {
  const defaults: Record<string, Record<string, unknown>> = {
    hero_banner: { title: '', body: '', cta_label: 'Lihat sekarang', deep_link: '/food' },
    campaign_strip: { title: '', body: '', cta_label: 'Lihat sekarang', deep_link: '/food' },
    info_card: { title: '', body: '' },
    notice: { title: '', body: '' },
    campaign_intro: { enabled: true, campaign_id: 'campaign', title: '', body: '' },
    service_grid: { title: 'Services', service_codes: ['food_delivery'], display_mode: 'cards' },
    spacer: { size: 'medium' },
  }
  return { id: `${component}-${Date.now()}`, component, properties: defaults[component] ?? {} }
}

const csv = (value: unknown) => Array.isArray(value) ? value.join(', ') : ''

export default function AppExperienceEditor({ value, onChange, onSave, onCancel, saving = false, disabled = false, isRevision = false }: Props) {
  const update = (patch: Partial<ExperienceForm>) => onChange({ ...value, ...patch })
  const updateSection = (index: number, patch: Partial<ExperienceSection>) => update({ sections: value.sections.map((section, sectionIndex) => sectionIndex === index ? { ...section, ...patch } : section) })
  const setProperty = (index: number, key: string, propertyValue: unknown) => updateSection(index, { properties: { ...value.sections[index].properties, [key]: propertyValue } })
  const canaryError = value.rollout_stage === 'canary' && !value.canary_cohort.trim()
  const broadWarning = !hasAudienceConstraints(value.targeting) && value.rollout_stage === 'public'

  return (
    <section className="space-y-5 rounded-3xl border border-white/10 bg-white/[0.03] p-5 shadow-xl shadow-black/10" aria-labelledby="experience-editor-title">
      <div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[0.2em] text-primary-light">Approved component schema</p><h2 id="experience-editor-title" className="mt-1 text-xl font-black text-zinc-100">{isRevision ? 'Create new revision' : 'Campaign / home layout editor'}</h2><p className="mt-1 text-xs text-zinc-500">Content is validated by the server before it can be published.</p></div><div className="flex gap-2"><button type="button" onClick={onCancel} className="rounded-xl border border-white/10 px-3 py-2 text-xs font-black uppercase tracking-widest text-zinc-400">Cancel</button><button type="button" disabled={disabled || saving || canaryError} onClick={onSave} className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2 text-xs font-black uppercase tracking-widest text-white disabled:opacity-50"><Save size={14} />{saving ? 'Saving...' : isRevision ? 'Create revision' : 'Save draft'}</button></div></div>
      {broadWarning ? <div className="rounded-2xl border border-amber-500/20 bg-amber-500/10 p-4 text-xs leading-relaxed text-amber-200">Broad public audience: server will require a different authorized operator to approve this campaign before publish.</div> : null}
      {canaryError ? <div className="rounded-2xl border border-red-500/20 bg-red-500/10 p-4 text-xs text-red-200">Canary rollout requires an explicit internal/test cohort.</div> : null}
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><label className="text-xs font-bold text-zinc-400">Market code<input className={inputClass} disabled={disabled} value={value.market_code} onChange={(event) => update({ market_code: event.target.value })} placeholder="id-jk" /></label><label className="text-xs font-bold text-zinc-400">Locale<input className={inputClass} disabled={disabled} value={value.locale} onChange={(event) => update({ locale: event.target.value })} placeholder="id-ID" /></label><label className="text-xs font-bold text-zinc-400">Surface<select className={inputClass} disabled={disabled} value={value.surface} onChange={(event) => update({ surface: event.target.value as ExperienceForm['surface'] })}><option value="customer_android">Customer Android</option><option value="customer_web">Customer Web</option><option value="merchant_android">Merchant Android</option><option value="courier_android">Courier Android</option></select></label><label className="text-xs font-bold text-zinc-400">Schema version<input type="number" min="1" max="10" className={inputClass} disabled={disabled} value={value.schema_version} onChange={(event) => update({ schema_version: Number(event.target.value) })} /></label><label className="text-xs font-bold text-zinc-400">Minimum app version<input className={inputClass} disabled={disabled} value={value.min_app_version} onChange={(event) => update({ min_app_version: event.target.value })} placeholder="1.0.0" /></label><label className="text-xs font-bold text-zinc-400">Maximum app version<input className={inputClass} disabled={disabled} value={value.max_app_version} onChange={(event) => update({ max_app_version: event.target.value })} placeholder="Optional" /></label><label className="text-xs font-bold text-zinc-400">Start (local time)<input type="datetime-local" className={inputClass} disabled={disabled} value={value.starts_at} onChange={(event) => update({ starts_at: event.target.value })} /></label><label className="text-xs font-bold text-zinc-400">End (local time)<input type="datetime-local" className={inputClass} disabled={disabled} value={value.ends_at} onChange={(event) => update({ ends_at: event.target.value })} /></label><label className="text-xs font-bold text-zinc-400">Schedule timezone<input className={inputClass} disabled={disabled} value={value.schedule_timezone} onChange={(event) => update({ schedule_timezone: event.target.value })} placeholder="Asia/Jakarta" /></label><label className="text-xs font-bold text-zinc-400">Rollout stage<select className={inputClass} disabled={disabled} value={value.rollout_stage} onChange={(event) => update({ rollout_stage: event.target.value as ExperienceForm['rollout_stage'], canary_cohort: event.target.value === 'canary' ? value.canary_cohort : '' })}><option value="public">Public</option><option value="canary">Canary / internal</option></select></label><label className="text-xs font-bold text-zinc-400">Canary cohort<input className={inputClass} disabled={disabled || value.rollout_stage !== 'canary'} value={value.canary_cohort} onChange={(event) => update({ canary_cohort: event.target.value })} placeholder="internal-test" /></label><label className="text-xs font-bold text-zinc-400">Cache policy<select className={inputClass} disabled={disabled} value={value.cache_policy} onChange={(event) => update({ cache_policy: event.target.value as ExperienceForm['cache_policy'] })}><option value="private">Private</option><option value="no-store">No store</option><option value="public">Public</option></select></label></div>
      <TargetingEditor value={value.targeting} disabled={disabled} onChange={(targeting) => update({ targeting })} />
      <section className="rounded-2xl border border-white/10 bg-black/10 p-5" aria-labelledby="component-builder-title"><div className="flex items-center justify-between gap-3"><div><h3 id="component-builder-title" className="text-sm font-black uppercase tracking-wider text-zinc-200">Layout components</h3><p className="mt-1 text-xs text-zinc-500">Compose banners, campaign cards and home sections from the allowlist.</p></div><button type="button" disabled={disabled} onClick={() => update({ sections: [...value.sections, newSection('hero_banner')] })} className="inline-flex items-center gap-1 rounded-xl border border-primary/30 bg-primary/10 px-3 py-2 text-[10px] font-black uppercase tracking-widest text-primary-light disabled:opacity-50"><Plus size={14} /> Add component</button></div><div className="mt-4 space-y-4">{value.sections.map((section, index) => <article key={section.id} className="rounded-2xl border border-white/10 bg-white/[0.03] p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><label className="flex-1 text-xs font-bold text-zinc-400">Component ID<input className={inputClass} disabled={disabled} value={section.id} onChange={(event) => updateSection(index, { id: event.target.value })} /></label><label className="sm:w-56 text-xs font-bold text-zinc-400">Type<select className={inputClass} disabled={disabled} value={section.component} onChange={(event) => { const component = event.target.value; updateSection(index, { component, properties: newSection(component).properties }) }}>{componentOptions.map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></label><button type="button" disabled={disabled || value.sections.length <= 1} aria-label={`Remove component ${section.id}`} onClick={() => update({ sections: value.sections.filter((_, sectionIndex) => sectionIndex !== index) })} className="mt-5 rounded-lg p-2 text-zinc-600 transition hover:bg-red-500/10 hover:text-red-300 disabled:opacity-50"><Trash2 size={16} /></button></div><div className="mt-4 grid gap-3 md:grid-cols-2">{section.component === 'spacer' ? <label className="text-xs font-bold text-zinc-400">Size<select className={inputClass} disabled={disabled} value={String(section.properties.size ?? 'medium')} onChange={(event) => setProperty(index, 'size', event.target.value)}><option value="small">Small</option><option value="medium">Medium</option><option value="large">Large</option></select></label> : section.component === 'service_grid' ? <><label className="text-xs font-bold text-zinc-400">Title<input className={inputClass} disabled={disabled} value={String(section.properties.title ?? '')} onChange={(event) => setProperty(index, 'title', event.target.value)} /></label><label className="text-xs font-bold text-zinc-400">Service codes<input className={inputClass} disabled={disabled} value={csv(section.properties.service_codes)} onChange={(event) => setProperty(index, 'service_codes', event.target.value.split(',').map((item) => item.trim().toLowerCase()).filter(Boolean))} placeholder="food_delivery" /></label><label className="text-xs font-bold text-zinc-400">Display mode<select className={inputClass} disabled={disabled} value={String(section.properties.display_mode ?? 'cards')} onChange={(event) => setProperty(index, 'display_mode', event.target.value)}><option value="cards">Cards</option><option value="compact">Compact</option></select></label></> : <>{(['title', 'body', 'badge', 'cta_label', 'deep_link', 'image_asset_id', 'media_asset_id', 'icon_asset_id', 'campaign_id'].filter((key) => ['info_card'].includes(section.component) ? ['title', 'body', 'deep_link', 'icon_asset_id'].includes(key) : section.component === 'campaign_intro' ? ['campaign_id', 'title', 'body', 'media_asset_id'].includes(key) : section.component === 'notice' ? ['title', 'body', 'cta_label', 'deep_link'].includes(key) : ['title', 'body', 'badge', 'cta_label', 'deep_link', 'image_asset_id'].includes(key)).map((key) => <label key={key} className="text-xs font-bold capitalize text-zinc-400">{key.replaceAll('_', ' ')}<input className={inputClass} disabled={disabled} value={String(section.properties[key] ?? '')} onChange={(event) => setProperty(index, key, event.target.value)} placeholder={key.includes('deep_link') ? '/food' : ''} /></label>))}</>}</div></article>)}</div></section>
      <AssetPicker value={value.asset_references} disabled={disabled} onChange={(asset_references) => update({ asset_references })} />
    </section>
  )
}
