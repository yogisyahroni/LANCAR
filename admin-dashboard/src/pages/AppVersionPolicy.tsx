/**
 * Canonical App Experience route for market/platform release policy.
 * The legacy Mobile Release Policies route remains available as a deep-link
 * compatibility alias and reuses this same implementation.
 */
export { default } from './MobileReleasePolicies'
