import { AlertTriangle, CalendarClock, CheckCircle2, Image, ShieldAlert, X } from 'lucide-react'
import type { LibraryAsset } from './assetLibraryTypes'
import AssetUsagePanel from './AssetUsagePanel'

const lifecycleLabel: Record<LibraryAsset['lifecycle'], string> = {
  active: 'active',
  deprecated: 'deprecated',
  scheduled_deletion: 'scheduled deletion',
}

const lifecycleClass: Record<LibraryAsset['lifecycle'], string> = {
  active: 'border-emerald-500/30 bg-emerald-500/10 text-emerald-300',
  deprecated: 'border-zinc-500/30 bg-zinc-500/10 text-zinc-300',
  scheduled_deletion: 'border-amber-500/30 bg-amber-500/10 text-amber-300',
}

const bytesLabel = (value?: number) => value ? `${(value / (1024 * 1024)).toFixed(2)} MiB` : 'Unknown'

export default function AssetDetailDrawer({ asset, onClose }: { asset: LibraryAsset; onClose: () => void }) {
  const isVideo = asset.content_type?.startsWith('video/') || asset.kind === 'video'
  const liveReference = asset.usages.some((usage) => usage.state === 'published')
  const surfaces = Array.from(new Set(asset.usages.map((usage) => usage.surface)))
  const placements = Array.from(new Set(asset.usages.flatMap((usage) => usage.placements)))
  return <aside className="rounded-3xl border border-white/10 bg-zinc-950/80 p-5 shadow-2xl" aria-labelledby="asset-detail-title"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[0.25em] text-primary-light">Asset detail</p><h2 id="asset-detail-title" className="mt-2 break-all text-xl font-black text-zinc-100">{asset.asset_id}</h2><p className="mt-1 text-xs text-zinc-500">Immutable version {asset.version || '1'} · SHA-256 {asset.checksum.slice(0, 12)}…</p></div><button type="button" onClick={onClose} aria-label="Close asset detail" className="rounded-lg p-2 text-zinc-500 hover:bg-white/10 hover:text-zinc-200"><X size={17} /></button></div><div className="mt-5 overflow-hidden rounded-2xl border border-white/10 bg-black/30">{asset.uri ? isVideo ? <video src={asset.uri} controls className="max-h-64 w-full object-contain" aria-label={`Preview ${asset.asset_id}`} /> : <img src={asset.uri} alt={`Preview ${asset.asset_id}`} className="max-h-64 w-full object-contain" /> : <div className="flex h-40 items-center justify-center text-zinc-700"><Image size={34} /></div>}</div><div className="mt-4 flex flex-wrap gap-2"><span className={`rounded-full border px-3 py-1 text-[10px] font-black uppercase tracking-widest ${lifecycleClass[asset.lifecycle]}`}>{asset.lifecycle === 'active' ? <CheckCircle2 size={12} className="mr-1 inline" /> : asset.lifecycle === 'scheduled_deletion' ? <CalendarClock size={12} className="mr-1 inline" /> : <AlertTriangle size={12} className="mr-1 inline" />}{lifecycleLabel[asset.lifecycle]}</span>{liveReference ? <span className="rounded-full border border-red-500/30 bg-red-500/10 px-3 py-1 text-[10px] font-black uppercase tracking-widest text-red-300"><ShieldAlert size={12} className="mr-1 inline" />live reference protected</span> : null}</div><dl className="mt-4 grid gap-3 text-xs sm:grid-cols-2"><div><dt className="text-zinc-600">Content type</dt><dd className="mt-1 text-zinc-200">{asset.content_type || 'Unknown'} · {asset.kind}</dd></div><div><dt className="text-zinc-600">Dimensions / size limit</dt><dd className="mt-1 text-zinc-200">{asset.width && asset.height ? `${asset.width} × ${asset.height}` : 'Unknown'} · {bytesLabel(asset.size_limit_bytes)}</dd></div><div><dt className="text-zinc-600">URI</dt><dd className="mt-1 break-all text-zinc-200">{asset.uri || 'Not registered'}</dd></div><div><dt className="text-zinc-600">Fallback</dt><dd className="mt-1 text-zinc-200">{asset.fallback_asset_id || 'None declared'}</dd></div><div><dt className="text-zinc-600">Expires</dt><dd className="mt-1 text-zinc-200">{asset.expires_at ? new Date(asset.expires_at).toLocaleString('id-ID') : 'No expiry'}</dd></div><div><dt className="text-zinc-600">Retention until</dt><dd className="mt-1 text-zinc-200">{asset.retention_until ? new Date(asset.retention_until).toLocaleString('id-ID') : 'Not scheduled'}</dd></div></dl><div className="mt-4 rounded-2xl border border-sky-500/20 bg-sky-500/[0.06] p-4 text-xs leading-relaxed text-sky-100"><p className="font-black uppercase tracking-widest">Compatibility hints</p><p className="mt-2">Observed surfaces: {surfaces.length ? surfaces.join(', ') : 'not referenced yet'} · placements: {placements.length ? placements.join(', ') : 'not classified'}. Final compatibility remains server-validated by the component and manifest schemas.</p></div><div className="mt-5 rounded-2xl border border-amber-500/20 bg-amber-500/[0.06] p-4 text-xs leading-relaxed text-amber-100"><p className="font-black uppercase tracking-widest">Historical safety</p><p className="mt-2">There is no destructive delete action here. Live and retained rollback references stay immutable; remove an asset only through a new versioned manifest after the retention window.</p></div><div className="mt-5"><div className="mb-3 flex items-center justify-between gap-3"><h3 className="text-sm font-black text-zinc-200">Usage references</h3><span className="text-[10px] font-black uppercase tracking-widest text-zinc-600">{asset.usages.length} revision{asset.usages.length === 1 ? '' : 's'}</span></div><AssetUsagePanel usages={asset.usages} /></div></aside>
}
